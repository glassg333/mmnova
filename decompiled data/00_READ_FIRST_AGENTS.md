# 00_READ_FIRST_AGENTS.md — прочти ПЕРВЫМ (v2, 2026-09-24)

Это сортированное дерево декомпилированных данных Novation MiniNova (MNM/SFX-60).
Собрано из **полного клона** `github.com/glassg333/mmnova@main` (2043 файла) + whitelist-копии
верифицированных паков. Дедуп по sha256: **каждый уникальный байт присутствует ровно один раз**.
Проверка целостности: `sha256sum -c SHA256SUMS.txt`. Док-свидетельство: `SORTING_REPORT_2026-09-24_v2.md`.

## ВАЖНОЕ В v2 (что изменилось после v1)
- Добавлены **34 файла, отсутствовавших в v1**: причина — рабочая копия собиралась из устаревшего
  клона, репозиторий обновлялся. Среди них: **`digitone2.txt`** и **`routing.txt`** (в корне дерева,
  как в оригинале), итерации добычи **19–24** в `12_mining_archive/filter_phaser_pack/`
  (coldfire transport/clock, echo kernel routing, phaser LFO, dsnd/dtim routing, dfb knob spec map),
  свежий `mnm_fm_exact.hpp`, док `MNM BLOCKS FOR JUCE/synth → dist → … → dsnd.txt`.
- Скриншот эмулятора: **`06_docs/reference_screenshots/ram program show hex on emulation.png`**
  (рядом одноимённый .txt). В v1 он терялся при сортировке — теперь место фиксированное.
- `12_mining_archive/filter_phaser_pack/` теперь содержит **ВСЕ 24 итерации** (01–24) целиком,
  включая `09_juce_port`, `22_fm_machines`, `19_m13_reverb`, `20_coldfire_host`.

## УРОВНИ ДОВЕРИЯ
- **T0-сырьё** — прошивка/образы памяти/листинги/таблицы, снятые с железа или образов: перепроверять НЕ нужно.
- **T0-результат** — модели, доказанные бит-в-бит сверкой с `dsp1_pmem.bin` + смоук-тестом: НЕ перепроверять.
- **T1** — документы, инструменты, аудиты с пруфами.
- **T2** — правдоподобное: чужой C++/JUCE-кандидаты, не сверенные бит-в-бит.
- **T3** — архив истории добычи (сырые попытки).
- **НЕ ДАННЫЕ** — гипотезы/фейки → `13_quarantine/`.

## ЗАКРЫТО (T0, не тратить время агентов на перепроверку)
| Тема | Результат | Где пруф |
|---|---|---|
| Фильтр m1 (SVF+halfband) | двойной SVF, halfband $F528BD/$4A4DF0, резонанс HPQ/LPQ | `03_listings`, `04_tables`, exp-серия f2/f3 |
| AMP-env фильтра | P:$088E–$08D7 | `03_listings/dispatch/dsp1_kernel_P0000-0B4D.txt` |
| Тембр/страница голоса | карта оффсетов $00–$28 (AMP ATK/HOLD/DEC/REL=$00–$03, FILT BASE/WDTH/HPQ/LPQ/ATK/DEC/BOFS/WOFS=$10–$17, TRIG_EVENT=$28) | эксп. журналы, `06_docs` |
| DIST (m2) | kDistRel $141A00 | листинги + сверка кривых |
| Фейзер (m18) | модулируемая линия задержки, 2 аллпасса, кубическое ядро (−1/6, −1/3, +1, +1/6), LFO sin/cos 8K | `12_mining_archive/filter_phaser_pack/14_m18_phaser`, `18_m18_final` |
| LFO | квадратура 8K, формы | `22_phaser_lfo`, `12_lfo_shapes` |
| Эхо/дилей | маршрутизация ядра | `21_echo_kernel_routing` |
| Реверб (m13) | FDN | `19_m13_reverb`, exp18_* |
| Диспетчер машин | P:$10016B/$10018D/$1001AF — таблицы указателей | `03_listings/dispatch/dsp1_dispatch.txt` |
| FM-машины (законы) | ratio=$141A80/2^19; уровни (K/64)² с wrap; друп 1−((K−64)/128)⁴; шейпер G=(K−64)/16; FB=K·32; скрытый ×2 op1; 1FIN=1+(K−64)/256; питч $140000=2048 шагов/октаву; TUNE +683·K/128 | `mnm_fm_exact.hpp`, exp27/exp28, `22_fm_machines` |

## IMPORT-READY (можно брать в JUCE-проект как есть)
- `09_dsp_models_verified/09_juce_port/Source/` — порт ядра, сверка с `dsp1_pmem.bin` + смоук-тест.
- `09_dsp_models_verified/22_fm_machines/mnm_fm_exact.hpp` — точные FM-законы (T0-результат).
- `10_juce_import_ready/01_CORE_VERIFIED/` — верифицированное ядро + TEST_main.cpp, TEST_REPORT.txt.
- `10_juce_import_ready/clean_proof_audit/` — чистовая версия с пруфами (ROUTING_BUFFER_EXACT.md и др.).
- Таблицы: `04_tables/` (523+ шт., .bin + .txt рядом).

## ОТКРЫТО (можно добывать)
- Питч-абсолют: необъяснённый множитель ×1.118 в цепочке.
- Конвейер 48-битной фазы фейзера (тонкости округления).
- Машина 33 (FX-EXT) и хвосты машин 27–33.
- ADSR-блок P:$04A8–$04F4 (общая огибающая).
- Карта X:$FFFF00 (внешняя память: параметры/команды).
- SID (m3): деталиring-mod.
- DIST: модуляция кривых (knob map см. `24_dfb_knob_spec_map`).

## ПРАВИЛА ДЛЯ АГЕНТОВ
1. НЕ перепроверяй T0 (таблица выше). Не пересчитывай то, что уже бит-в-бит доказано.
2. Работай от `04_tables` и `09_dsp_models_verified`, а не сырых листингов, если задача не про добычу нового.
3. Новый результат кладёт файл в `12_mining_archive/<тема>/` (сырец) и, после верификации, в `09_dsp_models_verified/` (+ строку в отчёт).
4. Гипотезы без пруфов — только в `13_quarantine/hypotheses_unverified/`, с пометкой HYPOTHESIS в имени.
5. Не копируй большие файлы в воркспейс (квота 128 МБ / 10 000 файлов). Архивы не распаковывать внутрь дерева.
6. Изменяешь дерево — обнови `SHA256SUMS.txt`, `MANIFEST_SORTED.json` и допиши `SORTING_REPORT_2026-09-24_v2.md`.
