
## ВОСПРОИЗВЕДЕНИЕ БОЛЬШИХ ДАМПОВ (не включены в пакет — генерируются скриптами)

Нужен только BIN прошивки из репозитория glassg333/mmnova:

    python3 scripts/mm_final_extract.py

генерирует:
- my_dsp1_pmem.bin / my_dsp2_pmem.bin (P-образы 1,343,588 слов, 0 расхождений с repo decompiled data/)
- my_dsp{1,2}_{x,y}mem.bin (в пакете)
- sec{1,2,3}_full.bin — сырые потоки записей заливки (LE24)
- mm_work/... 

    python3 -c "import sys; sys.path.insert(0,'scripts'); from mm_final_extract import depack_full; from mm_aplib_mm import read_sections; d=open('elektron_sfx6-60_os1.32b.bin','rb').read(); s=read_sections(d); o,i=depack_full(d,s[0]['off']+8,s[0]['off']+8+s[0]['slen']); open('sec0_full.bin','wb').write(o)"

генерирует sec0_full.bin — ColdFire main (425,858 байт), дескрипторы машин @0x58540+.


пример незаконченой работы
Сначала я декодирую fx_descriptors_region.bin, чтобы правильно определитьFX slots 20, 32, 33.

Теперь я вижу реальную структуру. Файл содержит 9 блоков по 0xB0 байт. Позвольте мне исправить парсер — проблема в том, что имя начинается с 0x05, а не с 0x06, а подимя — с 0x0E:

Дамп показал, что у меня смещение на 1 байт: имя начинается с 0x05 (сразу после page_id), а не с 0x06. Подимя находится с 0x0D, а не с 0x0E. Позвольте мне также взглянуть на фактическую структуру: 51 11 11 11 0c 46 58 00 00 00 00 00 00 06 54 48 52 55 00 00 00 00 00 2d 2d 2d 00 00 00 2d 2d 2d 00 ...

Подожди — 0c = page_id 12 ✓, затем 46 58 = "FX", но затем идут нули, а 06 54 48 52 55 = длина тега 06 + "THRU". Таким образом, имя «FX» находится с 0x05, а page-2 подимя «THRU» — с 0x0E. Позвольте мне декодировать с правильным макетом.

Парсер был неверным — параметр имеет 6-байтовый шаг (4 символа + 2 нуля), а не 8-байтовый. Позвольте мне исправить это, а также изучить листинги m32/m33, чтобы определить, чем они являются на самом деле.

m32 считывает 7 параметров ($4–$A) и использует задержку $114000 — это похоже на эффект задержки/эха. Позвольте мне посмотреть на m33:

Таблица LP-фильтра в $144AC7 — это просто непрерывный поток данных, неправильно дизассемблированный как код. Позвольте мне извлечь её как необработанные слова. Позвольте мне написать комплексный скрипт для извлечения таблиц:

Теперь я просканирую код ядра в поисках вспомогательных функций LFO, а также изучу m32/m33, чтобы определить их типы:

dsp1_machine_code.txt также представляет собой необработанный дизассемблированный код машин. Логика LFO должна находиться внутри самих машин (PHASER, FLANGER, CHORUS). Позвольте мне взглянуть на диспетчеризацию фреймов ядра, чтобы найти общие подпрограммы LFO:

Дизассемблирование ядра очень грубое (дизассемблер в основном не справляется). Логика LFO встроена в каждую машину (PHASER, FLANGER, CHORUS). Позвольте мне поискать общие подпрограммы LFO по ссылкам на таблицу синусов и паттернам LFO:

Логика LFO встроена в каждую машину (PHASER, FLANGER, CHORUS считывают параметр SPD по адресу y:(r6+$6) и обновляют фазу LFO, хранящуюся в y:(r6+$24)/$25/$26). LFO не реализованы как общие подпрограммы ядра — это встроенный код. Это уже задокументировано в файлах листингов.