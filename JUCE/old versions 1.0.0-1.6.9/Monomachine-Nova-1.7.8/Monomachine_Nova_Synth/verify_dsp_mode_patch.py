#!/usr/bin/env python3
"""Static checks for the DSP MODE / filter modes patch (revision v6).

Компилятор не нужен: скрипт читает исходники и проверяет обещания правки.

  1. dsp/mnm/MnmRealFilter.hpp -- основной фильтр: реальные таблицы, устойчивый
     TPT-SVF, DC-блокер, порядок аргументов страницы FILT, живое отпускание ноты.
  2. dsp/mnm/MnmKernel.hpp -- дубль FilterCore/real_detail удалён, std::clamp исправлен.
  3. dsp/mnm/MnmFilterModes.hpp (cascade/dual/raw) и альтернативные движки
     MnmDist2.hpp / MnmFm2.hpp / MnmBbox2.hpp -- УДАЛЕНЫ (v5/v6).
     режимов у каждого раздела, схема состояния 10.
     никаких веток dist2/fm2/bbox2, хорус и FX как в 1.6.0 (линейный INP, без домешиваний).
  6. dsp/monomachine_bbox.hpp -- BBOX: старый закон RTIM (5...255 мс) с инверсией,
     интервал никогда не 0, сглаживание по сэмплам, рабочий кит = старый + снейр/томы.
  7. PluginProcessor.cpp -- одно правило допустимости режима + миграция на схему 10.
  9. Обещания 1.6.5: unity-дилей (wet отдельно, dry не удваивается), repitch-slew
     без кликов, раздельные FM-файлы (stat/par), FM+STAT old удалён, FM+PAR old
     с устойчивым полом огибающих, ROUTE скрыт из меню, папки-категории и
     описание цепочки, параметр dly_repitch, drag-and-release модуляция с
     кнопок LFO, /utf-8 в .jucer и CMake для MSVC.

Run:  python3 verify_dsp_mode_patch.py <path-to-Monomachine_Nova_FX/Source>
"""

from pathlib import Path
import re
import sys


def read(path: Path) -> str:
    return path.read_text(encoding="utf-8", errors="replace")


def balanced(text: str) -> bool:
    return text.count("{") == text.count("}")


def without_comments(text: str) -> str:
    return "\n".join(line for line in text.splitlines() if not line.lstrip().startswith("//"))


def main() -> int:
    root = Path(sys.argv[1] if len(sys.argv) > 1 else ".").resolve()
    real = root / "dsp" / "mnm" / "MnmRealFilter.hpp"
    kernel = root / "dsp" / "mnm" / "MnmKernel.hpp"
    removed = root / "dsp" / "mnm" / "MnmFilterModes.hpp"
    bbox = root / "dsp" / "monomachine_bbox.hpp"
    modes = root / "models" / "DspModes.hpp"
    dsp = root / "NovaDSP.h"
    editor = root / "PluginEditor.cpp"
    processor = root / "PluginProcessor.cpp"

    for path in (real, kernel, bbox, modes, dsp, editor, processor):
        if not path.is_file():
            raise AssertionError(f"missing file: {path}")

    tests = root.parent / "tests" / "DeliveryTests.cpp"

    failures = []

    def check(ok: bool, message: str) -> None:
        print(("OK   " if ok else "FAIL ") + message)
        if not ok:
            failures.append(message)

    def present(text: str, needle: str, message: str) -> None:
        check(needle in text, message)

    def absent(text: str, needle: str, message: str) -> None:
        check(needle not in text, message)

    # 1. Основной фильтр
    text = read(real)
    check(balanced(text), "MnmRealFilter.hpp: скобки сбалансированы")
    present(text, "class RealFilterCore", "MnmRealFilter.hpp: определён RealFilterCore")
    present(text, "using FilterCore = RealFilterCore", "MnmRealFilter.hpp: FilterCore = реальный фильтр")
    present(text, "class MnmResoSVF", "MnmRealFilter.hpp: резонанс -- устойчивый TPT-SVF")
    present(text, "DcBlocker", "MnmRealFilter.hpp: DC-блокер на выходе активного фильтра")
    present(text, "kMaxQ", "MnmRealFilter.hpp: закон Q вынесен в константу kMaxQ")
    present(text, "float envAtk, float envDec, float bofs, float wofs",
            "MnmRealFilter.hpp: порядок аргументов = страница FILT (ATK, DEC, BOFS, WOFS)")
    present(text, "release() noexcept { released_ = true; }",
            "MnmRealFilter.hpp: отпускание ноты возвращает огибающую к нулю")
    absent(text, "bp1 += g1", "MnmRealFilter.hpp: старого Chamberlin больше нет")
    absent(text, "class FilterCore {", "MnmRealFilter.hpp: нет второго класса FilterCore")

    # 2. Ядро без дубля
    text = read(kernel)
    absent(text, "class FilterCore {", "MnmKernel.hpp: дубль класса FilterCore удалён")
    absent(text, "kRealSVF_x0_F528BD", "MnmKernel.hpp: дубль констант SVF удалён")
    absent(text, "namespace real_detail", "MnmKernel.hpp: дубль real_detail удалён")
    present(text, "class FilterCoreLegacy", "MnmKernel.hpp: резервный FilterCoreLegacy сохранён")
    absent(text, "std::clamp(attack,0,127)", "MnmKernel.hpp: std::clamp(float,0,127) исправлен")

    # 3. Мусорные режимы удалены
    check(not removed.is_file(), "dsp/mnm/MnmFilterModes.hpp удалён из сборки (*.bak-v5)")
    for name in ("MnmDist2.hpp", "MnmFm2.hpp", "MnmBbox2.hpp"):
        check(not (root / "dsp" / "mnm" / name).is_file(),
              f"dsp/mnm/{name} удалён из сборки (*.bak-v6)")

    live = [p for p in (dsp, modes, processor, editor, real, kernel, bbox) ]
    for word in ("MnmFilterModes.hpp", "SvfCascadeFilter", "DualSectionFilter", "TruthfulRawFilter",
                 "cascadeFilter", "dualFilter", "rawFilter", "rawBlockCounter",
                 "dspModeCascade", "dspModeDual", "dspModeRaw", "filt_detail",
                 "MnmDist2.hpp", "MnmFm2.hpp", "MnmBbox2.hpp",
                 "Dist2Core", "FMPlusBlock2", "Bbox2Core",
                 "dspModeDist2", "dspModeFm2", "dspModeBbox2",
                 "fm2Core", "bbox2Core", "dist2Core",
                 "dspModeIsDistOnly", "dspModeIsSyntOnly"):
        hits = [p.name for p in live if word in without_comments(read(p))]
        if hits:
            check(False, f"{word} остался в коде: " + ", ".join(hits))
        else:
            check(True, f"{word} не встречается в коде DSP-части")

    # 4. Реестр режимов
    text = read(modes)
    present(text, '"mnm|old|fma"', "1.6.14 DspModes.hpp: список режимов = mnm|old|fma")
    check(re.search(r"dspModeMnm\s*=\s*0\b", text) is not None, "dspModeMnm = 0 (основной)")
    check(re.search(r"dspModeOld\s*=\s*1\b", text) is not None, "dspModeOld = 1 (резерв)")
    check(re.search(r"dspModeCount\s*=\s*3\b", text) is not None, "1.6.14 dspModeCount = 3 (mnm/old/fma)")
    present(text, "dspSectionModeChoices", "DspModes.hpp: список режимов на каждый раздел")
    present(text, "dspModeAllowedForSection", "DspModes.hpp: правило допустимости режима")
    present(text, "dspModeLegacyIndexToCurrent", "DspModes.hpp: перевод старой нумерации режимов")
    present(text, "kDspModeSchemaVersion = 13", "DspModes.hpp: версия схемы состояния = 13 (1.7.8: rate 0..1 + RMODE/KEY/LPOINT)")
    present(text, "kShowBackupModes", "DspModes.hpp: резервные режимы скрываются флагом")
    present(text, "dspMachineModeParamId", "DspModes.hpp: режим SYNT хранится по каждой машине (mode_synt_m<id>)")
    table = re.search(r"dspSectionModeChoices\(int section\)\s*\{(.*?)\n\}", text, re.S)
    check(table is not None, "DspModes.hpp: найдена таблица режимов по разделам")
    body = table.group(1) if table else ""
    for section, expect in (("DspSynt", "mnm|old|fma"), ("DspAmp", "mnm"), ("DspFilter", "mnm|old"),
                            ("DspDist", "mnm|old"), ("DspDelay", "mnm|old"),
                            ("DspRouting", "mnm"), ("DspChorus", "mnm")):
        m = re.search(r"case %s:\s+return \"([^\"]+)\"" % section, body)
        check(bool(m) and m.group(1) == expect,
              f"DspModes.hpp: {section} -> {expect}")
        present(text, r'case DspFilter: return "FILT: mnm = the real filter',
            "DspModes.hpp: у FILT основной режим описан как реальный фильтр прошивки")
    absent(text, r'"FILT: mnm = ' + '\\xd0',
            "DspModes.hpp: русские \\xNN-строки описаний режимов убраны (кракозябры)")

    # 5. Диспетчер
    text = read(dsp)
    present(text, "MnmRealFilter.hpp", "NovaDSP.h: подключён основной фильтр")
    call = re.search(r"mnmFilter\.setParameters\(([^;]*)\)", text)
    check(call is not None, "NovaDSP.h: вызов основного фильтра найден")
    if call:
        args = re.sub(r"\s+", "", call.group(1))
        check("params[20]-64" not in args and "params[21]-64" not in args,
              "NovaDSP.h: ATK/DEC не передаются как BOFS/WOFS (баг «по частотам» закрыт)")
        check(args == "fltBase,fltWidth,fltHpq,fltLpq,fltAtk,fltDec,fltBofs,fltWofs",
              "NovaDSP.h: порядок аргументов fltBase..fltWofs")
    present(text, "filter.processStereo(l,r,l,r", "NovaDSP.h: резервный режим old (MonomachineFilter) на месте")
    present(text, "bbox.setParameters(b(0),b(1),b(4),b(5),b(2),b(3)>0,false)",
            "NovaDSP.h: BBOX получает ручки как в 1.6.0 (PTCH,ST/RT,START-позиция,SLOT,RND)")
    present(text, "bbox.setChromatic(b(6)>0)", "NovaDSP.h: хроматический режим BBOX на месте")
    present(text, "if(id!=15){const float g=p[7]/64.0f;",
            "NovaDSP.h: INP для FX -- линейный, как в 1.6.0 (без квадратичного «FIXED»)")
    present(text, "const float wet=norm(p[3]);l[i]=dl*(1-wet)+wl*wet;",
            "NovaDSP.h: хорус микшируется dry->wet по MIX (как в 1.6.0, без домешивания x1.5)")
    for word in ("inpGainQuad", "chorusMakeup", "reverbMakeup", "FIXED"):
        absent(without_comments(text), word, f"NovaDSP.h: нет остатков приёма «{word}»")

    # 6. BBOX
    text = read(bbox)
    check(balanced(text), "monomachine_bbox.hpp: скобки сбалансированы")
    present(text, "float retrigMs = 5.0f + ((127.0f - static_cast<float>(rtim)) / 127.0f) * 250.0f;",
            "BBOX: старый закон RTIM (5...255 мс) с инверсией")
    absent(text, "255.0f * std::pow((127.0f-static_cast<float>(rtim))/126.0f,2.0f)",
           "BBOX: прежняя формула с интервалом 0 мс удалена")
    present(text, "static constexpr size_t kDeclickSamples = 16;",
            "BBOX: сглаживание задано в сэмплах (kDeclickSamples)")
    present(text, "samplesToRestart", "BBOX: спад доходит до нуля к перезапуску (деклик)")
    present(text, "static const bool modernVoice[24]=", "BBOX: таблица голосов нового кита")
    present(text, "x*x*x*std::min", "BBOX 1.6.12: STRT -- кубическая шкала с шапкой ~25 мс")
    present(text, "void generateLegacyKit()", "BBOX: полностью старый кит оставлен рядом")

    # 7. Аудио-поток
    text = read(processor)
    present(text, "dspModeAllowedForSection(i,idx)",
            "PluginProcessor.cpp: режим проверяется списком своего раздела")
    absent(text, "dspModeIsFilterOnly(idx)", "PluginProcessor.cpp: ad-hoc правил больше нет")
    present(text, "dspModeLegacyIndexToCurrent", "PluginProcessor.cpp: старая нумерация переводится")
    present(text, "state.setProperty(\"schema\",monomachine::kDspModeSchemaVersion,nullptr)",
            "PluginProcessor.cpp: схема состояния обновлена")
    present(text, "if(schema<10)", "PluginProcessor.cpp: миграция состояний до схемы 10")
    present(text, "static_cast<float>(monomachine::dspModeOld),nullptr",
            "PluginProcessor.cpp: старые состояния без режимов заполняются old (как в DeliveryTests)")

    # 8. Меню
    text = read(editor)
    bounds = re.search(r"dspModeButton\.setBounds\(([^)]*)\)", text)
    check(bounds is not None, "PluginEditor.cpp: dspModeButton позиционируется")
    if bounds:
        values = [int(v) for v in re.findall(r"-?\d+", bounds.group(1))]
        x, y = values[0], values[1]
        check(y != 58 or x >= 1014, "PluginEditor.cpp: dspModeButton не залезает на строку ARP")
    present(text, "dspModePrimaryForSection", "PluginEditor.cpp: в меню основной режим идёт первым")
    present(text, "dspModeAllowedForSection", "PluginEditor.cpp: меню предлагает только режимы раздела")
    present(text, "BACKUP (not the primary engine):", "PluginEditor.cpp: резервные режимы помечены отдельной группой (1.6.8: текст на английском)")
    present(text, "ENCODING TEST: ", "PluginEditor.cpp: проверочная строка кодировки на месте")
    absent(text, "DSP MODE -- \\xd0", "PluginEditor.cpp: русские \\xNN-строки тултипа убраны")

    # 9. Тесты
    if tests.is_file():
        text = read(tests)
        present(text, "RTIM 127 must be about 5 ms, not zero",
                "DeliveryTests.cpp: ожидание нового закона RTIM")
        present(text, "new snare/toms missing from the working kit",
                "DeliveryTests.cpp: ожидание рабочего кита (старый + снейр/томы)")
    else:
        print("..   tests/DeliveryTests.cpp не найден рядом с проектом -- проверки тестов пропущены")

    # 10. Обещания 1.6.5
    delay = root / "dsp" / "mnm" / "MnmDelay.hpp"
    fmpar = root / "dsp" / "monomachine_fm_par.hpp"
    fmstat = root / "dsp" / "monomachine_fm_stat.hpp"
    fmold = root / "dsp" / "monomachine_fm_stat_par.hpp"
    data = root / "NovaData.h"
    header = root / "PluginProcessor.h"

    text = read(delay)
    present(text, "float send, bool pingPong,", "1.6.5 MnmDelay: send вместо mix в сигнатуре")
    present(text, "(pingPong ? 0.5f * (inL + inR) : inL) * send", "1.6.5 MnmDelay: в линию пишется вход*send (топология old, 1.6.6)")
    present(text, "outL=aL;", "1.6.5 MnmDelay: выход -- чистый wet, dry не удваивается")
    present(text, "smoothSamples", "1.6.5 MnmDelay: slewing длины линии (repitch без кликов)")

    text = read(dsp)
    present(text, "dsp/monomachine_fm_par.hpp", "1.6.5 NovaDSP: FM+PAR в отдельном файле")
    present(text, "dsp/monomachine_fm_stat.hpp", "1.6.5 NovaDSP: FM+STAT в отдельном файле")
    absent(text, "monomachine_fm_stat_par.hpp", "1.6.5 NovaDSP: совмещённый fm_stat_par не подключён")
    absent(text, "MonomachineFmStatic stat;", "1.6.5 NovaDSP: old-движок FM+STAT удалён")
    absent(text, "stat.processStereo", "1.6.5 NovaDSP: вызовов stat больше нет")
    present(text, "(syntMode==monomachine::dspModeMnm||id==8)&&(id==8||id==9||id==10)",
            "1.6.5 NovaDSP: машина 8 (FM+STAT) всегда на mnm")
    present(text, "mnmDelay.process(params[27],feedback,send,pingPong,ppMode,std::max(repitchSlew,declickSlew),dryL,dryR,wetL,wetR)",
            "1.6.5 NovaDSP: wet добавляется один раз (unity thru); 1.6.12: ppMode в вызове")
    present(text, "void setRepitch(float raw,float smoothRaw)", "1.6.5/1.6.8 NovaDSP: настройка скорости repitch (непрерывная шкала + антиклик)")

    check(fmpar.is_file(), "1.6.5 monomachine_fm_par.hpp существует")
    if fmpar.is_file():
        t2 = read(fmpar)
        check(balanced(t2), "1.6.5 fm_par: скобки сбалансированы")
        present(t2, "class MonomachineFmParallel", "1.6.5 fm_par: класс FM+PAR на месте")
        present(t2, "getFmListedRatio", "1.6.5 fm_par: таблица ratios на месте")
        present(t2, "kSustain = 0.45f", "1.6.5 fm_par: устойчивый пол 45% (1FRQ/1ENV слышны всю ноту)")
        present(t2, "0.25f * static_cast<float>(m_sampleRate)", "1.6.5 fm_par: огибающие 0.25/0.45/0.65 с вместо 20 мс")
    check(fmstat.is_file(), "1.6.5 monomachine_fm_stat.hpp существует")
    check(not fmold.is_file(), "1.6.5 совмещённый monomachine_fm_stat_par.hpp удалён (.bak-v7)")

    text = read(modes)
    present(text, "dspSectionVisibleInMenu", "1.6.5 DspModes: видимость разделов в меню")
    present(text, "return section != DspRouting && section != DspAmp;", "1.6.5 DspModes: ROUTE не выбирается (1.6.19: и AMP)")
    present(text, "dspSectionCategory", "1.6.5 DspModes: папки-категории разделов")
    present(text, "dspRouteDescription", "1.6.5 DspModes: подробное описание цепочки для наведения")

    text = read(data)
    present(text, "dly_repitch", "1.6.5/1.6.8 NovaData: параметр dly_repitch (c 1.6.8 непрерывный 0..3)")
    present(text, "dly_repitch_smooth", "1.6.8 NovaData: параметр dly_repitch_smooth (антиклик-доводка)")

    text = read(processor)
    present(text, "addRouteFromSource", "1.6.5 Processor: маршрут из drag-and-release")
    present(text, "chain.setRepitch", "1.6.5 Processor: repitch доходит до цепочки")
    if header.is_file():
        present(read(header), "int addRouteFromSource(int src, uint8_t target);",
                "1.6.33 Processor.h: addRouteFromSource возвращает слот маршрута")

    text = read(editor)
    present(text, "class ModSourceButton", "1.6.5 Editor: одинаковые кнопки LFO1/2/3")
    present(text, "lfo1Button", "1.6.5 Editor: LFO1 теперь кнопка")
    present(text, "beginModDrag", "1.6.5 Editor: модуляция прицелом перетаскиванием")
    present(text, "endModDrag", "1.6.5 Editor: отпустил на ручке -- маршрут назначен")
    present(text, "\"mseg_rate\",\"RATE\"", "1.6.5 Editor: MSEG-панель с быстрыми ручками")
    absent(text, "showLfo3", "1.6.5 Editor: старого переключателя showLfo3 нет")
    present(text, "BUILD 1.7.6", "1.6.37 Editor: маркер версии сборки (обновляется +1 каждым патчем)")
    present(text, 'showMessageBoxAsync(juce::MessageBoxIconType::NoIcon,"MONOMACHINE NOVA","mnm mod by restrange aka kraduvremya")', "1.6.29 Editor: About = только подпись, без иконки i")

    core_tests = root.parent / "tests" / "MnmCoreTests.cpp"
    if core_tests.is_file():
        text = read(core_tests)
        present(text, "DLY send=0 wet is silent", "1.6.5 Tests: unity-проверка дилея")
        present(text, "repitch slew keeps the read pointer continuous", "1.6.5 Tests: repitch без кликов")

    cmake = root.parent / "CMakeLists.txt"
    if cmake.is_file():
        present(read(cmake), "/utf-8", "1.6.5 CMake: /utf-8 для MSVC")
    jucers = list(root.parent.glob("*.jucer"))
    check(bool(jucers) and all('extraCompilerFlags="/utf-8"' in read(j) for j in jucers),
          "1.6.5 jucer: extraCompilerFlags /utf-8 в конфигах VS")

    # ---- 11. обещания 1.6.6
    text = read(root / "dsp" / "mnm" / "MnmKernel.hpp")
    present(text, "holdPeak", "1.6.6 Kernel: hold пика огибающей для HOLD")
    text = read(root / "NovaDSP.h")
    present(text, "kernelEnv.holdPeak()", "1.6.6 Envelope: атака идёт во время hold, пик заморожен")
    present(text, "kernelEnv.setParameters(p[0],p[2],p[3],0.0f)", "1.6.6 Envelope: сустейн не следует за DEC")
    text = read(root / "dsp" / "mnm" / "MnmFm.hpp")
    present(text, "1.6.6: unipolar", "1.6.6 FM DYN: фидбек 0 = нет фидбека")
    text = read(root / "dsp" / "mnm" / "MnmDelay.hpp")
    present(text, "0.5f * (inL + inR)", "1.6.6 Delay: send по закону old DSP (ping-pong моно-микс)")
    text = read(root / "NovaData.h")
    present(text, "if(m.id==9) defaultMachine=index", "1.6.6/1.7.5: FM+PAR -- машина по умолчанию при открытии")
    text = read(root / "PluginEditor.cpp")
    present(text, "class StepLane", "1.6.6 ARP: горизонтальный секвенсор с рисованием")
    present(text, "rebuildModeControls", "1.6.6: MODE-селекторы в заголовках страниц")
    present(text, "showRepitchMenu", "1.6.6: repitch дилея по ПКМ на DTIM")
    present(text, "struct RndButton", "1.6.6 ARP: рандом мгновенно, меню по ПКМ")
    text = read(root.parent / "tests" / "MnmCoreTests.cpp")
    present(text, "DLY 1.6.6 ping-pong send=0", "1.6.6 Tests: новая топология send")

    # ---- 12. обещания 1.6.8
    text = read(modes)
    present(text, 'return "MACHINES (OSC)"', "1.6.8 DspModes: категории меню на английском")
    absent(text, "return \"\\xd0", "1.6.8 DspModes: русских \\xNN-строк категорий больше нет")
    text = read(data)
    present(text, "if(section==monomachine::DspSynt)continue;", "1.6.8 NovaData: общий mode_synt больше не создаётся")
    present(text, "dspMachineModeParamId(m.id)", "1.6.8 NovaData: у каждой машины свой параметр mode_synt_m<id>")
    text = read(processor)
    present(text, "syntModeRaw[static_cast<size_t>(selectedMachine)]", "1.6.8 Processor: SYNT-режим читается из параметра текущей машины")
    present(text, "if(schema<11)", "1.6.8 Processor: миграция mode_synt -> mode_synt_m<id> (схема 11)")
    present(text, "if(slot>0)moveModRoute(slot,0);", "1.6.8 Processor: свежий маршрут LFO всплывает на первую строку матрицы")
    present(text, "if(column<1||column>7)return;", "1.6.8/41 Processor: сортируются колонки 1..7 (вкл. AUX и AUX DEPTH)")
    text = read(editor)
    present(text, "void showDspModes(int section,juce::Point<int> screen", "1.6.8 Editor: меню режимов открывается у курсора (SRR)")
    present(text, "withTargetScreenArea", "1.6.8 Editor: попап привязан к точке курсора")
    present(text, "class RepitchSliderPanel", "1.6.8 Editor: repitch DTIM -- слайдер, а не список")
    present(text, "void showCallout(std::unique_ptr<juce::Component> panel,juce::Point<int> screen,const juce::String& kind)", "1.6.8/41 Editor: доп-настройки -- нёмодальное плавающее окно (не пропадает по клику вне)")
    present(text, "const juce::String lfoPanelName=\"LFO\"", "1.6.8 Editor: заголовок LFO-панели без цифры")
    present(text, 'names[]{"SYNT","",lfoPanelName,"FILT","EFFX","MSEG"}', "1.6.8 Editor: LFO наверху, MSEG внизу (панели поменялись)")
    present(text, "aimLfoDest", "1.6.8 Editor: прицел LFO выбирает внутренний PAGE/DEST")
    present(text, "if (c >= 1 && c <= 7) sortBy(c);", "1.6.8/41 Editor: сортировка матрицы по любой колонке (вкл. AUX/AUX DEPTH)")
    present(text, '"ON", "SOURCE", "DESTINATION", "DEPTH", "MODE"', "1.6.8 Editor: заголовок ON в матрице")
    present(text, "paintingTranspose = e.y < geo.yVel", "1.6.8/1.6.13 Editor: степы ARP -- квадратные пады (TR от середины, VEL от низа, ряд в mouseDown)")
    present(text, "processor.dspModeParamIdFor(monomachine::DspSynt)", "1.6.8 Editor: SYNT-селектор привязан к параметру выбранной машины")

    # ---- 13. обещания 1.6.8.1
    text = read(editor)
    present(text, "{if(panel==5)continue;", "1.6.8.1 Editor: пропускается только MSEG-панель -- LFO-панель снова с ручками")
    absent(text, "{if(panel==2||panel==5)continue;", "1.6.8.1 Editor: старый пропуск LFO-панели убран (пустая страница LFO)")
    present(text, "lfo1Button.selected=(lfoPageSel==0)", "1.6.21 Editor: выбор LFO-страницы -- заливкой квадрата с номером")
    present(text, "void beginModDrag(int src,bool matrix)", "1.6.8.1 Editor: ЛКМ = прямой PAGE/DEST, ПКМ = матрица")
    present(text, "bool modDragMatrix", "1.6.8.1 Editor: флаг режима перетаскивания LFO")
    present(text, "lfo1Button.setBounds(975,97,52,24)", "1.6.21 Editor: три кнопки-гнезда LFO в шапке панели (номер + джек)")
    present(text, "addRow(rep, \"dly_repitch\"", "1.6.8.1 Editor: repitch -- непрерывный слайдер в call-out панели")
    present(text, "addRow(smo, \"dly_repitch_smooth\"", "1.6.8.1 Editor: SMOOTH-слайдер под REPITCH (антиклик)")
    present(text, "FOLDER: ", "1.6.8.1/1.6.12 Editor: DSP MODE -- группировка по папкам (метки FOLDER)")
    present(text, "menu.addSubMenu(title,sub)", "1.6.12 Editor: DSP MODE снова вложенные подменю (группировка по папкам)")
    text = read(dsp)
    present(text, "anchors[4]={0.0f,0.006f,0.030f,2.5f}", "1.6.8.1 NovaDSP: шкала repitch 0=OFF,1=FAST,2=MED(дефолт),3=~2.5с")
    present(text, "std::max(repitchSlew,declickSlew)", "1.6.8.1 NovaDSP: антиклик-доводка применяется к длине линии")
    text = read(data)
    absent(text, "OFF|FAST|MED|SLOW", "1.6.8.1 NovaData: список OFF/FAST/MED/SLOW больше не выбор -- шкала непрерывная")

    # ---- 14. обещания 1.6.12
    text = read(editor)
    present(text, "class DragChoice", "1.6.12 Editor: чёрный пиксельный список с драгом (DragChoice)")
    present(text, "class DragValueBox", "1.6.12 Editor: окошко значения с драгом (DragValueBox)")
    present(text, "class LfoLockPanel", "1.6.12 Editor: панель замков PAGE/DEST LFO")
    present(text, "openLocks", "1.6.12 Editor: ПКМ на PAGE/DEST LFO открывает замки")
    present(text, "void flashDestCell(int row)", "1.6.31 Editor: после бинда прицелом мигает РАМКА DEST-ячейки в матрице (не параметр)")
    present(text, "else if (((ms / 330) & 1) == 0) { g.setColour(ink); g.drawRect(colX(3) + 1, rowY(flashDestRow) + 1, colWidth(3) - 2, 29, 2); } }", "1.6.31 Matrix: рамка DEST мигает белым/черным x2, 330мс фаза")
    present(text, "if (r < 0 || c < 0) return; // 1.6.31", "1.6.31 Matrix: драг за колонками (края матрицы) больше не меняет DEPTH (был UB cells[r*5-1])")
    present(text, "if (c == 3 && e.x >= colX(c) + colWidth(c) - 22) { // 1.6.31/32", "1.6.32 Matrix: прицел по кресту (компактная иконка), замок НЕ мешает прицелу")
    present(text, "if(matrixPage)matrixPage->flashDestCell(wasEmpty?0:pickRoute);", "1.6.34 Editor: completeTargetPick -- новый маршрут всплывает наверх, мигает рамка DEST")
    present(text, "isAltDown()", "1.6.12 Editor: BPM -- целые шаги, Alt/Shift = десятые")
    present(text, "closeButton.setBounds(870,58,140,30)", "1.6.12 Editor: BACK ровно на месте MATRIX")
    present(text, "WILL LOAD", "1.6.12 Editor: серый WILL LOAD -- предпросмотр загружаемых сэмплов")
    text = read(data)
    present(text, 'prefix+"lock"', "1.6.12 NovaData: замок маршрута FREE/LOCK/SOLO")
    present(text, "dly_ppmode", "1.6.12 NovaData: режим ping-pong дилея (CLASSIC/MID SAFE)")
    present(text, "old|mnm", "1.6.12 NovaData: режимы огибающей old/mnm")
    present(text, "page_solo", "1.6.12 NovaData: solo PAGE/DEST LFO (0 = выкл, 1..8)")
    present(text, "arp_s", "1.6.12 NovaData: параметры ARP-страницы (транспозиция/velocity/hold по шагам)")
    text = read(dsp)
    present(text, "interlace", "1.6.12 NovaDSP: INTL = interlace по мануалу (волна чередуется с нулём)")
    present(text, "void setPpMode(int m){ppMode=m;}", "1.6.12 NovaDSP: MID SAFE ping-pong в DSP")
    text = read(processor)
    present(text, "chain.setPpMode", "1.6.12 PluginProcessor: ppMode протянут в DSP")
    # ---- 15. обещания 1.6.13
    text = read(editor)
    present(text, "drawPopupMenuBackgroundWithOptions", "1.6.13 Editor: чёрный пиксельный скин для ВСЕХ popup-меню")
    present(text, "drawPopupMenuItemWithOptions", "1.6.13 Editor: пункты меню -- пиксельный шрифт по центру")
    present(text, "class DsndPanel", "1.6.13 Editor: у DSND своё окно (PP MODE), DTIM -- только REPITCH/SMOOTH")
    present(text, "GUI DRAG SPEED", "1.6.13 Editor: настройки плавности прокрутки каждого списка (только UI)")
    present(text, "class FloatBox", "1.6.13 Editor: редактор множителя скорости (не VST-параметр)")
    present(text, "holdLfoPage", "1.6.13 Editor: удержание над кнопкой другого LFO (1 с) переключает страницу")
    present(text, "setHover", "1.6.13 Editor: подсветка прицела мгновенная, без шлейфа")
    present(text, "drawRect(x, geo.yHold, geo.pad, geo.holdH, 1)", "1.6.13/1.6.14 Editor: отдельный ряд HOLD-кнопок (рисуется зажатием)")
    present(text, "applyErase", "1.6.13 Editor: ПКМ-драг стирает velocity+transpose")
    present(text, "PITCH", "1.6.13 Editor: кнопки RANDOM PAGE/ALL/PITCH/VEL")
    present(text, "randomizeRange", "1.6.13 Editor: батч-рандом 128 степов без лагов (suspendProcessing)")
    present(text, "machineButton.arrows=true", "1.6.13 Editor: стрелки пресета по бокам названия машины")
    present(text, "dragPixelsPerStep=18", "1.6.13 Editor: прокрутка машины замедлена, хитбоксы прежние")
    present(text, "tempoSync.setButtonText(\"SYNC\")", "1.6.14 Editor: галочка SYNC вплотную к значению BPM")
    present(text, "previewSample(row)", "1.6.13 Editor: PLAY-колонка в списке BBOX")
    present(text, "list.getVerticalScrollBar()", "1.6.13 Editor: скроллбар списка BBOX белый")
    text = read(processor)
    present(text, "void MonomachineNovaAudioProcessor::previewSample", "1.6.13 PluginProcessor: голос предпрослушивания семплов")
    present(text, "suspendProcessing(true);", "1.6.13 PluginProcessor: сортировка матрицы без шторма нотификаций (фикс краша Ableton)")

    # ---- 16. обещания 1.6.14
    text = read(editor)
    present(text, "menu.setLookAndFeel(&getLookAndFeel())", "1.6.14 Editor: все верхнеуровневые меню явно в чёрном пиксельном скине")
    present(text, "class PortaPanel", "1.6.14 Editor: у PORT своё окно (TIME + SPEED)")
    present(text, "openPorta", "1.6.14 Editor: ПКМ по PORT открывает окно портаменто")
    present(text, "unfocusAllComponents", "1.6.14 Editor: callout не забирает клавиатуру (QWERTY-ноты играют)")
    present(text, "autoSwapToggle", "1.6.42 Editor: AUTO SWAP PAGE -- цепочка страниц (вместо сломанного STEP RND)")
    present(text, "followPlayToggle", "1.6.42 Editor: FOLLOW PLAY -- лейн следует за играющей страницей")
    present(text, "int pageDisplay() const", "1.6.42 Editor: лейн читает playingPage при FOLLOW PLAY")
    absent(text, "allRndButton", "1.6.42 Editor: ALL убрана -- ПКМ на PAGE и так рандомит все страницы")
    present(text, "arpStepEcho", "1.6.14 PluginProcessor: эхо шага арпеджиатора для UI")
    present(text, "holdH = 26", "1.6.14 Editor: ряд HOLD выше")
    absent(text, "scrollView", "1.6.14/1.6.16 Editor: ЛКМ-драг-скролл страницы ARP убран")
    absent(text, "void mouseWheelMove(const juce::MouseEvent&,const juce::MouseWheelDetails&)override{}", "1.6.14/1.6.16 Editor: запрет колеса убран -- скролл снова колёсиком")
    present(text, "class DragValueBox", "1.6.14 Editor: PAGE -- квадратное окошко с displayOffset")
    present(text, "displayOffset", "1.6.14 Editor: PAGE-бокс показывает 1..16 для 0-базного параметра")
    present(text, "arrowClick", "1.6.14 Editor: стрелки < > у названия машины (клик = один шаг)")
    present(text, "rightClick", "1.6.14 Editor: ПКМ-хук кнопок (ARP -> страница ARP)")
    present(text, "keyPressed(const juce::KeyPress& k)", "1.6.14 Editor: ESC закрывает матрицу/ARP/оверлеи")
    present(text, "hoverLock", "1.6.14 Editor: подсказка режима замка при наведении")
    present(text, "lfoHoldIndex", "1.6.14 Editor: удержание над другим LFO (1 с) переключает страницу")
    present(text, "1.6.14: та же ячейка -- не перестраиваем", "1.6.14 Editor: мгновенная смена страниц LFO (bind early-out)")
    text = read(dsp)
    present(text, "softFmSat", "1.6.14 NovaDSP: fma -- мягкая сатурация для FM вместо жёсткого фолда")
    present(text, "bool fmaActive()", "1.6.14 NovaDSP: флаг режима fma у машины")
    text = read(data)
    present(text, "porta_speed", "1.6.14 NovaData: скорость портаменто")
    text = read(processor)
    present(text, "portaSpeedRaw", "1.6.14 PluginProcessor: glide быстрее и регулируемый")
    present(text, "nova::AmpEnvelope::kKernelAlgorithm", "1.6.14 PluginProcessor: old AMP DSP удалён -- всегда mnm")

    # ---- 17. обещания 1.6.15
    text = read(editor)
    present(text, "armed=false", "1.6.15 Editor: мёртвая зона 6 px у драга множителей скорости")
    present(text, "dy/48.0", "1.6.15 Editor: мягкий наклон драга скорости (Shift = x0.1)")
    present(text, "refreshTo", "1.6.15 Editor: RESET ALL для сетки GUI DRAG SPEED")
    present(text, "Restore all GUI drag speeds to their defaults.", "1.6.15/1.6.19 Editor: кнопка RESET у панели скорости GUI")
    present(text, '{"MODE AMP","AMP MODE",1.0}', "1.6.21 Editor: MODE AMP вернулся в сетку скорости (AMP MODE на панели)")
    present(text, "ampModeCombo", "1.6.21 Editor: комбо режима усилителя в шапке (old/mnm)")

    # ---- 18. обещания 1.6.16
    text = read(editor)
    absent(text, "rateSync", "1.6.16 Editor: кнопки SYNC/TIME удалены -- HOST TEMPO единственный переключатель")
    present(text, "DragValueBox pageBox, pageLimit, octavesBox, gateBox", "1.6.16 Editor: GATE -- квадрат со значением")
    absent(text, "gateLink", "1.6.16 Editor: слайдер GATE и его attachment убраны")
    present(text, "p->setValue(p->convertTo0to1(v));", "1.6.16 Editor: батч-рандом без нотификаций хосту (лаг Ableton устранён)")
    present(text, "getIdealPopupMenuItemSizeWithOptions", "1.6.16 Editor: крупный пиксельный шрифт меню (до 21 px)")
    present(text, "HOLD: LMB drag like velocity", "1.6.16/1.6.20 Editor: подсказка управления при наведении на степы")
    present(text, "else if (row == 2) setParam(pg, st, \"hold\", 0.0f);", "1.6.16/1.6.20 Editor: ПКМ стирает только захваченный ряд (row locking)")
    present(text, '"LFO"+juce::String(cur-4)+" "+lbl', "1.6.16 Editor: правильные имена DEST в панели замков")

    # ---- 19. обещания 1.6.17
    text = read(editor)
    present(text, "int overLfoButton", "1.6.17 Editor: определение кнопки LFO под курсором при прицеле")
    present(text, "aimLfoDest(static_cast<uint8_t>(32+over*8))", "1.6.17 Editor: отпускание прицела над кнопкой LFO = выбрать его целью (PAGE на его страницу)")
    present(text, "lfoPageSel!=over", "1.6.17/1.6.18 Editor: удержание не перезапускается циклично")
    # ---- 20. обещания 1.6.18
    present(text, "if(modDragSrc<4){lfoHoldIndex=-1;", "1.6.18 Editor: удержание переключает страницу и в ПКМ-режиме (матрица; 1.6.19: строка расширена сбросами grayBlink)")

    # ---- 21. обещания 1.6.19
    text = read(editor)
    present(text, "grayBlink", "1.6.19 Editor: серое мигание кнопки LFO, на чью страницу идёт прицел")
    present(text, "blinkPhase()", "1.6.19 Editor: общий таймер мигания (280 мс)")
    present(text, "if(over>=0&&lfoPageSel!=over){lfoPageSel=juce::jlimit(0,2,over);bind();}", "1.6.19/1.6.20 Editor: прицел переключает страницу МГНОВЕННО, возврат к исходному разрешен")
    present(text, "showGuiOptions", "1.6.19 Editor: GUI OPTIONS -- отдельное окно из MENU (пункт 9)")
    present(text, "class GuiSpeedPanel final : public FloatingPanel", "1.6.19 Editor: скорость GUI -- отдельная панель (окно из MENU), не страница ARP")
    present(text, "\"amp_mode\"", "1.6.19 Editor: комбо огибающей old/mnm вернулось (attachment amp_mode)")
    text = read(root / "models" / "DspModes.hpp")
    present(text, "section != DspAmp", "1.6.19 DspModes: AMP скрыт из меню DSP MODE")
    text = read(processor)
    present(text, "kKernelAlgorithm:0", "1.6.19 Processor: огибающая выбирается по amp_mode, не по DSP-режиму")
    present(text, "msegIndex<3?10+msegIndex:15+msegIndex", "1.6.29 Processor: addMsegRoute пишет src страницы MSEG (10..12, 18..22)")
    text = read(editor)
    present(text, "processor.addMsegRoute(m,cell->targetId())", "1.6.21 Editor: MSEG-джек -- провод к ручке = маршрут этой кривой")
    absent(text, "class MsegOutputButton final : public juce::Component, public juce::DragAndDropTarget", "1.6.19 Editor: старый DnD MsegOutputButton удалён")
    present(text, "LFO CURVE EDITOR", "1.6.19 Editor: страница MSEG пересоздана по фото (LFO CURVE EDITOR)")
    present(text, '"UNDO","REDO","INVERT","FLIP X","COPY","PASTE"', "1.6.19 Editor: ряд инструментов MSEG")
    present(text, "ROUTING / SIGNED AMOUNT", "1.6.19 Editor: маршруты MSEG со знаковой величиной")
    present(text, "static float gridDenF(int idx)", "1.6.29/1.7.7 Editor: полная сетка OFF/1..1/64 + dotted/triplet (таблица dens как у ARP)")

    # ---- 22. обещания 1.6.20
    text = read(editor)
    present(text, "const int end=getWidth()-54,cy=getHeight()/2;", "1.6.33 Editor: стрелки машин -- полоса вплотную к правому краю")
    present(text, "канарейка живет ТОЛЬКО в полном меню", "1.6.20 Editor: ENCODING TEST только в дебаг-меню кнопки DSP MODE")
    absent(text, "FREE (LMB: LOCK, RMB: modes)", "1.6.20 Editor: подсказки на замках матрицы убраны")
    present(text, 'clearButton.onClick=[this,wipeField]{for(const char* f:{"on","src","dest","depth","mode","lock","aux","aux_depth"})wipeField(f);repaint();};', "1.6.35 Editor: CLEAR стирает ВСЁ, включая AUX SRC и AUX DEPTH")
    present(text, "clearButton.rightClick=[this,wipeField]", "1.6.21 Editor: CLEAR ПКМ = меню выборочного стирания")
    present(text, '"host_sync",tempoSync', "1.6.20 Editor: SYNC главной панели = общий host_sync (весь синт)")
    absent(text, '"arp_sync",tempoSync', "1.6.20 Editor: SYNC больше не висит на arp_sync (развязан с HOST TEMPO)")
    present(text, "if(firstOfCat||row==hover)pixel::text(g,juce::String(m.category),{8,y,colW-14,20},14,false);", "1.6.26 Editor: тип слева ОДИН раз на категорию; SYNTH-машины первыми, FX в конце")
    present(text, "const bool blink=grayBlink&&blinkPhase();", "1.6.20 Editor: яркое мигание кнопки страницы-цели LFO")
    absent(text, "0xff3a3a3a", "1.6.20 Editor: слабый серый мигания заменен")
    present(text, "if(dimmed)fill=juce::Colour(0xffc8c8c8);", "1.6.21 Editor: кнопка-источник прицела чуть серее (dimmed)")
    present(text, "if(!dragStarted){if(e.getDistanceFromDragStart()<6)return;dragStarted=true;", "1.6.21 Editor: провод стартует только с реальным драгом от джека (клик по номеру = выбор)")
    present(text, "if(over>=0&&lfoPageSel!=over){lfoPageSel=juce::jlimit(0,2,over);bind();}", "1.6.20 Editor: возврат прицела к исходному LFO разрешен")
    present(text, "gateOuter(p,\"arp_length\",1,127,64)", "1.6.20 Editor: SET заменен на GATE (arp_length) на внешней панели")
    absent(text, "arpSettingsButton", "1.6.20 Editor: кнопка SET удалена")
    present(text, "const int row = eraseRow >= 0 ? eraseRow : rowUnder(geo, static_cast<int>(e.y));", "1.6.20 Editor: ПКМ-стирание не задевает чужие ряды (ряд захвачен в mouseDown)")
    present(text, "const bool on = static_cast<int>(e.y) < geo.yHold + geo.holdH / 2;", "1.6.20 Editor: HOLD рисуется как VEL (верх половины = ставит, низ = стирает)")
    present(text, "tempoSync.setBounds(961,4,61,30)", "1.6.20/43 Editor: SYNC вплотную к цифрам BPM (961,4,61,30 -- surf.sync)")
    present(text, "setMsegPointCount", "1.6.19 Processor: редактор форм задаёт число точек MSEG")
    present(text, "playH = 12", "1.6.19 Editor: линия игры под рядом HOLD")

    # ---- 23. обещания 1.6.21
    text = read(editor)
    present(text, "class CableLayer final : public juce::Component", "1.6.21 Editor: слой патч-корда поверх интерфейса")
    present(text, "cableLayer.setBounds(getLocalBounds())", "1.6.21 Editor: слой корда растянут на всю панель")
    present(text, "juce::Rectangle<int> jackBounds() const", "1.6.21 Editor: у кнопки-источника есть ГНЕЗДО (джек) справа")
    present(text, "b.numberClick=[this,i]{lfoPageSel=juce::jlimit(0,2,i);bind();}", "1.6.21 Editor: клик по НОМЕРУ = выбор страницы LFO")
    present(text, "wireMseg(msego1,0);wireMseg(msego2,1);wireMseg(msego3,2);", "1.6.21 Editor: ТРИ MSEG-гнезда с номерами 1/2/3")
    present(text, "void showMseg(int page=0)", "1.6.21 Editor: MSEG-редактор открывается на выбранной странице")
    present(text, "MsegPage>(processor,page)", "1.6.21 Editor: страница MSEG передаётся в редактор")
    present(text, "gateOuter.rightClick=[this]{if(settings)closeOverlay();else showSettings(false);}", "1.6.22 Editor: ПКМ по GATE открывает И закрывает ARP")
    present(text, "if(!dragHandler||!e.mods.isLeftButtonDown())return;", "1.6.21 Editor: драг значений front-панели -- только ЛКМ")

    present(text, "dspMenuLf.setColour(juce::PopupMenu::backgroundColourId,juce::Colours::black)", "1.6.21 Editor: меню DSP MODE -- обычный шрифт, ЧЁРНЫЙ фон")
    present(text, '"arp_step_random", pageRndToggle', "1.6.21 Editor: PAGE RND -- отдельный параметр arp_step_random")
    present(text, "pixel::text(g,item.text,area.reduced(10,0),14,false); // 1.6.34: шрифт списков меньше", "1.6.34 Editor: пункты попапов -- 14px, выровнены влево")
    present(text, "lockPaint = lockPaintWasRmb ? 2 : (lockValue(r) > 0 ? 0 : 1);", "1.6.21 Editor: замки матрицы красятся ПРОВЕДЕНИЕМ (ЛКМ toggle, ПКМ SOLO)")
    present(text, "paintValue=was?0:1;lockPainting=true;", "1.6.21 Editor: замки LFO-панели красятся проведением")
    present(text, '{"MODE AMP","AMP MODE",1.0}', "1.6.21 Editor: скорость-mode AMP в defs")
    text = read(processor)
    present(text, "setMsegOutputs(msegValue[0],msegValue[1],msegValue[2])", "1.6.21 Processor: все ТРИ кривые MSEG идут в DSP")
    present(text, "juce::jlimit(0,17,juce::roundToInt(routeRaw[r][1]->load()))", "1.6.21 Processor: источники маршрутов до 17 (MSEG3)")
    text = read(root / "models" / "arpeggiator.hpp")
    present(text, "const bool newPass = (sequenceStep % stepCount) == 0;", "1.6.21 Arp: новый проход = конец паттерна (реген текущей страницы, без джиттера)")
    present(text, "rs.transpose = static_cast<int8_t>(static_cast<int>(rng % 25u) - 12);", "1.6.23/1.7.7 Arp: PAGE RND -- случайные ШАГИ с питчем каждый проход (движок transient-паттерна)")
    text = read(root / "models" / "mseg.hpp")
    present(text, "k", "1.6.21 models: mseg.hpp -- кривая с изгибом сегмента (k)")

    # ---- 24. обещания 1.6.22
    text = read(editor)
    absent(text, "[this,&wipeField]", "1.6.22 Editor: CLEAR больше не держит висячую ссылку на локальную лямбду (краш ЛКМ)")
    present(text, "void setOnPaint(int r)", "1.6.22 Editor: галочки ON красятся проведением (ЛКМ -- вкл, ПКМ -- стирание)")
    present(text, "if (!e.mods.isLeftButtonDown()) return; // 1.6.22", "1.6.22 Editor: SRC/DEST/DEPTH/MODE матрицы -- только ЛКМ")
    absent(text, "struct Cord", "1.6.24 Editor: постоянные провода удалены (двоились)")
    absent(text, "rebuildCables", "1.6.24 Editor: перерисовка проводов удалена вместе с проводами")
    absent(text, "cablesFocus", "1.6.24 Editor: фокус-подсветка проводов удалена")
    absent(text, "jackRightClick", "1.6.24 Editor: ПКМ-подсветка проводов у гнёзд удалена (проводов больше нет)")
    present(text, "void bakeSteps()", "1.6.22 Editor: ПКМ по STEPS применяет ступеньки к графику (повторный ПКМ возвращает)")
    present(text, "void rebuildStairs()", "1.6.22 Editor: DRAW STEP рисует квадратные ступени на ЛКМ")
    present(text, "class WheelCombo final : public juce::ComboBox", "1.6.22 Editor: списки MSEG крутятся колесом (предустановки и сетка)")
    present(text, "if(e.x>=end-8){", "1.6.28 Editor: полоса стрелок забронирована (с запасом слева), стрелки в своей рамке")
    present(text, "double dragGain = 1.0;", "1.6.22 Editor: множитель чувствительности драга значений")
    present(text, "gateOuter.dragGain=6.0;", "1.6.22 Editor: GATE -- весь диапазон за ~170 px")
    present(text, "holdRndButton{\"HOLD\"}", "1.6.22 Editor: кнопка HOLD-рандома")
    present(text, "pageRndButton.setBounds(869, 45, 42, 26); pitchRndButton.setBounds(915, 45, 42, 26); velRndButton.setBounds(961, 45, 42, 26); holdRndButton.setBounds(1007, 45, 42, 26);", "1.6.22/1.7.1 Editor: ряд RANDOM PAGE PITCH VEL HOLD + PLK/SLIDE (1.7.1, ряд ужат)")
    present(text, "BUILD 1.7.6", "1.6.37 Editor: маркер версии сборки обновлен")
    present(text, 'showMessageBoxAsync(juce::MessageBoxIconType::NoIcon,"MONOMACHINE NOVA","mnm mod by restrange aka kraduvremya")', "1.6.29 Editor: About = только подпись")
    absent(text, "showCords", "1.6.24 Editor: следов постоянных проводов нет")
    absent(text, "SHOW PATCH CORDS", "1.6.24 Editor: постоянные провода и их тумблер УДАЛЕНЫ (двоились)")
    present(text, "if (c == 4) { if (hitWasRmb) openValueMenu(r, 4, e.getScreenPosition()); return; }", "1.6.25 Editor: DEPTH -- список по ПКМ, двойной ЛКМ -- сброс, ПКМ никогда не сбрасывает")
    present(text, "const int values[]{-64, -48, -32, -16, 0, 16, 32, 48, 63}; for (int i = 0; i < 9; ++i)", "1.6.23 Editor: DEPTH-список -100% ... 0% ... +100%")
    present(text, "auto* p = c == 7 ? processor.parameters.getParameter(\"r\" + juce::String(r) + \"_aux_depth\") : cell(r, c); if (!p) return;", "1.6.32 Matrix: дабл-клик сбрасывает DEPTH/MODE/DEST(=OFF)/AUX DEPTH")
    present(text, 'auto wipeField=[this](const char* f){for(int r=0;r<64;++r)if(auto* p=processor.parameters.getParameter("r"+juce::String(r)+"_"+f)){p->setValue(p->convertTo0to1(0.0f));}};', "1.6.23/1.7.7 Editor: CLEAR -- ровно ноль одной конвертацией, без нотификации хоста (64 слота)")
    present(text, "if (auto* raw = processor.parameters.getRawParameterValue(id)) *raw = v;", "1.6.23 Editor: рандомайзеры шагов пишут прямо в атомы параметров")
    absent(text, "processor.suspendProcessing(true);", "1.6.23 Editor: suspendProcessing убран из рандомайзеров (подвисал интерфейс)")

    # ---- 26. обещания 1.6.24
    text = read(editor)
    absent(text, "struct Cord", "1.6.24 Editor: постоянные патч-корды удалены")
    present(text, "aimBackupValid", "1.6.24 Editor: провод отпущен мимо -- PAGE/DEST откатываются к до-прицельным")
    present(text, "if (c == 5) { if (!hitWasRmb) { if (auto* p = cell(r, 5)) toggle(*p); return; } openValueMenu(r, 5, e.getScreenPosition()); return; } // 1.6.32: UNI/BIP -- клик переключает", "1.6.32 Editor: MODE -- клик переключает UNI<->BIP, список по ПКМ")
    present(text, "void openAuxMenu(int r, juce::Point<int> screen)", "1.6.24 Editor: AUX SOURCE -- список вторичных источников")
    present(text, '"DESTINATION", "DEPTH", "MODE", "AUX SRC"', "1.6.24 Editor: колонка AUX SRC в матрице")
    present(text, "for (int i = 0; i < 2; ++i) menu.addItem(i + 1, polarityName(i), true, i == selected);", "1.6.25 Editor: полярность -- список UNIPOLAR/BIPOLAR по клику")
    present(text, "g.drawVerticalLine(edge, float(by + 1), float(by + bh - 1));", "1.6.24 Editor: DEPTH -- белая обводка по границе заполнения")
    present(text, "c == 3 ? 14 : 16, c == 4);", "1.6.24 Editor: значение DEPTH по центру ячейки")
    present(text, "if (!e.mods.isLeftButtonDown()) return;", "1.6.24 Editor: двойной сброс -- только ЛКМ")
    absent(text, "retrigSync.setButtonText", "1.6.25 Editor: RETRIG SYNC удалена (делала не то)")
    absent(text, "arp_retrig_sync", "1.6.25 Editor: параметр arp_retrig_sync выкорчеван")
    present(text, "machineButton.verticalDrag=true;", "1.6.24 Editor: имя машины листается ВВЕРХ/ВНИЗ драгом")
    present(text, "wheelAccum+=w.deltaY;", "1.6.24 Editor: колесо пресетов срабатывает каждый раз (накопление дельты)")
    present(text, "g.drawRect(end-3,cy-13,56,26,1);", "1.6.24 Editor: стрелки пресетов в отдельной рамке, полоса забронирована")
    present(text, "g.drawRect(x + 2 + std::min(fillEnd, width - 4)", "1.6.24 Editor: незаполненный остаток шкалы LEV -- полая белая рамка")
    absent(text, "цель прицела -- ТОЛЬКО мигающая рамка", "1.6.25 Editor: мигания при прицеле больше нет (как в прошлой версии)")
    present(text, "setMouseCursor(juce::MouseCursor::NormalCursor); // 1.6.24", "1.6.24 Editor: указательный палец над ручками убран, ПКМ везде работает")
    present(text, "arpButton.doubleClickHandler", "1.6.24 Editor: двойной клик по ARP открывает страницу")
    text = read(processor)
    present(text, "auxRaw[static_cast<size_t>(r)]=parameters.getRawParameterValue", "1.6.24 Processor: атомы AUX SOURCE маршрутов")
    text = read(root / "models" / "modulation_matrix.hpp")
    present(text, "uniOf(static_cast<ModSource>(route.auxSource - 1))", "1.6.25 Matrix: AUX SOURCE масштабирует Amount")
    text = read(processor)
    present(text, "state.setProperty(\"schema\",monomachine::kDspModeSchemaVersion,nullptr)", "1.6.24 Processor: сохранение пишет АКТУАЛЬНУЮ schema (миграции больше не портят пресет)")
    absent(text, 'state.setProperty("schema",8', "1.6.24 Processor: старая schema=8 при сохранении искоренена")

    # ---- 27. обещания 1.6.25
    text = read(editor)
    present(text, "if (c == 2 || c == 3 || c == 5 || c == 6 || c == 7) { // 1.6.25/32", "1.6.32 Editor: SRC/DEST/AUX/AUX DEPTH -- клик = список, ЛКМ-драг = листать")
    present(text, "else { const int hi = hitCol == 3 ? 64 : hitCol == 5 ? 1 : hitCol == 6 ? 18 : 17;", "1.6.32 Editor: диапазоны драга (DEST 0..64, 0 = OFF)")
    present(text, "g.setColour(ink.withAlpha(0.45f)); g.drawRect(bx, by, bw, bh, 1);", "1.6.25 Editor: DEPTH -- только контур ячейки, без серой заливки")
    present(text, "g.setColour(juce::Colours::white); g.drawRect(8, 26, 1120, 64 * 34 + 36, 1);", "1.6.25/1.7.7 Editor: рамка матрицы белая (64 строки)")
    present(text, "g.drawRect(juce::Rectangle<int>(0,0,width,height),1);", "1.6.30 Editor: списки -- полная белая рамка (включая правую кромку)")
    present(text, "g.fillRect(area.reduced(1,0));", "1.6.25 Editor: пункты списков не затирают боковые кромки рамки")
    present(text, "lockZone = r >= 0 && e.x >= colX(3) + colWidth(3) - 50 && e.x < colX(3) + colWidth(3) - 22; // 1.6.32", "1.6.32 Editor: хитбокс замка под компактную иконку")
    present(text, 'n[]{"KEY", "VEL", "MACRO X", "MACRO Y", "LFO1", "LFO2", "LFO3", "PITCH WHL", "MOD WHL", "AFTERTOUCH", "MSEG1", "MSEG2", "MSEG3", "STEP VEL", "STEP TRANS", "ARP GATE", "ARP RATE", "RANDOM", "MSEG4", "MSEG5", "MSEG6", "MSEG7", "MSEG8"}', "1.6.29 Editor: 23 источника в порядке ModSource")
    absent(text, "flashTicks>0){g.setColour(juce::Colours::white.withAlpha(0.40f));", "1.6.33 Editor: серый мигок ячейки ручки удалён (MAP FLASH вырезана)")
    present(text, "if(pickMode&&hoverNow){g.setColour(ink);g.drawRect(getLocalBounds().reduced(2),3);} // 1.6.26", "1.6.28 Editor: подсветка прицела -- БЕЛАЯ рамка только на текущей цели (и из матрицы)")
    present(text, "slider.setInterceptsMouseClicks(!pickMode,false);", "1.6.28 Editor: ручка видна, но закрыта для мыши на время прицела")
    present(text, "if(machine==7&&knob==2&&e.mods.isPopupMenu()){popupHandled=true;if(openSamples)openSamples();return;} // 1.6.25", "1.6.25 Editor: ПКМ по SAMPLES перед общим фолбэком")
    present(text, "arrowHl=e.x<end+24?0:1;arrowPressed=true;", "1.6.25 Editor: своя подсветка у каждой стрелки пресетов")
    present(text, "if(arrowPressed)return; // 1.6.25", "1.6.25 Editor: драг со стрелки не листает пресеты")
    present(text, "if((juce::String(nova::machines()[static_cast<size_t>(i)].category).startsWith(\"FX\"))==(pass==1))order.push_back(i);", "1.6.25 Editor: SYNTH-машины первыми, FX -- в конце")
    present(text, "showDocked(std::move(panel),juce::Rectangle<int>(machineButton.getX(),machineButton.getBottom(),machineButton.getWidth(),0),true);", "1.6.30 Editor: список машин -- ПОД строкой имени, без стрелки")
    present(text, "class PopupDock final : public juce::Component", "1.6.30 Editor: док-попапы -- ниже хитбокса, одна белая рамка")
    present(text, "juce::Time::getMillisecondCounter()-lastMachineCycleMs<300", "1.6.30 Editor: клик сразу после листания машины список не открывает")
    present(text, "rateButton.rightClick=[this]{ // 1.6.25", "1.6.25 Editor: список рейтов по ПКМ")
    present(text, "pageBox.editOnDoubleClick=true;pageLimit.editOnDoubleClick=true;", "1.6.25 Editor: PAGE/PAGE LIMIT -- двойной клик вводит значение")
    present(text, "pageBox.setBounds(695, 45, 40, 26); pageLimit.setBounds(740, 45, 40, 26);", "1.6.25/42 Editor: PAGE и PAGE LIMIT вплотную (по скрину)")
    present(text, "pageRndButton.rightClick=[this]{randomizeRange(0,15,true,true,true);};", "1.6.25 Editor: ПКМ по рандомайзерам = все страницы")
    present(text, "g.drawRect(getLocalBounds().reduced(1), 1); // 1.6.25", "1.6.25 Editor: GATE-бокс ровно с кнопками")
    present(text, "const int s=std::min(b.getHeight()-2,28),oy=(b.getHeight()-s)/2;", "1.6.28 Editor: квадрат галки на всю высоту кнопки")
    present(text, "ampModeCombo.setBounds(759,95,96,28);", "1.6.32 Editor: AMP MODE -- края на линиях колонок, границы категории")
    present(text, "label.setBounds(1, 1, box.getWidth() - 2, box.getHeight() - 2); label.setJustificationType(juce::Justification::centred);", "1.6.33 Editor: текст MODE -- лейбл внутри рамки, по центру (как кнопки категорий)")
    present(text, "refreshNames(); // живые имена каждый кадр", "1.6.30 Editor: панель замков перечитывает имена каждый кадр")
    text = read(processor)
    present(text, "settings.stepPage=static_cast<uint8_t>(juce::jlimit(0,15,juce::roundToInt(global[ArpStepPage])));", "1.6.25 Processor: строка PAGE/LIMIT/RANDOM восстановлена (была проглочена комментарием)")
    present(text, 'const char* fields[]{"on","src","dest","depth","mode","aux","aux_depth"}; // 1.6.34', "1.6.34 Processor: sortModRoutes -- 7 полей (aux_depth не отрывается от строки)")
    present(text, "juce::jlimit(0,17,src); // 1.6.25", "1.6.25 Processor: addRouteFromSource принимает 18 источников")
    present(text, "VelocityMode::Step,", "1.6.25 Processor: порядок velocity-режимов STEP-первыми отображён в движок")
    absent(text, "retrigRaw", "1.6.25 Processor: ретриг-хук из processBlock удалён")
    text = read(root / "models" / "modulation_matrix.hpp")
    present(text, "MSEG2 = 11,", "1.6.25 Matrix: enum без дыры (MSEG2=11, MSEG3=12, шаги арпа 13..17)")
    present(text, "float uniOf(ModSource source) const noexcept", "1.6.25 Matrix: нормализация источников к 0..1")
    present(text, "route.polarity == ModPolarity::Bipolar ? source * 2.0f - 1.0f : source; // Serum", "1.6.25 Matrix: полярность по Serum -- UNI одна сторона, BI по центру")
    text = read(root / "NovaData.h")
    present(text, '"KEY|VEL|MACRO X|MACRO Y|LFO1|LFO2|LFO3|PITCH WHL|MOD WHL|AFTERTOUCH|MSEG1|MSEG2|MSEG3|STEP VEL|STEP TRANS|ARP GATE|ARP RATE|RANDOM|MSEG4|MSEG5|MSEG6|MSEG7|MSEG8"', "1.6.29 Defaults: src -- 23 источника в порядке enum")
    absent(text, "arp_retrig_sync", "1.6.25 Defaults: arp_retrig_sync удалён")

    # ---- 28. обещания 1.6.26
    text = read(editor)
    present(text, "syntModeCombo.setBounds(360,95,99,28);", "1.6.34 Editor: SYNT MODE -- левый край на линии колонки, правый до рамки панели")
    present(text, "filtModeCombo.setBounds(360,335,99,28);", "1.6.34 Editor: FILT MODE -- правый край до рамки панели")
    present(text, "effxDlyCombo.setBounds(663,335,96,28);", "1.6.32 Editor: DLY -- левый край на линии SRR")
    present(text, "effxDistCombo.setBounds(759,335,99,28);", "1.6.34 Editor: DIST -- правый край до рамки панели EFFX")
    present(text, "g.fillRect(end-2,2,56,getHeight()-4);", "1.6.26 Editor: у стрелок свой чёрный фон (нажатие списка не заливает их белым)")
    present(text, "pixel::text(g,shortName(i),{colW+4,y,getWidth()-colW-10,20},14,false);", "1.6.26 Editor: короткие имена машин -- правым списком")

    # ---- 29. обещания 1.6.27
    text = read(editor)
    absent(text, "static bool mapPostFlash=true;", "1.6.33 Editor: MAP FLASH удалена (пост-мигок ячейки не нужен)")
    absent(text, "void flashPick(){if(!mapPostFlash)return;", "1.6.33 Editor: flashPick удалён -- после модуляции ячейка ручки не мигает")
    absent(text, 'mapFlash.setButtonText("MAP FLASH")', "1.6.33 Editor: галка MAP FLASH удалена из GUI OPTIONS")
    absent(text, "mapFlash.setBounds(845,6,155,24);", "1.6.33 Editor: MAP FLASH убрана из ряда RESET")
    present(text, "void mouseExit(const juce::MouseEvent&) override { if(pickMode) setHover(false); }", "1.6.27 Editor: подсветка ховера корректно гаснет")
    present(text, "juce::Time::getMillisecondCounter()<flashUntilMs", "1.6.29 Editor: PixelButton -- вспышка-подтверждение")
    present(text, "down&&outlined&&juce::Desktop::getInstance().getMainMouseSource().isDragging()", "1.6.29 Editor: залипшее «нажато» самоликвидируется")
    present(text, "||getToggleState()||", "1.6.27 Editor: PixelButton отражает toggle (галка залита белым)")

    # ---- 30. обещания 1.6.28
    text = read(editor)
    present(text, "slider.setInterceptsMouseClicks(!pickMode,false);", "1.6.28 Editor: в прицеле ручка закрыта для мыши")
    present(text, "if(e.mods.isAltDown()&&parameter&&!isChoice){altTurn=true;", "1.6.28 Editor: Alt+ЛКМ в прицеле крутит ручку")
    present(text, "cell->setPickMode(true,true); // 1.6.28", "1.6.28 Editor: выбор цели из матрицы -- мапинг по ЛКМ")
    present(text, 'menu.addSubMenu("MIDI", midi); menu.addSubMenu("LFO", lfom); menu.addSubMenu("ARP", arp); menu.addSubMenu("MSEG", msegm); menu.addSubMenu("MACRO", mac);', "1.6.34 Editor: SOURCE -- папки MIDI, LFO, ARP, MSEG, MACRO")
    present(text, "static const int vals[]{17, 0, 1, 7, 8, 9, 4, 5, 6, 13, 14, 15, 16, 10, 11, 12, 18, 19, 20, 21, 22, 2, 3}; // 1.6.34", "1.6.34 Editor: SOURCE/AUX -- таблица позиций = RANDOM, MIDI, LFO, ARP, MSEG, MACRO")
    present(text, 'void openAuxMenu(int r, juce::Point<int> screen) { // 1.6.31: AUX = копия меню SOURCE + OFF первым', "1.6.31 Editor: AUX SOURCE = копия SOURCE + OFF первым (папки)")
    present(text, 'menu.addSubMenu(groups[g], sub);', "1.6.29 Editor: DEST -- папки-подменю (дефолтная задержка JUCE)")
    present(text, 'menu.addSubMenu("MSEG", msegSub);', "1.6.29 Editor: DEST -- секция MSEG OUT после модулей")
    present(text, 'c == 3 ? (value == 0 ? juce::String("---") : destName(value - 1))', "1.6.32 Editor: DEST -- прочерки = OFF (значение 0), цель = значение-1")
    present(text, "if (fw > 0) { if (value >= 0) g.fillRect(mid, by, fw, bh); else g.fillRect(mid - fw, by, fw, bh); }", "1.6.28 Editor: DEPTH -- серая полоса заполнения возвращена")
    present(text, "colW=std::max(colW,static_cast<int>(juce::String(nova::machines()[static_cast<size_t>(idx)].category).length())*12);", "1.6.28 Editor: пикер -- колонки под полный кегль (SWAVE не мельче)")
    present(text, "if(verticalDrag&&std::abs(dx)>3)wasDragged=true;", "1.6.28 Editor: драг имени не открывает список")
    present(text, "if(arrowPressed){arrowPressed=false;arrowHl=-1;wasDragged=false;repaint();return;}", "1.6.28 Editor: стрелки -- изолированный жест")
    present(text, "if(e.x>=end-8){", "1.6.28 Editor: запас хитбокса левой стрелки")
    present(text, 'wireCat(msegCatButton,"MSEG: open the curve editor")', "1.6.28 Editor: кнопка MSEG открывает редактор")
    present(text, "syntCatButton.setBounds(72,95,96,28);lfoCatButton.setBounds(870,95,96,28);filtCatButton.setBounds(72,335,96,28);effxCatButton.setBounds(471,335,96,28);msegCatButton.setBounds(870,335,96,28); // 1.6.32", "1.6.32 Editor: категории 96px -- стенка ровно по границе первой крутилки")
    present(text, "g.setColour(ink);g.drawRect(x,y,387,30,2); // 1.6.31", "1.6.31 Editor: рамка только у шапки, серый прямоугольник панели убран")
    present(text, "ampButton.setBounds(471,95,96,28);", "1.6.32 Editor: AMP -- стенка на границе первой крутилки")
    present(text, "syntModeCombo.setBounds(360,95,99,28);filtModeCombo.setBounds(360,335,99,28);", "1.6.34 Editor: MODE -- правый край до рамки панели страницы")
    present(text, "const int s=std::min(b.getHeight()-2,28),oy=(b.getHeight()-s)/2;", "1.6.28 Editor: квадрат галки на всю высоту кнопки")
    present(text, "arpButton.setBounds(507,59,116,30);gateOuter.setBounds(624,58,42,32);", "1.6.29/1.7.0 Editor: ARP прилегает к галке, шаги ARP->GATE->TRIG сохранены (surf.arp/gate)")
    absent(text, "g.drawRect(8,94,55,512,2);", "1.6.29 Editor: серая полоса-шлейф у LEV убрана")
    present(text, "pixel::dotted(g,8,94,8,562)", "1.6.31 Editor: пунктир LEV заканчивается на линии секций (562), а не 570")
    present(text, "g.drawHorizontalLine(124,27.0f,43.0f);g.drawHorizontalLine(224,27.0f,43.0f);", "1.6.31 Editor: риски LEV по сетке панелей -- макс на низе шапки (124), дефолт на пунктире ячеек (224)")
    present(text, "g.setColour(juce::Colours::black);g.drawHorizontalLine(top+1,float(cx-6),float(cx+6));", "1.6.34 Editor: LEV -- заполнение + чёрная насечка-хват на кусочной позиции")
    present(text, '{"hRANDOM",{893,25,110,14}}}', "1.6.28/43 Editor: ARP-шапка по скрину -- заголовки-теги (MODE/OCT/RATE/GATE/VELOCITY/WRAP)")
    present(text, "e.x >= barX - 2 && e.x <= barX + 192 && e.y >= y - 3 && e.y <= y + 28", "1.6.35/1.7.1 Editor: ампаунт MSEG -- зона драга = бар (едет с rownames)")
    text = read(root / "NovaData.h")
    present(text, '"STEP|STEP+KEY|STEP+HOLD|KEY|HOLD"', "1.6.24 Defaults: velocity -- STEP, первые в списке")
    absent(text, "ARP Retrig sync", "1.6.25 Defaults: параметр arp_retrig_sync удалён")
    present(text, 'aux source",0,23,0,1', "1.6.29 Defaults: r*_aux -- OFF + 23 источника")
    text = read(processor)
    present(text, "static const monomachine::MonomachineArpeggiator::Play playMap[]", "1.6.22 Processor: порядок режимов ARP STEP-первыми отображён в движок")
    text = read(root / "models" / "arpeggiator.hpp")
    present(text, "uint8_t playPage = settings.stepPage; // 1.7.7: страница играет выбранная", "1.6.22/1.7.7 Arp: без PAGE RND играется выбранная страница (без хвоста)")
    present(text, "gate = std::max(1.0, stepHold ? duration - 1.0 : std::min(duration - 1.0, normalGate));", "1.6.23 Arp: HOLD -- нота держится до следующего шага (по мануалу MNM)")
    # 31. 1.6.29: страницы MSEG 4..8, MSEG OUT как DEST, About-подпись, фиксы панели
    mmat = read(root / "models" / "modulation_matrix.hpp")
    present(mmat, "kTargetCount = 132", "1.6.29/1.7.8 Matrix: 132 цели (67..130 ROUTE, 131 LFO FM)")
    present(mmat, "MSEG4 = 18,", "1.6.29 Matrix: источники MSEG4..8 = 18..22")
    present(mmat, "void setMsegOutputs(const float* values, int count)", "1.6.29 Matrix: выходы восьми страниц")
    present(mmat, "msegDests[route.targetParamId - 56]", "1.6.29 Matrix: суммы маршрутов на MSEG OUT")
    present(mmat, 'if (paramId >= 56) return "MSEG" + std::to_string(paramId - 56 + 1) + " OUT";', "1.6.29 Matrix: имена целей MSEG OUT")
    pdata = read(root / "NovaData.h")
    present(pdata, 'n+"_rate","MSEG"+juce::String(mi)+" rate"', "1.6.29 Defaults: параметры mseg4..8 (rate/sync/loop)")
    present(pdata, 'ARP RATE|RANDOM|MSEG4|MSEG5|MSEG6|MSEG7|MSEG8', "1.6.29 Defaults: источники 18..22 в конце списков выбора")
    present(pdata, '"MSEG"+juce::String(i-55)+" OUT"', "1.6.29 Defaults: имена DEST 56..63")
    ptext = read(processor)
    present(ptext, 'state.setProperty("msegpages",msegPageCount(),nullptr);', "1.6.29 State: счётчик страниц сохраняется")
    absent(ptext, "state.removeChild(savedMseg,nullptr);", "1.6.29 State FIX: MSEG1 больше не теряет точки при загрузке")
    present(ptext, "for(int mi=0;mi<8;++mi)loadMseg(msegTags[mi],mi);", "1.6.29 State: загрузка восьми страниц")
    present(ptext, "void MonomachineNovaAudioProcessor::removeMsegPage(int page){", "1.6.29 Processor: удаление страницы с переименованием назначений")
    present(ptext, "void MonomachineNovaAudioProcessor::addMsegPage(){", "1.6.29 Processor: добавление страницы")
    present(ptext, "msegValue[static_cast<size_t>(mi)]+msegMod[static_cast<size_t>(mi)]/127.0f", "1.6.29 Engine: DEST 56..63 сдвигает выход кривой")
    etext = read(editor)
    present(etext, "DragChoice shape, grid, viewGrid, retrig, rmodeBox;", "1.6.33/1.7.8 Editor: SHAPE/GRID/VIEW/RETRIG/RMODE -- DragChoice (клик = список после отпускания, драг = листать, колесо)")
    # 32. 1.6.30: UX-пакет (пикер/матрица/MSEG-draw/локи/mini-AMP/state)
    present(etext, "g.drawRect(juce::Rectangle<int>(0,0,width,height),1);", "1.6.30 Editor: правая кромка белой рамки списков")
    present(etext, "if(item.subMenu!=nullptr){g.setColour(ink);const int ax=area.getRight()-16", "1.6.30 Editor: стрелка '>' у папок-подменю")
    present(etext, "void drawComboBoxTextWhenNothingSelected(juce::Graphics& g,juce::ComboBox& box,juce::Label&)override", "1.6.30 Editor: текст MODE -- по центру")
    present(etext, "const int dx = static_cast<int>(e.getScreenPosition().x) - hitX;", "1.6.30 Matrix: DEPTH -- горизонтальный драг")
    present(etext, "int colw[8]{15, 52, 165, 165, 150, 124, 120, 148};", "1.6.44 Matrix: ширины колонок из JSON пользователя (DEST 165, AUX 120) -- ресайз за границу в шапке")
    present(etext, "int colX(int c) const { int x=13; const int n=juce::jlimit(0,7,c); for(int i=0;i<n;++i)x+=colw[(size_t)i]+5; return x; }", "1.6.44 Matrix: накопительные позиции, правый край 1128")
    present(etext, "ink.withAlpha(0.55f)); for (int c = 1; c < 8; ++c) g.drawVerticalLine", "1.6.30/46 Matrix: вертикальные линии колонок ярче")
    present(etext, "ink.withAlpha(0.5f)); g.drawHorizontalLine(y + 31, 8.0f, static_cast<float>(colX(7) + colWidth(7) + 2));", "1.6.44/46 Matrix: линии строк ярче (до правой границы)")
    present(etext, "pickClick=false;", "1.6.30 Editor: клик в прицеле не открывает список")
    absent(etext, "void flashPick(){", "1.6.33 Editor: мигок подтверждения бинда на ячейке ручки удалён (осталась рамка DEST в матрице)")
    present(etext, "if(!pickMode)setHover(false);", "1.6.31 Editor: белая рамка прицела не залипает на параметре после бинда")
    absent(etext, "static bool mapPostFlash=true;", "1.6.33 Editor: MAP FLASH (пост-анимация) удалена полностью")
    present(etext, 'tagLev=std::make_unique<TextTag>(*this,"lev",juce::Rectangle<int>(7,58,58,30));tagLev->factoryText="LEV";', "1.6.30/42 Editor: LEV -- тег (вровень с рядом ARP, двигается/переименовывается)")
    present(etext, "level.setBounds(12,124,46,438);", "1.6.32 Editor: фейдер LEV 124..562 -- заполнение доходит до рисок")
    present(etext, "int lfoPageSel=0;", "1.6.30 Editor: по умолчанию выделен LFO1")
    present(etext, 'menu.addItem(5,"RESET ALL PARAMETERS");menu.addItem(1,"ARP");menu.addItem(2,"MOD MATRIX");', "1.6.30 Menu: RESET первым, короткие имена")
    present(etext, 'menu.addItem(7,"BBOX SAMPLES");menu.addItem(3,"BBOX RESTORE KIT");', "1.6.30 Menu: BBOX-имена")
    present(etext, 'menu.addSeparator();menu.addItem(4,\"PANIC / CLEAR TAILS\");menu.addItem(9,\"GUI\");menu.addItem(10,\"LAYOUT EDIT\");menu.addItem(6,\"ABOUT\");', "1.6.30/38 Menu: PANIC/GUI/LAYOUT EDIT/ABOUT")
    present(etext, "LfoLockPanel(MonomachineNovaAudioProcessor& p,int lfoIndex,bool)", "1.6.30 LFO: единая панель замков (PAGE+DEST)")
    present(etext, 'pixel::text(g,"PAGE",{46,rowsTop()-16,120,14},11,false);pixel::text(g,"DEST",{colSplit()+16,rowsTop()-16,120,14},11,false);', "1.6.33 LFO: секции PAGE и DEST -- ДВЕ КОЛОНКИ в одном окне")
    present(etext, "EnvelopePage(MonomachineNovaAudioProcessor& p,bool compact=false)", "1.6.30 AMP: компактный режим окна огибающей")
    present(etext, "void toggleEnvMini()", "1.6.30 AMP: ЛКМ по категории -- мини-окно (закрытие повторным ЛКМ), ПКМ -- большое")
    present(etext, 'mode.speedKey="MODE AMP";', "1.6.30 AMP: список режимов -- ЛКМ-драг, обычный клик -- список")
    absent(ptext, "syncTreeFromAtoms", "1.6.32x State: таймер-синк удалён -- он писал НОРМ в ДЕНОРМ-поле дерева (нули/откат значений)")
    present(etext, "b->textHeight = 14; // 1.6.32", "1.6.32 MSEG: единый шрифт кнопок 14px")
    present(etext, "page(juce::jlimit(0,7,startPage))", "1.6.31 MSEG: страницы 1..8 фиксированы -- все активны, активация не нужна")
    present(etext, 'pageButtons.getUnchecked(0)->setButtonText(juce::String(page + 1));', "1.6.31 MSEG: кнопка страниц = просто номер текущей (без /8)")
    present(etext, 'm.addItem(i + 1, "MSEG " + juce::String(i + 1), true, i == page);', "1.6.31 MSEG: список страниц -- черный скин, 8 пунктов без пометок (+)")
    present(etext, "DragChoice shape, grid, viewGrid, retrig, rmodeBox;", "1.6.33/1.7.8 MSEG: списки шейпов/грида/вида/retrig/rmode -- клик открывает ПОСЛЕ отпускания (драг не триггерит пункт)")
    present(etext, "const int n = processor.msegPointCount(page); // 1.6.31: вертикальные линии от точек убраны", "1.6.31 MSEG: линии-грид от каждой точки больше не рисуются")
    present(etext, "if (x < sx0 - 0.005f || x > sx1 + 0.005f) merged.push_back", "1.6.31 MSEG: DRAW дорисовывает к кривой (точки вне штриха сохраняются)")
    present(etext, "stepTouched[static_cast<size_t>(col)] = true;", "1.6.31 MSEG: DRAW STEP -- помечает тронутые колонки")
    present(etext, "for (const auto& q : stepOrig) if (q.x < x0 - 0.001f || q.x > x1 + 0.001f) pts.push_back(q);", "1.6.31 MSEG: ступени пишутся только в тронутом диапазоне -- вход/касание не квадратит кривую")
    present(etext, "void wireMsegAim()", "1.6.31 MSEG: прицел из списка маршрутов (провод от номера строки)")
    present(etext, "if (pickStart) pickStart(page, row); return; } } // 1.6.34: гнездо = ПРИЦЕЛ", "1.6.34 MSEG: гнездо строки = прицел клик-клик (провод убран)")
    present(etext, "g.drawRect(rowX, y + 2, 34, 26, 1); // 1.6.31/34", "1.6.34/1.7.1 MSEG: гнездо-прицел на строке маршрута (едет с rownames)")
    present(etext, "g.drawRect(rowX, y2 + 2, 34, 26, 1); // 1.7.1", "1.6.34/1.7.1 MSEG: гнездо-прицел на UNASSIGNED")
    present(etext, 'processor.setMsegPoint(page, 1, 0.0f, 0.0f); markCustom();', "1.6.30 MSEG: CLEAR -- одна точка слева внизу")
    present(etext, '{"FLAT","RISE","FALL","TRI","SINE","SQUARE"}', "1.6.30 MSEG: порядок SHAPE (custom flat rise fall tri sine square)")
    present(etext, '{"DRAW","DRAW STEP","RAND DRAW 1","RAND DRAW 2","VITAL"}', "1.6.30/1.7.8 MSEG: кнопки DRAW/RAND DRAW + VITAL (старые на месте)")
    present(etext, "(rng.nextFloat() * 2.0f - 1.0f) * 0.35f", "1.6.30 MSEG: RAND DRAW 1 -- дикая random draw")
    present(etext, "randPhase += 0.9f;", "1.6.30 MSEG: RAND DRAW 2 -- управляемый гладкий шум")
    present(etext, "if (snapStroke && drawMode == 1) for (auto& p : pts) p.x = snapX(p.x);", "1.6.30 MSEG: обычный DRAW -- по сетке")
    present(etext, "snapStroke = drawMode == 1 && !e.mods.isAltDown();", "1.6.30 MSEG: Alt -- рисование без сетки")
    present(etext, "int gridDen() const", "1.6.30 MSEG: квадраты -- по сетке GRID (без собственной сетки)")
    present(etext, "isCtrlDown() && key.getKeyCode() == '1'", "1.6.30 MSEG: хоткеи сетки -- CTRL 1..4")
    present(etext, "}); // 1.6.43: список всегда у 120,227", "1.6.34/43 Matrix: SOURCE-порядок в одном массиве позиций")
    present(ptext, "if(msegIndex>=msegPageCount())setMsegPageCount(msegIndex+1);", "1.6.31 Engine: активация при бинде не нужна (страницы фиксированы)")
    # 33. 1.6.31: UX-правки по скриншоту (chrome/LEV/SOURCE/MSEG-draw/aim)
    present(etext, "for(int i=0;i<5;++i){g.fillRect(static_cast<float>(ax-4+i),static_cast<float>(ay-5+i),2.0f,2.0f);", "1.6.31 Editor: стрелка папок смотрит ВПРАВО (к параметру)")
    present(etext, "pixel::text(g,l.getText(),l.getLocalBounds().reduced(5,0),20,true);}", "1.6.35 Editor: текст MODE/MNM -- 1-в-1 как категории: отступ 5px, шрифт 20, центр")
    present(etext, "g.drawRect(getLocalBounds(),1);} // 1.6.31: стенка заподлицо", "1.6.31 Editor: рамки PixelButton -- заподлицо с границей (имя машины = панели)")
    present(etext, "g.setColour(ink);g.drawRect(x,y,387,30,2); // 1.6.31: серый прямоугольник панели убран", "1.6.31 Editor: серые прямоугольники панелей убраны (разметка не видна)")
    present(etext, "ampModeCombo.setBounds(759,95,96,28);", "1.6.32 Editor: границы MODE = категории, края на линиях колонок")
    present(etext, "page = juce::jlimit(0, 7, m); if (isShowing()) grabKeyboardFocus(); // 1.6.31: страницы фиксированы", "1.6.31 Engine/MSEG: страницы фиксированы -- все 8 всегда активны")
    present(etext, "int lfoLockAxis() const", "1.6.30 Locks: PAGE/DEST LFO-ячеек знает ось замка")
    present(etext, "!safe->lfoTargetBlocked(result-1)", "1.6.30 Locks: чужой замок -- пункт списка недоступен и в колбеке")
    present(etext, "if(otherLocksBlock(pg,row))return;", "1.6.30 Locks: клик по имени под чужим замком игнорируется")
    present(etext, "showDocked(std::make_unique<LfoLockPanel>(processor,lfo,dest)", "1.6.30 Locks: окно замков -- док ниже ячейки (без CallOutBox)")
    present(etext, "void rebuildPageStrip()", "1.6.29 MSEG: лента страниц + «+»/«−»")
    present(etext, "void removePageWithConfirm()", "1.6.29 MSEG: удаление страницы с подтверждением")
    present(etext, "juce::AlertWindow::showOkCancelBox", "1.6.29 MSEG: диалог подтверждения удаления")
    present(etext, "b->onClick = [this] { showPageList(); };", "1.6.30 MSEG: клик по списку страниц открывает список ниже хитбокса")
    present(etext, "b->verticalDrag = true; b->dragPixelsPerStep = 14;", "1.6.30 MSEG: LMB-драг листает страницы")
    present(etext, "(sv >= 18 && sv <= 22) ? sv - 15 : -1", "1.6.29 MSEG: роутинг понимает источники MSEG4..8")
    present(etext, "std::array<juce::String, 131>& targetNames()", "1.6.29/1.7.7 MSEG: таблица целей 0..130")
    present(etext, "routeY() + row * 34", "1.6.29 MSEG: строки роутинга 34 px как в матрице")
    present(etext, "if(target>=56)return", "1.6.29 Editor: targetName знает MSEG OUT")
    present(etext, 'RIGHT CLICK POINT: REMOVE   ALT: BYPASS SNAP   CTRL+1/2 GRID -/+   CTRL+3 TRIPLET   CTRL+4 SNAP   B DRAW', "1.6.30 MSEG: подсказка с хоткеями (CTRL)")
    present(etext, "class SquareToggle final : public juce::ToggleButton", "1.6.29 Editor: галка-квадрат на всю высоту (ARP)")
    present(etext, "machineButton.flashFor(160);", "1.6.29 Editor: строка машины мигает при смене стрелкой")
    present(etext, "juce::Desktop::getInstance().getMainMouseSource().isDragging()", "1.6.29 Editor: залипшее «нажато» самоликвидируется")
    present(etext, "void dismissDock()", "1.6.30 Editor: пикер машин -- док под кнопкой, закрытие кликом вне")
    present(etext, "ampButton.textHeight=20;", "1.6.29 Editor: шрифт кнопки AMP как у всех категорий")
    present(etext, "arpEnabled.setBounds(471,57,32,32);arpButton.setBounds(507,59,116,30);", "1.6.34/1.7.0 Editor: галка ARP на уровне кнопки ARP (surf.arpon/arp)")
    present(etext, "CTRL+1/2 GRID", "1.6.30 MSEG: хоткеи сетки в подсказке (CTRL)")
    text = read(root / "NovaData.h")
    present(text, '"STEP|STEP CHORD|TRUE|UP|DOWN|CYCL|RND"', "1.6.22 Defaults: режим ARP -- STEPS, первые в списке")
    present(text, '"Route target",0,132,0,1', "1.6.32/1.7.8 Defaults: DEST 0..132 (0 = OFF, цель = значение-1)")
    present(text, '"arp_grid","ARP Rate",0,18,12,1', "1.6.22/1.7.0 Defaults: RATE арпа -- 1/16 (19 рейтов, SYNC по умолчанию)")
    present(etext, "UndoRedoButton undoBtn{true},redoBtn{false};", "1.6.32 Editor: UNDO/REDO -- две кнопки-стрелки у BPM/MENU")
    present(etext, "undoBtn.setBounds(1098,4,25,30);redoBtn.setBounds(1129,4,25,30);", "1.6.32/43 Editor: UNDO/REDO -- размер и ряд как у галки BPM-синка (surf.undo/redo)")
    present(etext, "void doUndo() { if (undoStack.empty()) return;", "1.6.32 Editor: UNDO -- полный снимок состояния (ARP/MSEG тоже)")
    present(etext, "void doRedo() { if (redoStack.empty()) return;", "1.6.32 Editor: REDO")
    present(etext, "cell->pushEdit=[this]{pushUndoPoint();};", "1.6.32 Editor: правки матрицы = точка отката")
    present(etext, "class SmallMenuLaf final : public juce::LookAndFeel_V4 {", "1.6.32 Editor: компактный скин списков MODE (высота пункта 22, шрифт 14)")
    present(etext, "static void showComboBelow(juce::ComboBox& box,bool small){", "1.6.32 Editor: списки MODE открываются ПОД кнопкой")
    present(etext, "mc->smallPopup=true; // 1.6.32: списки MODE -- компактные, по размеру кнопки", "1.6.32 Editor: списки MODE -- компактные, по размеру кнопки")
    present(etext, "const int top=y+juce::roundToInt((1.0-norm)*static_cast<double>(height));", "1.6.35 Editor: L&F LEV -- заполнение сверху вниз, top на кусочной позиции (127=верх, 100=риска, 0=низ)")
    present(etext, "c == 3 ? (value == 0 ? juce::String(\"---\") : destName(value - 1))", "1.6.32 Matrix: DEST 0 = OFF (прочерки), в списке прочерков нет")
    present(etext, "const int v = result == 1 ? 0 : result == 2 ? 18 : vals2[result - 2] + 1;", "1.6.32/43 Matrix: AUX = OFF, RANDOM, затем копия SOURCE (значение = SRC+1; 1.6.43: result-2, off-by-one убран)")
    present(etext, "\"AUX DEPTH\"", "1.6.32 Matrix: колонка AUX DEPTH (+-100%, по умолчанию 0 -- AUX молчит)")
    present(etext, "openAuxDepthMenu(r, e.getScreenPosition());", "1.6.32 Matrix: AUX DEPTH -- список по клику")
    present(etext, "hitCol == 7 ? processor.parameters.getParameter(\"r\" + juce::String(hitRow) + \"_aux_depth\")", "1.6.32 Matrix: драг/колесо AUX DEPTH (±100, дабл-клик = 0)")
    present(etext, "if (c == 5) { if (!hitWasRmb) { if (auto* p = cell(r, 5)) toggle(*p); return; }", "1.6.32 Matrix: UNI/BIP -- клик переключает, список по ПКМ")
    present(etext, "clearButton.setBounds(1013,1,115,26);", "1.6.32 Editor: CLEAR у верхней рамки")
    present(etext, "b->textHeight = 14; // 1.6.32", "1.6.32 MSEG: все кнопки одним шрифтом 14px")
    present(etext, "if (stroke.empty() || std::abs(x - stroke.back().x) >= 0.015f || std::abs(y - stroke.back().y) >= 0.02f) stroke.push_back({x, y, 0.0f});", "1.6.32 MSEG: DRAW -- штрих копится для превью")
    present(etext, "writeStroke();", "1.6.32 MSEG: DRAW -- запись один раз при отпускании (джиттер начала убран)")
    present(etext, "stroke.size() >= 2", "1.6.32 MSEG: превью штриха рисуется полупрозрачно")
    absent(ptext, "startTimerHz(4)", "1.6.32x State: таймера нет -- сохранение через штатный copyState->flushToTree (до 1.6.29)")
    present(ptext, "dvv>0?dvv-1:0", "1.6.32 Engine: DEST 0 = маршрут выключен, цель = значение-1")
    present(ptext, "static_cast<int8_t>(juce::jlimit(-64,63,auxDepthRaw", "1.6.32 Engine: AUX DEPTH +-100 (0 = молчит)")
    present(text, '"r"+juce::String(row)+"_aux_depth","Route "+juce::String(row+1)+" aux depth",-64,63,0,1,{}', "1.6.32 Defaults: 16 параметров AUX DEPTH, по умолчанию 0")
    present(etext, "menuButton.setBounds(1161,0,94,32);tempoSync.setBounds(961,4,61,30);tempoButton.setBounds(1022,5,70,30);tempoEntry.setBounds(1022,5,70,30);", "1.6.32x/43 FIX: BPM (SYNC/цифры) на ряду, surf.menu/sync/bpm применены")
    present(etext, "ampButton.setBounds(471,95,96,28);ampModeCombo.setBounds(759,95,96,28);", "1.6.32x FIX: bounds MODE-кнопок -- живой код (не в комментарии)")
    present(etext, "modHover=nullptr;modDragMatrix=false;lfoHoldIndex=-1;lfo1Button.grayBlink=lfo2Button.grayBlink=lfo3Button.grayBlink=false;", "1.6.32x FIX: после отпускания корда hover/blink ячейки ручки снимаются")
    present(etext, "setViewportMode(true); // 1.6.32: после бинда ОТКРЫВАЕТСЯ матрица", "1.6.32x Editor: бинд прицелом открывает матрицу с миганием DEST-ячейки")
    present(etext, "box.getWidth(),1)), // 1.6.44: по ПРАВОМУ краю кнопки", "1.6.32x FIX: списки MODE открываются ПОД кнопкой (target area не пустая)")
    present(etext, "g.fillAll(juce::Colours::black);g.setColour(ink);\n        pixel::text(g,l.getText(),l.getLocalBounds().reduced(5,0),20,true);}", "1.6.35 FIX: фон лейбла MODE чёрный, рамка кнопки жива")
    present(etext, "safe->processor.resetAllMsegs();for(auto* parameter:safe->processor.getParameters()){", "1.6.32x Editor: RESET ALL PARAMETERS сбрасывает и MSEG-страницы")
    present(ptext, "void MonomachineNovaAudioProcessor::resetAllMsegs(){for(auto& m:mseg)m.reset();}", "1.6.32x Processor: resetAllMsegs -- 8 страниц к дефолту")
    present(ptext, "БЕЗ перезаписей -- copyState() сам флашит атомы в дерево", "1.6.32x State: сохранение = чистый copyState (поведение до 1.6.29), ARP-атомы флашатся штатно")
    present(etext, "juce::PopupMenu m; m.setLookAndFeel(&smallMenuLaf());", "1.6.33 Matrix: список AUX DEPTH -- компактный, с рамкой, ПОД ячейкой (имена, сортировка по значению)")
    present(etext, "if (c == 7) { openAuxDepthMenu(r, e.getScreenPosition()); return; } // 1.6.33", "1.6.33 Matrix: AUX DEPTH -- клик (ЛКМ/ПКМ) открывает список")
    present(etext, "hitX = static_cast<int>(e.getScreenPosition().x); dragged = false; // 1.6.33", "1.6.33 Matrix: hitX для списковых колонок -- драг AUX DEPTH не берёт мусор (скачок +100 исправлен)")
    present(etext, "clearButton.rightClick=[this,wipeField]{juce::PopupMenu m;m.setLookAndFeel(&smallMenuLaf());", "1.6.33 Matrix: список CLEAR (ПКМ) -- компактный, как MODE")
    present(etext, "menu.setLookAndFeel(&smallMenuLaf()); // 1.6.14/33", "1.6.33 Editor: список MENU -- компактный скин")
    present(etext, "const int slot=processor.addRouteFromSource(src,cell->targetId()); // ПКМ: маршрут в матрицу", "1.6.33 Editor: бинд патч-кордом (ПКМ-мод) ОТКРЫВАЕТ матрицу")
    present(etext, "setViewportMode(true);if(matrixPage)matrixPage->flashDestCell(slot);}", "1.6.33 Editor: после корда мигает РАМКА DEST-ячейки; ячейка ручки больше НЕ мигает")
    present(etext, "erasing = true; processor.removeMsegPoint(page, near);", "1.6.33 MSEG: ПКМ по точке = РЕЖИМ СТИРАНИЯ (ведение мыши стирает точки)")
    present(etext, "if (drawMode > 0) { setMouseCursor(juce::MouseCursor::CrosshairCursor); hoverSeg = -1; repaint(); return; }", "1.6.33 MSEG: в DRAW курсор -- перекрестие; точки и изгибы не подсвечиваются")
    present(etext, "viewGrid.setSelectedItemIndex(12, juce::dontSendNotification); // 1.6.33: старт = 1/16 как GRID", "1.6.33/1.7.7 MSEG: GRID VIEW стартует синхронно с GRID (1/16, LINK включён)")
    present(etext, "shape.smallPopup=grid.smallPopup=viewGrid.smallPopup=true;", "1.6.33 MSEG: списки шейп/грид/вид -- компактные, ПОД кнопкой, драг листает")
    present(etext, 'pixel::text(g,undoMode?"UNDO":"REDO",{0,19,w,10},7,true);}', "1.6.33 Editor: UNDO/REDO -- КВАДРАТНЫЕ пиксель-стрелки + подписи UNDO/REDO")
    present(etext, "int rowAt(int x,int y)const{if(y<rowsTop()", "1.6.33 LFO: окно локов -- ДВЕ КОЛОНКИ PAGE|DEST, строки 30px, шрифт крупнее")
    present(ptext, "if(slot>0)moveModRoute(slot,0);\n    return slot;", "1.6.33 Processor: addRouteFromSource возвращает слот маршрута")
    present(ptext, "if(slot>0)moveModRoute(slot,0); // 1.6.34: новый маршрут MSEG всегда всплывает наверх", "1.6.34 Processor: addMsegRoute -- новый маршрут всегда сверху, возвращает слот")
    present(etext, 'auto node = processor.parameters.state.getChildWithProperty("id", id); // 1.6.33', "1.6.33 ARP: рандом степов пишет шаги ПРЯМО в дерево -- пресет сохраняет всегда (тихо, без notify)")
    present(text, 'juce::String targets="--";', "1.6.33 Defaults: DEST -- 65 значений, 0=OFF прочерк (MSEG8 OUT теперь выбирается)")
    present(etext, "level.setNormalisableRange(lev);}", "1.6.34 Editor: LEV -- кусочная шкала на слайдере (мышь/рисовка по рискам), параметр 0..127 линейный")
    present(etext, "for (int c = 1; c < 8; ++c) pixel::text(g, headers[c], {colX(c), 30, colWidth(c), 19}, 13, true);", "1.6.34 Matrix: заголовок AUX DEPTH на месте (цикл не обрезан)")
    present(etext, "for (int c = 1; c < 8; ++c) g.drawVerticalLine(colX(c) - 3, 30.0f, 49.0f); g.drawVerticalLine(colX(7) + colWidth(7) + 2, 30.0f, 49.0f);", "1.6.34/46 Matrix: вертикальные границы колонок в шапке (AUX SRC | AUX DEPTH)")
    absent(etext, "m.addSeparator(); // 1.6.32: RANDOM вернули", "1.6.34 Matrix: пустая ячейка-сепаратор после RANDOM в AUX SRC убрана")
    present(etext, "if(msegPickPage>=0){ // 1.6.34: прицел из строки маршрутов MSEG", "1.6.34 Editor: прицел MSEG -- маршрут страницы, возврат в MSEG, мигает строка")
    present(etext, "if(wasEmpty)processor.moveModRoute(pickRoute,0); // 1.6.34", "1.6.34 Editor: новое назначение прицелом всегда всплывает на строку 0")
    present(etext, "const int slot=processor.addMsegRoute(m,cell->targetId());setViewportMode(true);if(matrixPage)matrixPage->flashDestCell(slot);}", "1.6.34 Editor: MSEG-корд открывает матрицу и мигает DEST-ячейкой")
    present(etext, "g.drawHorizontalLine(y + 14, float(barX), float(barX + 190));", "1.6.35/1.7.1 MSEG: ампаунт едет с матрицей (заводской бар 395..585)")
    present(etext, "routeBase + static_cast<float>(static_cast<int>(e.x) - routeY0) * 0.5f", "1.6.34 MSEG: ампаунт -- горизонтальный драг")
    present(etext, "if (flashRouteRow >= 0 && juce::Time::getMillisecondCounter() < flashRouteUntil", "1.6.34 MSEG: мигание строки маршрута (рамка, как в матрице)")
    present(etext, "g.drawHorizontalLine(cy2, float(cx2 - 8), float(cx2 + 8)); g.drawVerticalLine(cx2, float(cy2 - 8), float(cy2 + 8));", "1.6.34 MSEG: UNASSIGNED -- гнездо-прицел с крестом")
    present(ptext, 'const char* fields[]={"on","src","dest","depth","mode","aux","aux_depth"}; // 1.6.34: + aux_depth -- перетаскивание/сортировка', "1.6.34 Processor: moveModRoute переносит и AUX DEPTH")
    present(etext, "level.setNormalisableRange(lev);}", "1.6.36 FIX: кусочная шкала LEV после attachment; convert-функции НЕ перепутаны (клик=100 на риске, даблклик=100=риска)")
    present(etext, "smallMenuLaf().minWidth=small?box.getWidth():0;", "1.6.35 Editor: компактный список ровно по ширине кнопки")
    present(etext, "idealWidth=juce::jlimit(96,420,juce::jmax(minWidth - 2,text.length()*12+24));", "1.6.37 Editor: пункт = кнопка-2 (рамка 1px) -- внешняя ширина списка ровно по кнопке")
    present(etext, "mode.smallPopup=true;mode.addItem(\"old\",1);", "1.6.35 EnvelopePage: режим old/mnm -- компактный список по ширине кнопки")
    present(etext, "const int keepY=viewport.getViewPositionY();", "1.6.35 Editor: возврат в MSEG сохраняет позицию скролла")
    present(etext, "viewport.setViewPosition(juce::Point<int>(0,keepY));", "1.6.35 Editor: скролл MSEG восстановлен после прицела")
    present(etext, "g.drawRect(eraseX, y + 2, 20, 18, 1);", "1.6.35/1.7.1 MSEG: кнопка стирания маршрута едет с баром")
    present(etext, "juce::jlimit(0.0,1.0,slider.valueToProportionOfLength(slider.getValue()));", "1.6.35 Editor: L&F LEV читает кусочную пропорцию слайдера")
    present(etext, "pixel::text(g, getText(), getLocalBounds().reduced(2, 0), textHeight, getJustificationType() == juce::Justification::centred);", "1.6.37 FIX (корень): лейбл комбобокса = PixelLabel со своим paint() -- drawLabel не вызывался; центр по justification")
    present(etext, "l->textHeight = 20; l->setJustificationType(juce::Justification::centred);", "1.6.37 Editor: текст MODE -- 20px как категории, по центру")
    present(etext, "int getPopupMenuBorderSizeWithOptions(const juce::PopupMenu::Options&)override{return 1;}", "1.6.37 Editor: компактный список -- рамка попапа 1px, без лишних пикселей")
    present(etext, "if (s.isVertical() && std::abs(rng.getLength() - 127.0) < 0.5 && rng.getStart() == 0.0) layout.sliderBounds = s.getLocalBounds();", "1.6.37 FIX: LEV -- зона слайдера на всей высоте (V4 давал отступы ~15px -> заполнение 233..549)")
    # ---- 1.6.38: LAYOUT EDIT (простой редактор интерфейса) + standalone
    present(etext, "struct UiLayoutStore {", "1.6.38 Editor: хранилище раскладки layout.json")
    present(etext, "static UiLayoutStore& uiLayout()", "1.6.38 Editor: глобальный доступ, первая загрузка файла")
    present(etext, "static void uiLayoutApply(juce::Component& root,const char* rootKey)", "1.6.38 Editor: применение сохранённых bounds по componentID")
    present(etext, "class LayoutOverlay final : public juce::Component {", "1.6.38 Editor: оверлей-редактор поверх страницы")
    present(etext, "uiLayout().put(rootKey+\".\"+rsItem->getComponentID(),rsItem->getBounds());", "1.6.38/47 Editor: каждое перемещение сразу пишется в файл")
    present(etext, "menu.addItem(10,\"LAYOUT EDIT\")", "1.6.38 Editor: пункт в меню")
    present(etext, "if(result==10)safe->toggleLayoutEdit();", "1.6.38 Editor: обработчик пункта")
    present(etext, "void toggleLayoutEdit()", "1.6.38 Editor: тумблер оверлея (работает и на лицевой панели, и на ARP/MSEG/MATRIX)")
    present(etext, "uiLayoutApply(*this,\"surf\");", "1.6.38 Editor: применение к лицевой панели")
    present(etext, "uiLayoutApply(*this,\"arp\");", "1.6.38 Editor: применение к ARP-странице")
    present(etext, "uiLayoutApply(*this,\"mseg\");", "1.6.38 Editor: применение к MSEG-странице")
    present(etext, 'uiLayout().has("matrix.clear"))clearButton.setBounds(uiLayout().get("matrix.clear"))', "1.6.38 Editor: применение к CLEAR матрицы")
    present(etext, 'for(auto& pid:layIds)pid.first->setComponentID(pid.second);', "1.6.38 Editor: именование компонентов для редактора")
    ptext = read(root.parent / "CMakeLists.txt")
    present(ptext, "    FORMATS VST3", "1.6.39 CMake (Synth): только VST3")
    absent(ptext, "Standalone", "1.6.39 CMake (Synth): standalone откачен")
    present(ptext, "VERSION 1.7.8", "1.7.8 CMake (Synth): версия проекта")
    ptext_fx = read(root.parent.parent / "Monomachine_Nova_FX" / "CMakeLists.txt")
    present(ptext_fx, "    FORMATS VST3", "1.6.39 CMake (FX): только VST3")
    absent(ptext_fx, "Standalone", "1.6.39 CMake (FX): standalone откачен")
    # ---- 1.6.40: отдельная прога LAYOUT EDITOR (без аудио)
    present(etext, "void MonomachineNovaAudioProcessorEditor::startLayoutEdit()", "1.6.40 Editor: старт LAYOUT EDIT для отдельной проги")
    eheader = read(root / "PluginEditor.h")
    present(eheader, "void startLayoutEdit();", "1.6.40 Header: публичный startLayoutEdit")
    emain = read(root / "LayoutEditorMain.cpp")
    present(emain, "START_JUCE_APPLICATION", "1.6.40 Main: GUI-приложение редактора расклада")
    present(emain, "editor->startLayoutEdit();", "1.6.40 Main: сразу режим правки при запуске")
    present(ptext, "juce_add_gui_app(MonomachineNovaLayout", "1.6.40 CMake: отдельное приложение LAYOUT EDITOR (в обоих деревьях)")
    present(ptext_fx, "juce_add_gui_app(MonomachineNovaLayout", "1.6.40 CMake (FX-дерево): отдельное приложение LAYOUT EDITOR")
    # ---- 1.6.41: апгрейд LAYOUT EDIT + матрица/доп-окна
    present(etext, "std::map<juce::String, std::array<int,8>> arrs;", "1.6.41 Editor: массивы в layout.json (ширины колонок)")
    present(etext, "juce::String labelOverride;", "1.6.41 Editor: переименование подписи ячейки")
    present(etext, "static void applyLayoutText(juce::Component* c,const juce::String& t)", "1.6.41 Editor: применение текста расклада")
    present(etext, "if(k.isKeyCode(juce::KeyPress::F1Key))", "1.6.41 Editor: F1 = редактор расклада")
    present(etext, "if(k.isKeyCode(juce::KeyPress::F2Key))", "1.6.41 Editor: F2 = сохранить расклад")
    present(etext, "e.mods.isAltDown()", "1.6.41 Editor: ALT = тянуть одну границу элемента")
    present(etext, "if(e.mods.isCtrlDown())snap(want);", "1.6.41 Editor: CTRL = прилипание к соседям")
    present(etext, "int guideX=-1,guideY=-1", "1.6.41 Editor: линии выравнивания")
    present(etext, "void renameDialog(juce::Component* it)", "1.6.41 Editor: даблклик по тексту = переименование")
    present(etext, 'uiLayout().setArr("matrix.colw", colw, 8)', "1.6.41 Editor: ширины колонок матрицы сохраняются")
    present(etext, "int colw[8]{15, 52, 165, 165, 150, 124, 120, 148};", "1.6.44 Editor: ресайз колонок матрицы (ширины из JSON пользователя)")
    present(etext, 'm.addItem(6,"ONLY AUX SOURCE")', "1.6.41 Editor: RMB-очистка только AUX")
    present(etext, 'm.addItem(7,"ONLY AUX DEPTH")', "1.6.41 Editor: RMB-очистка только AUX DEPTH")
    present(etext, '"mode","lock","aux","aux_depth"})wipeField(f); // 1.6.41: ERASE чистит и AUX DEPTH', "1.6.41 Editor: ERASE EVERYTHING чистит AUX DEPTH")
    present(etext, 'b->setComponentID(juce::String("form")+juce::String(formButtons.size()-1))', "1.6.41 Editor: кнопки форм MSEG доступны в редакторе")
    present(etext, 'b->setComponentID(juce::String("tool")+juce::String(toolButtons.size()-1))', "1.6.41 Editor: кнопки инструментов MSEG доступны в редакторе")
    present(etext, 'b->setComponentID("pg"+juce::String(pageButtons.size()-1))', "1.6.41 Editor: уникальный ID страниц MSEG")
    # ---- 1.6.42: редактор v3 (заметки/F4/Ctrl+Z/снап ресайза), нарисованные тексты, ARP по скрину
    present(etext, "void commentDialog(juce::Point<int> pos,int index)", "1.6.42 Editor: заметка для агента (F2 у курсора)")
    present(etext, "juce::SystemClipboard::copyTextToClipboard", "1.6.42 Editor: COPY COORDS -- координата в буфер обмена")
    present(etext, "std::vector<Comment> comments;", "1.6.42 Editor: заметки хранятся в layout.json")
    present(etext, "if(k.isKeyCode(juce::KeyPress::F4Key)){ resetAll(); return true; }", "1.6.48 Editor: F4 = СБРОС расклада к дефолту (сохранение -- кнопка SAVE)"),
    absent(etext, "if(k.isKeyCode(juce::KeyPress::F4Key)){ saveFlash(); return true; }", "1.6.48: старое F4-save убрано")
    present(etext, "if(k.getModifiers().isCtrlDown()&&k.isKeyCode('Z')){ undoOp(); return true; }", "1.6.42 Editor: CTRL+Z -- откат движения")
    present(etext, "if(k.getModifiers().isCtrlDown()&&k.isKeyCode('Y')){ redoOp(); return true; }", "1.6.42 Editor: CTRL+Y -- повтор движения")
    present(etext, "void snapResize(juce::Rectangle<int>& b,juce::Component* mover,int mask)", "1.6.42/43 Editor: прилипание краёв при ресайзе (SHIFT/ALT+CTRL, v4: corner-mask)")
    present(etext, "class TextTag final : public juce::Component {", "1.6.42 Editor: нарисованные тексты -- объекты редактора")
    present(etext, 'tagTitle=std::make_unique<TextTag>(*this,"title"', "1.6.42 Editor: название синта двигается/переименовывается")
    present(etext, 'tagRows=std::make_unique<TextTag>(*this,\"rownames\"', "1.6.42 Editor: имена строк матрицы MSEG -- тег rownames")
    present(etext, 'viewport.setComponentID("viewport");', "1.6.42 Editor: окно страниц двигается/ресайзится в редакторе")
    present(etext, "addMouseListener(&rmbWatcher,true);", "1.6.42 Editor: ПКМ в любом месте гасит доп-панель")
    present(etext, "if(item.isSeparator){g.setColour(juce::Colours::white.withAlpha(0.35f));", "1.6.42 Editor: сепаратор меню -- линия, не пустая кнопка")
    present(etext, "if(mini){g.drawRect(0,0,387,236,1);", "1.6.42 Editor: мини-ENV -- рамки AMP|MNM по скрину, настоящие кнопки поверх")
    present(etext, "ampButton.toFront(false);ampModeCombo.toFront(false);", "1.6.42 Editor: AMP и MODE -- живая шапка мини-ENV")
    present(etext, "arpEnable.setBounds(21, 20, 104, 21);", "1.6.44 ARP: раскладка по скрину arp ref clean2")
    present(etext, "lane.setBounds(21, 87, 634, 217);", "1.6.42/1.7.0 ARP: лейн -- заводская из JSON пользователя")
    present(etext, "setSize(1160, 478); startTimerHz(15);", "1.6.42 ARP: высота страницы 478 (viewport 480)")
    apg = read(root / "models" / "arpeggiator.hpp")
    present(apg, "bool autoSwap = false;", "1.6.42 Arp: AUTO SWAP PAGE в настройках движка")
    present(apg, "playingPage = playPage;", "1.6.42 Arp: playingPage для FOLLOW PLAY")
    pproc = read(root / "PluginProcessor.cpp")
    present(pproc, "settings.autoSwap=arpAutoSwap.load();", "1.6.42 Processor: AUTO SWAP передаётся в движок")
    present(pproc, "arpPageEcho.store(arp.playingPage);", "1.6.42 Processor: играющая страница для FOLLOW PLAY")
    # ---- 1.6.43: зум/F3/выделение/группы/стили текста + AUX fix + WRAP квадрат
    present(etext, "void mouseWheelMove(const juce::MouseEvent& e,const juce::MouseWheelDetails& w) override", "1.6.43 Editor: колесо = зум в режиме расклада")
    present(etext, "if(k.isKeyCode(juce::KeyPress::F5Key)){ pushHistory(); uiLayout().load(); root.resized(); repaint(); return true; }", "1.6.48/49 Editor: F5 -- перечитать layout.json с диска")
    present(etext, "std::vector<juce::Component*> sel;", "1.6.43 Editor: мультивыделение (рамка/SHIFT-клик)")
    present(etext, "std::vector<std::vector<juce::String>> groups;", "1.6.43/44 Editor: группы в layout.json (клавиша G убрана в 1.6.44)")
    present(etext, "void openColorPicker()", "1.6.43 Editor: живой пикер цвета (отдельное окно)")
    present(etext, "int cornerMaskAt(juce::Point<int> p,juce::Component* it,bool cornersOnly=false)", "1.6.43/47 Editor: угловой ресайз по диагонали + курсоры")
    present(etext, "g.setColour(juce::Colours::lime);", "1.6.43 Editor: тянемая стенка -- зелёная подсветка")
    present(etext, "juce::Colour tagColor{juce::Colours::white}; bool colorSet=false;", "1.6.43 Editor: стиль тегов -- цвет/шрифт/масштаб")
    present(etext, "vals2[result - 2] + 1; p->beginChangeGesture();", "1.6.43 FIX Matrix: AUX-меню без смещения (KEY=KEY, VEL=VEL)")
    absent(etext, "vals2[result - 3]", "1.6.43 FIX Matrix: старый off-by-one убран")
    present(etext, 'wrapBox(p, "arp_wrap", 1, 16, 16)', "1.6.43 FIX ARP: WRAP -- квадрат DragValueBox 1..16 (range/attachment восстановлены)")
    absent(text, "wrapLink", "1.6.43 FIX ARP: мёртвый SliderAttachment WRAP удалён")
    present(etext, "drawHead(\"hWRAP\",\"WRAP\"", "1.6.43 ARP: подпись WRAP -- тег")
    present(etext, "for(int i=6;i>=0&&over>0;--i)", "1.6.43 Matrix: последняя колонка не выталкивается за край")
    # ---- 1.6.43 финал: DSND/DTUN живы, readout границ, списки у 120,227, surf.*
    present(etext, "if(auto* cell=dynamic_cast<Cell*>(pc)){ if(cell->consumedPopup())return; }", "1.6.43/44 FIX: ПКМ-панель (DSND/REPITCH) не гасится тем же кликом (mouseDown раньше listener)"),
    absent(etext, "if(auto* cell=dynamic_cast<Cell*>(src))if(cell->consumedPopup())return;", "1.6.44: старый watcher (без прохода по родителям) убран")
    present(etext, "bool consumedPopup()const{return popupHandled;}", "1.6.43: ячейка сообщает о своём ПКМ-обработчике")
    present(etext, "void showDocked(std::unique_ptr<juce::Component> panel,juce::Rectangle<int> localAt,bool belowAnchor){ closeExtraPanel();", "1.6.43: dock гасит доп-панель (не сосуществуют)")
    present(etext, "juce::String nearestEdges(juce::Component* it) const", "1.6.43 Editor: readout -- ближайшие границы соседей (nx/ny)")
    present(etext, "nearestEdges(ch)", "1.6.43 Editor: nx/ny в строке readout")
    present(etext, "withTargetScreenArea(localAreaToGlobal(juce::Rectangle<int>(120, 227, 1, 1)))", "1.6.43 Matrix: списки значений всегда у 120,227")
    present(etext, "undoBtn.setBounds(1098,4,25,30);redoBtn.setBounds(1129,4,25,30);menuButton.setBounds(1161,0,94,32);tempoSync.setBounds(961,4,61,30);tempoButton.setBounds(1022,5,70,30);tempoEntry.setBounds(1022,5,70,30);", "1.6.43 Surface: surf.undo/redo/menu/sync/bpm из JSON")
    present(etext, "arpEnabled.setBounds(471,57,32,32);arpButton.setBounds(507,59,116,30);gateOuter.setBounds(624,58,42,32);", "1.7.0 Surface: surf.arpon/arp/gate -- заводские из JSON пользователя")
    present(etext, "viewport.setBounds(72,95,1180,470);", "1.6.43 Surface: surf.viewport из JSON")
    present(etext, 'juce::Rectangle<int>(72,4,399,36)', "1.6.43 Surface: surf.title из JSON")
    present(etext, 'juce::Rectangle<int>(26,16,18,29)', "1.6.43 Surface: surf.logo из JSON")
    absent(etext, "arpEnabled.setBounds(469,57,26,32)", "1.6.43: старые координаты ARP-галки убраны")
    # ---- 1.6.44: ПКМ по крутилке, булавка/драг доп-окон, групповой ресайз, Z-слой, матрица 85/133
    present(etext, "class FloatingPanel : public juce::Component {", "1.6.44: плавающие доп-окна -- драг за пустое место ЛКМ")
    present(etext, "bool pinned=false;", "1.6.44: булавка доп-окон (закреплённое не закрывается кликом мимо)")
    present(etext, "drawPin(g); // 1.6.44: булавка", "1.6.44: булавка на всех доп-панелях (REPITCH/DSND/PORTA/GUI)")
    present(etext, "if(auto* cs=dynamic_cast<CurveSlider*>(pc)){ if(cs->popup)return; }", "1.6.44 FIX: ПКМ по самой КРУТИЛКЕ открывает DSND/REPITCH (слайдер -- CurveSlider)")
    present(etext, "if(auto* fp=dynamic_cast<FloatingPanel*>(extraPanel.get()); fp!=nullptr&&fp->pinned)return;", "1.6.44: закреплённое окно живёт, пока не отцепишь")
    present(etext, "if(envelopePage!=nullptr)return; // 1.6.44: мини-ENV НЕ поверх большого окна", "1.6.44 FIX: мини-ENV не открывается поверх большого")
    present(etext, "arpEnable.setBounds(21, 20, 104, 21);", "1.6.44/1.7.0 FIX: ARP ENABLE уже -- не перекрывает текст MODE (127)")
    present(etext, "std::max(0,box.getScreenX()+box.getWidth()-estW)", "1.6.44: списки MODE-комбо по ПРАВОМУ краю -- не выпирают из категории")
    present(etext, "const int panelRight=72+((pageIndex<0?0:pageIndex%3))*399+387;", "1.6.44: списки ручек (DTIM и др.) не выпирают за категорию")
    present(etext, "void zDialog()", "1.6.44: Z -- слой выделенного (вперёд/назад + список 1..N)")
    present(etext, "void setZ(const juce::String& id,int z2,juce::Rectangle<int> r)", "1.6.44: слой хранится в layout.json (ключ z)")
    present(etext, "par->addChildComponent(ch,en.z);", "1.6.44: слой применяется из layout.json при раскладке")
    present(etext, "juce::TextButton ok{\"OK\"};", "1.6.44: пикер цвета -- кнопка OK (подтвердить)")
    present(etext, "dR=b.getRight()-rsSnap.getRight()", "1.6.44/47: ALT-край двигается у ВСЕХ выделенных (от снапшота)")
    present(etext, "nb.setWidth(juce::jmax(8,gs.second.getWidth()+dW))", "1.6.44/47: SHIFT-размер меняет ВСЕХ выделенных (от снапшота)")
    present(etext, "const int z=par?par->getIndexOfChildComponent(ch):0;", "1.6.44: hitAt берёт САМЫЙ верхний (viewport легко подцепить)")
    absent(etext, "isKeyCode('G')", "1.6.44: G-группы убраны (кнопка+патчкорд LFO -- единый элемент)")
    absent(etext, "G GROUP", "1.6.44: G убрана из подсказки редактора")
    present(etext, "g.drawHorizontalLine(y + 31, 8.0f, static_cast<float>(colX(7) + colWidth(7) + 2));", "1.6.46 Matrix: строка кончается у правой границы AUX DEPTH")
    # ---- 1.6.45 FIX: угловой ресайз от точки нажатия (grab -- offset, не точка)
    
    
    
    
    # ---- 1.6.46: границы матрицы -- шапка и строки на одной вертикали
    present(etext, "for (int c = 1; c < 8; ++c) g.drawVerticalLine(colX(c) - 3, 30.0f, 49.0f); g.drawVerticalLine(colX(7) + colWidth(7) + 2, 30.0f, 49.0f);", "1.6.46 Matrix: шапка -- те же вертикали colX-3 + передняя у ON + правая у AUX DEPTH")
    present(etext, "for (int c = 1; c < 8; ++c) g.drawVerticalLine(colX(c) - 3, float(y), float(y + 31)); g.drawVerticalLine(colX(7) + colWidth(7) + 2, float(y), float(y + 31));", "1.6.46 Matrix: строки -- все 7 границ на colX-3 (совпадают с шапкой) + правый край")
    absent(etext, "ink.withAlpha(0.25f)); g.drawVerticalLine(colX(6)", "1.6.46 Matrix: тусклый отдельный разделитель AUX убран")
    present(etext, "if (c >= 1 && std::abs(e.x - (colX(c) - 3)) <= 4) { colDrag = c;", "1.6.46/1.7.5 Matrix: передняя граница ON тянется (хитбокс = видимая линия разделителя)")
    present(etext, "setMouseCursor(hc >= 1 && std::abs(e.x - colX(hc)) <= 3", "1.6.46 Matrix: курсор ресайза и перед ON")
    # ---- 1.6.47: единый снапшот-движок ресайза + краш-гигиена
    present(etext, "int rsMode=0;", "1.6.47 Editor: единый жест (двигать/край/угол/размер) -- снапшот + точка старта")
    present(etext, "juce::Rectangle<int> rsSnap; juce::Component* rsItem=nullptr;", "1.6.47 Editor: жест = снапшот прямоугольника")
    present(etext, "const int dx=p.x-rsStart.x,dy=p.y-rsStart.y;", "1.6.47 Editor: ОДНА дельта от точки старта (никаких offset)")
    present(etext, "auto b=rsSnap;", "1.6.47 Editor: все размеры -- только от снапшота")
    present(etext, "void syncItems()", "1.6.47 Editor: пересборка items + чистка висячих указателей (краш-гигиена)")
    present(etext, "if(!alive(rsItem)){ rsItem=nullptr; rsMode=0; rsMask=0; }", "1.6.47 Editor: жест гасится, если элемент исчез")
    present(etext, "cornerMaskAt(juce::Point<int> p,juce::Component* it,bool cornersOnly=false)", "1.6.47 Editor: угол = только углы (край без ALT = двигать)")
    absent(etext, "juce::Point<int> grab;", "1.6.47: offset-семантика grab удалена (источник улетаний)")
    absent(etext, "bool dragResize=false;", "1.6.47: разрозненные флаги ресайза удалены")
    absent(etext, "auto b=cornerSnap;", "1.6.47: отдельный corner-путь удалён (всё в едином rsMode)")
    # ---- 1.6.48: чистый оверлей (один readout), F5/F4, честный undo, пин поверх
    present(etext, "const bool hot=(ch==rsItem||(ch==hover&&rsItem==nullptr));", "1.6.48 FIX: белым подсвечен ТОЛЬКО тащимый элемент (не весь список)")
    present(etext, "if(ch==rsItem){ const int by=juce::jlimit(18,getHeight()-16,r.getBottom()+2);", "1.6.48 FIX: readout один -- у тащимого (каша текстов убрана)")
    present(etext, "for(auto* ch:items){ if(!ch->isVisible())continue;", "1.6.48: невидимые элементы не рисуются")
    present(etext, "if(!ch->isVisible()||!rectOf(*ch).contains(p))continue;", "1.6.48 FIX: невидимый viewport не перехватывает клик (крутилки выделяются снова)")
    present(etext, "preState=uiLayout().toJSON(); // 1.6.48: снимок ДО жеста", "1.6.48/53: фокус на клике + снимок ДО жеста (старт -- со свежим файлом)")
    present(etext, "if(uiLayout().toJSON()!=preState){ histUndo.push_back(preState);", "1.6.48 FIX: undo -- только реальные изменения (Ctrl+Y не умирает)")
    present(etext, "if(pinned){ toFront(true); for(auto* c:getChildren())c->toFront(false); } repaint(); if(auto* pr=getParentComponent())pr->repaint();", "1.6.48/1.7.5: закреплённое окно -- поверх соседей, дети подняты (текст не прячется)"),
    absent(etext, "setAlwaysOnTop(pinned)", "1.7.0: setAlwaysOnTop на child убран (поднимал родителя, не панель)")
    present(etext, "void resetAll(){\n        syncItems();", "1.6.48: RESET ALL синхронизирует список элементов")
    # ---- 1.6.49: F3=SAVE, AMP-кнопка двигаемая, сброс тегов к заводу, Ctrl-guards
    present(etext, "if(k.isKeyCode(juce::KeyPress::F3Key)){ saveFlash(); return true; }", "1.6.49 Editor: F3 -- СОХРАНИТЬ расклад (кнопка SAVE дублирует)")
    present(etext, "F3 SAVE  F4 RESET  F5 RELOAD", "1.6.49 Editor: подсказка хоткеев (F1 close F2 note F3 save F4 reset F5 reload)")
    present(etext, '{&ampButton,"ampcat"}', "1.6.49 FIX: кнопка категории AMP -- двигается в LAYOUT EDIT (была вне layIds)")
    present(etext, "void resetBoundsToFactory(juce::Component* it)", "1.6.49 FIX: сброс возвращает теги на ЗАВОДСКИЕ места (название синта)")
    present(etext, "if(auto* tg=dynamic_cast<TextTag*>(it)){ tg->setBounds(tg->base); return; }", "1.6.49: теги -- к базовым границам при сбросе/DEL")
    present(etext, "&&!k.getModifiers().isCtrlDown()){ styleDialog(); return true; }", "1.6.49: C/Z-диалоги не открываются от Ctrl+C/Ctrl+Z")
    # ---- 1.6.50: файл == хранилище (жест без движения не пишет, undo/redo пишут на диск)
    present(etext, "const bool moved=(p-rsStart)!=juce::Point<int>(0,0);", "1.6.50 FIX: клик БЕЗ движения не создаёт запись в layout.json")
    present(etext, "if(alive&&moved){ uiLayout().put(rootKey+\".\"+rsItem->getComponentID()", "1.6.50/54 FIX: сохранение только за реальный жест (пачкой -- всё выделение)")
    present(etext, "uiLayout().loadFrom(histUndo.back()); histUndo.pop_back(); uiLayout().save();", "1.6.50 FIX: Ctrl+Z сразу в файл (расхождение экран/файл убрано)")
    present(etext, "uiLayout().loadFrom(histRedo.back()); histRedo.pop_back(); uiLayout().save();", "1.6.50 FIX: Ctrl+Y сразу в файл")
    # ---- 1.6.51: механика мышкой (UNDO/REDO/COPY JSON), save() = bool, фокус после диалогов
    present(etext, "bool save(){ file().getParentDirectory().createDirectory(); return file().replaceWithText(toJSON()); }", "1.6.51: save() возвращает успех (молчаливый сбой убран)")
    present(etext, "savedFlash=ok?1:2;", "1.6.51: SAVE FAIL! виден на экране при ошибке записи")
    
    
    
    present(etext, "juce::SystemClipboard::copyTextToClipboard(uiLayout().toJSON());", "1.6.51: COPY JSON кладёт текущий расклад в буфер обмена")
    
    present(etext, "colorWin.reset(); grabKeyboardFocus();", "1.6.51: фокус возвращается после пикера цвета")
    # ---- 1.6.52: канва без виртуальных кнопок, всё в меню F2 + ERASE FILE
    absent(etext, "juce::Rectangle<int> saveRect()", "1.6.52: виртуальные кнопки редактора убраны с канвы (перекрывали верхний ряд)")
    absent(etext, "juce::Rectangle<int> undoRect()", "1.6.52: зон кликов редактора на канве больше нет")
    present(etext, "void editMenu()", "1.6.52: F2 -- МЕНЮ редактора (SAVE/RELOAD/RESET/UNDO/REDO/COPY JSON/ERASE/NOTE)")
    present(etext, "void eraseFile(){ pushHistory(); uiLayout().eraseAll(); uiLayout().file().deleteFile();", "1.6.52: ERASE FILE -- стереть json, чистый заводской расклад")
    present(etext, "void eraseAll(){ map.clear(); arrs.clear(); comments.clear(); groups.clear(); }", "1.6.52: полное стирание хранилища расклада")
    present(etext, "F2 MENU  F3 SAVE  F4 RESET  F5 RELOAD", "1.6.52: подсказка -- F2 меню, внизу канвы")
    present(etext, "if(k.isKeyCode(juce::KeyPress::F2Key)){ editMenu(); return true; }", "1.6.52: F2 открывает меню редактора")
    # ---- 1.6.53: мультиинстанс-гонка (свежий файл на старте жеста) + диагностика записи
    present(etext, "grabKeyboardFocus(); uiLayout().load();", "1.6.53 FIX: старт жеста = перечитать файл (другой инстанс мог его перезаписать)")
    present(etext, '"entries:"+juce::String(static_cast<int>(uiLayout().map.size()))', "1.6.53: счётчик записей расклада на канве")
    present(etext, "DEBUG: PUT PROBE", "1.6.53: проба put+save в меню F2 (без жеста)")
    present(etext, 'uiLayout().put(safe->rootKey+".__probe"', "1.6.53: __probe -- запись-проверка конвейера сохранения")
    # ---- 1.6.54: пачка пишется целиком по sel, микросдвиг не ресайзит
    present(etext, "for(auto* s2:sel)if(s2!=rsItem&&std::find(items.begin(),items.end(),s2)!=items.end()&&s2->isVisible())uiLayout().put(rootKey+\".\"+s2->getComponentID(),s2->getBounds());", "1.6.54 FIX: mouseUp пишет ВСЁ активное выделение (селекция = источник записи)")
    present(etext, "if(rsMode==4&&std::abs(dx)+std::abs(dy)<3)return;", "1.6.54 FIX: микросдвиг после SHIFT-клика не ресайзит всех")
    absent(etext, "if(rsGroup.size()>1)for(auto& gs:rsGroup)if(std::find(items.begin(),items.end(),gs.first)!=items.end())uiLayout().put", "1.6.54: запись пачки больше не через книгу жеста")
    # ---- 1.6.55: WROTE-телеметрия, COPY JSON первым в меню
    present(etext, "wroteInfo=\"WROTE \"+juce::String(rsItem->getComponentID())", "1.6.55: жест показывает WROTE <id> [+N] entries:M на канве")
    present(etext, "if(wroteInfo.isNotEmpty())pixel::text(g,wroteInfo,", "1.6.55: телеметрия записи -- на канве")
    present(etext, "aw->addButton(\"COPY JSON\",6,juce::KeyPress(juce::KeyPress::returnKey));", "1.6.55: COPY JSON -- ПЕРВАЯ кнопка меню F2 и на Enter")
    absent(etext, "NOTE AT CURSOR", "1.6.55: старое подменю заметок из меню убрано")
    # ---- 1.7.0: COPY COORDS, lane 21/87, MSEG-заводская, 19 рейтов, hold-клик, пин
    present(etext, "aw->addButton(\"COPY COORDS\",10);", "1.7.0: COPY COORDS вернулся в меню F2 (x,y из-под курсора)")
    present(etext, "copyTextToClipboard(juce::String(safe->lastMouse.x)+\",\"+juce::String(safe->lastMouse.y))", "1.7.0: копирует x,y курсора")
    present(etext, "lane.setBounds(21, 87, 634, 217);", "1.7.0 ARP: lane -- из JSON пользователя (было 14,147,1150,326)")
    present(etext, "shape.setBounds(146, 70, 182, 26);", "1.7.0 MSEG: заводская из JSON пользователя")
    present(etext, "static const int tx[]{336, 468, 608, 743, 892, 1024};", "1.7.0 MSEG: инструменты -- из JSON пользователя")
    present(etext, "pixel::text(g, \"SHIFT\", {shift.getX() - 66, shift.getY() + 4, 60, 18}", "1.7.0 MSEG: подпись SHIFT едет с ручкой")
    present(etext, "shift.getRight() + 8, shift.getY() + 4", "1.7.0 MSEG: значение SHIFT едет с ручкой")
    present(text, "\"1/1|1/2D|1/1T|1/2|1/4D|1/2T|1/4|1/8D|1/4T|1/8|1/16D|1/8T|1/16|1/32D|1/16T|1/32|1/64D|1/32T|1/64\"", "1.7.0 ARP: 19 рейтов от меньшего к большему (Sylenth1)")
    present(read(processor), "const double ticks[]{96,72,64,48,36,32,24,18,16,12,9,8,6,4.5,4,3,2.25,2,1.5}", "1.7.0 ARP: тики новых 19 рейтов")
    present(text, "\"arp_step_random\",\"Page random\",0,1,0,1,\"OFF|ON\"", "1.7.0 FIX ARP: Page random -- бинарный, галка не слетает")
    present(etext, "void applyHoldClick(const juce::MouseEvent& e, const Geo& geo)", "1.7.0 ARP: ЛКМ-клик по HOLD всегда ставит")
    present(etext, "for(auto* c:getChildren())c->toFront(false);", "1.7.0/1.7.5 FIX: пин поднимает детей панели (setAlwaysOnTop на child поднимал родителя)")
    absent(etext, 'pixel::text(g, "ROUTING / SIGNED AMOUNT', "1.7.0 MSEG: описание удалено")
    mmtext = read(root / "models" / "modulation_matrix.hpp")
    ndtext = read(root / "NovaData.h")
    # ---- 1.7.1: P-LOCK, STEP RND, папка ARP в DEST, RETRIG, ресайз MSEG, фикс пина/клампов
    present(etext, "class PlockLane final", "1.7.1 ARP: линия P-LOCK (как velocity/hold)")
    present(etext, "std::function<void(int)> armRequest; // 1.7.1: прицел P-LOCK (замыкает Surface)", "1.7.1 ARP: прицел P-LOCK -- арм из страницы")
    present(etext, "void beginPlockPick(int slot)", "1.7.1 Surface: прицел P-LOCK -- клик по ручке = цель слота")
    present(etext, "if(pickingTarget&&plockPickSlot>=0)", "1.7.1 Surface: completeTargetPick ведёт цель в слот P-LOCK")
    present(read(processor), "plockStep(arp.playingPage,arp.stepEcho);", "1.7.1 DSP: применение P-LOCK на степе")
    present(read(processor), "R.sliding=true;R.from=R.now", "1.7.1 DSP: SLIDE -- диагональ до следующего p-lock")
    present(read(processor), "settings.stepRnd=global[ArpStepRnd]>0.5f;", "1.7.1 ARP: STEP RND -- из глобального параметра")
    present(read(processor), "settings.stepRandom=juce::jlimit(0.0f,1.0f,global[ArpStepRandom]);", "1.7.1 FIX ARP: PAGE RND без /100 (движок видел OFF)")
    present(mmtext, "static constexpr size_t kTargetCount = 132;", "1.7.8 DEST: ROUTE 1..64 (67..130) + LFO FM (131)")
    present(mmtext, "if (paramId >= 64) return paramId == 64 ? \"ARP RATE\" : \"ARP GATE\";", "1.7.1 DEST: имена целей папки ARP")
    present(ndtext, "\"arp_step_rnd\",\"Step random order\",0,1,0,1,\"OFF|ON\"", "1.7.1 ARP: бинарный STEP RND (порядок степов)")
    present(ndtext, 'n+"_retrig","MSEG"+(mi>1?', "1.7.2/1.7.7 MSEG: retrig постранично -- теперь СПИСОК RETRIG|ENVELOPE|FREE (default RETRIG)")
    present(read(processor), "MsegRetrig+4*mi", "1.7.2/1.7.8 MSEG: рестарт фазы своей страницы (шаг 4: retrig,rmode,key,lpoint)")
    present(read(root / "PluginProcessor.h"), "kPlockSlots=32", "1.7.2 P-LOCK: слоты безграничны (32, играют все)")
    present(read(processor), "int MonomachineNovaAudioProcessor::plockAcquire(int slot,int target)", "1.7.2 P-LOCK: acquire -- та же цель = тот же слот")
    present(read(processor), "if(plockTarget(s)<0)continue;", "1.7.2 P-LOCK: пустые слоты не хранятся в состоянии")
    present(etext, "PixelButton slotBtn{\"S1\"}", "1.7.2 P-LOCK: лейн один, список слотов вызывает нужный")
    present(etext, "void plockAfterBind(int slot, int target)", "1.7.2 P-LOCK: прицел ведёт цель через acquire")
    present(etext, "tag + \"_retrig\", retrig", "1.7.2/1.7.7 MSEG: retrig биндится постранично (ComboBoxAttachment)")
    present(etext, "slotBtn.setBounds(670, 312, 44, 26);", "1.7.2 ARP: кнопка списка слотов")
    present(ndtext, "\"Route target\",0,132,0,1", "1.7.8 DEST: диапазон целей 0..132")
    present(read(processor), "jlimit(0,130,static_cast<int>(target)); // 1.7.7", "1.7.7: кламп прицела 0..130 (PITCH/ROUTE 1..64)")
    present(etext, "retrig.setBounds(920, 70, 72, 26); // 1.7.5 FIX", "1.7.5 MSEG: RETRIG на 920,70 -- виден на всех страницах (600 тоже перекрывался)")
    present(etext, "graphW = juce::jlimit(600, 2400, 984 + (getWidth() - 1170));", "1.7.1 MSEG: окно редактируемого размера (грип)")
    present(etext, "uiLayout().setArr(\"msegsize\", arr, 8);", "1.7.1 MSEG: размер окна в layout.json")
    present(etext, "const int rowX = tagHeads->getX(), barX = tagRows->getRight() + 11, eraseX = barX + 261;", "1.7.1/1.7.5 FIX MSEG: матрица дестинейшенов едет за тегами rowheads/rownames")
    present(etext, "double wheelAccum=0; // 1.7.1: накопление дельты", "1.7.1 MSEG: скролл списков смягчён (накопление)")
    present(mmtext, "arpDests[route.targetParamId - 64] += source * depth; continue; }", "1.7.1/1.7.5 DSP: маршруты на ARP RATE/GATE/PITCH")
    # ---- 1.7.3: P-LOCK в undo/redo + FIX сохранения target/pol (1.7.2)
    present(read(processor), "juce::ValueTree MonomachineNovaAudioProcessor::plockToTree() const", "1.7.3 UNDO: P-LOCK сериализуется в helper (хост и undo)")
    present(read(processor), "void MonomachineNovaAudioProcessor::plockFromTree(const juce::ValueTree& pl)", "1.7.3 UNDO: восстановление слотов из дерева")
    present(read(processor), "plockOn.fill(false); // 1.7.3", "1.7.3: снятый/откатившийся слот отпускает цель сразу")
    present(read(processor), 'n.setProperty("target",plockTarget(s),nullptr);n.setProperty("pol",plockPolarity(s),nullptr);', "1.7.3 FIX: target/pol снова пишутся в состояние (в 1.7.2 съедены комментарием)")
    absent(read(processor), "не хранимn.setProperty", "1.7.3 FIX: битая строка 1.7.2 устранена")
    present(etext, "st.addChild(processor.plockToTree(),-1,nullptr);", "1.7.3 UNDO: снимок undo включает P-LOCK")
    present(etext, "processor.plockFromTree(pl); bind(); repaint(); }", "1.7.3 UNDO: applyTree восстанавливает P-LOCK")
    present(etext, "plck.pushEdit=[this]{ if (pushEdit) pushEdit(); }; destBox->pushEdit=plck.pushEdit;", "1.7.3 UNDO: правки линии/цели = точки отката")
    present(etext, "plockPickSlot=-1;pickingTarget=false;pushUndoPoint();", "1.7.3 UNDO: прицел-бинд P-LOCK = точка отката")
    # ---- 1.7.4: FIX мёртвых ручек после прицела P-LOCK + RETRIG из-под SHAPE + без дублей PLOCK
    present(etext, "for(auto& column:cells)for(auto& cell:column)if(cell)cell->setPickMode(false); // 1.7.4 FIX", "1.7.4 FIX: прицел P-LOCK снимает режим выбора -- ручки снова крутятся")
    present(read(processor), "state.removeChild(state.getChildWithName(\"PLOCK\"),nullptr);state.addChild(plockToTree(),-1,nullptr);", "1.7.4: PLOCK в состоянии без дублей после undo")
    # ---- 1.7.5: PITCH/ROUTE-цели, P-LIST слоты, DETACH ARP, прицел из матрицы, PAGE RND, MMB-pan
    present(etext, "\"P-LIST\"", "1.7.5 Editor: кнопка слотов P-LOCK (драг листает слоты, клик = список)")
    present(etext, "\" - \" + targetName", "1.7.5 P-LIST: формат \"N - имя параметра\"")
    present(etext, "erSlide", "1.7.5 P-LOCK: слайд-ряд = высота HOLD, ЛКМ только клик-тумблер, ПКМ стирает свой ряд")
    present(etext, "rowheads", "1.7.5 MSEG: теги rowheads = хитбокс+выделение строк в layout")
    present(etext, "colMin", "1.7.5 Matrix: минимум ширины колонок -- текст не схлопывается")
    present(etext, "aimDragRow", "1.7.5 Matrix: ПКМ-патчкорд из прицела в DEPTH/AUX DEPTH другой строки (dest = ROUTE)")
    present(etext, "destName(int id)", "1.7.5 Matrix: DEST показывает \"N - что модулирует\" для строк-маршрутов")
    present(etext, "isMiddleButtonDown()", "1.7.5: СКМ+драг = пан зумленного layout")
    present(etext, "toggleSize", "1.7.5 Envelope: ПКМ = переключение малое/большое в обоих режимах")
    present(etext, "detachArp", "1.7.5: DETACH ARP -- отдельным окном (повторный клик = вернуть)")
    present(etext, "seenRateText", "1.7.5: SYNC/RATE мигание устранено -- repaint только по изменению")
    present(etext, "setSize(238, 90)", "1.7.5 DSND: компактное окно")
    present(etext, "pixel::text(g, juce::String(r + 1)", "1.7.5 Matrix: нумерация строк 1..16 перед ON")
    present(ndtext, "targets+=\"|PITCH\"", "1.7.5 Defaults: PITCH (66) + ROUTE 1..16 в списке целей")
    present(read(root / "models" / "arpeggiator.hpp"), "rs.velocity = static_cast<uint8_t>(1u + rng % 127u);", "1.7.7 Arp: PAGE RND -- случайные шаги (velocity 1..127), старая randomPage-реализация снята")
    present(read(processor), "plockResetAll", "1.7.5: RESET ALL сбрасывает P-LOCK (с точкой отката)")
    # ---- 1.7.6: критичный фикс звука (evaluate съеден комментарием) + репорты
    present(read(processor), "matrix.evaluate(before,after,msegMod.data(),arpMod.data(),routeExtra.data(),lfoFm.data());", "1.7.6/1.7.8 КРИТИЧНО: вызов evaluate жив (+ARP-цели; +routeExtra; 1.7.8: +LFO FM)")
    present(mmtext, "route.targetParamId >= 67) continue;", "1.7.6 FIX: ROUTE-цели исключены из pass 2 (была запись за границей arpDests)")
    present(read(processor), "msegPhaseUi[static_cast<size_t>(mi)].store(msegPhase[static_cast<size_t>(mi)]", "1.7.6: фазы MSEG в UI для PLAY FOLLOWER") if False else None
    present(read(processor), "msegPhaseUi[static_cast<size_t>(mi)].store", "1.7.6: фазы MSEG в UI -- маркер PLAY FOLLOWER")
    present(etext, "pointToX(static_cast<float>(markerDisp))", "1.7.7 MSEG: маркер на графе с делителем/фризом (база 1.7.6 PLAY FOLLOWER)")
    present(etext, "autoSwapToggle.setToggleState(processor.arpAutoSwap.load()", "1.7.6 ARP: галки AUTO SWAP/FOLLOW PLAY не отжимаются при закрытии окна")
    present(etext, "arpFollowPlay", "1.7.6: FOLLOW PLAY хранится в процессоре")
    present(etext, "pageLabel(cur-2,k)", "1.7.6 FIX: LFO LOCKS -- имена без сдвига таблицы (LFO3 читала за таблицей: мусор TOM/CLAP/RIM)")
    present(etext, "randomizePlock(pageDisplay(),pageDisplay()", "1.7.6: PLK/SLIDE рандомят вид страницы (FOLLOW PLAY)")
    present(etext, "if (sl != curSlot()) setCurSlot(sl);", "1.7.6: слот после фолбэка сразу виден в линии P-LOCK")
    present(etext, "callAsync([sp]{if(sp!=nullptr){sp->closeOverlay();sp->toggleEnvMini();}})", "1.7.6 Envelope: ПКМ из большого -- закрыть и открыть малое на месте панели")
    present(etext, "callAsync([sp]{if(sp!=nullptr){sp->dismissDock();sp->showEnvelope(-1);}})", "1.7.6 Envelope: ПКМ из малого -- большое окно")
    present(etext, "struct ArpDialog", "1.7.6: детач-окно ARP -- крестик работает, заголовок чёрный")
    present(etext, "arpWindow->setLookAndFeel(&getLookAndFeel())", "1.7.6: детач-окно в стиле мода (MODE: STEP и списки не джус)")
    present(etext, "closeOverlay(); } // страница ARP в оверлее закрывается", "1.7.6/1.7.7: при детаче исходная страница ARP закрывается -- панель свободна")
    present(etext, "if(!matrix&&arpWindow){arpWindow.reset();setViewportMode(false);return;}", "1.7.7: кнопка ARP при детаче ВОЗВРАЩАЕТ страницу (toggle)")
    present(etext, "flipIcon", "1.7.6: DETACH -- иконка квадратик со стрелкой, кнопка чуть выше")
    present(etext, "aimDragRow >= 0) { const float d = w.isReversed", "1.7.6 Matrix: колесо в режиме патчкорда листает матрицу, режим не отменяется")
    present(etext, "if (pickStart) pickStart(page, rest); return; } } } // 1.7.6", "1.7.6 MSEG: прицел ЛКМ как раньше; список -- только по ПКМ на UNASSIGNED ниже занятых строк")
    # ---- 1.7.7: PAGE RND (шаги с питчем), матрица 64, MSEG 256, WAV import, RETRIG/ENVELOPE/FREE, сетка ARP, P-LIST имя, детач-режим, маркер
    present(mmat, "static constexpr size_t kTargetCount = 132;", "1.7.8 Matrix: 132 цели -- ROUTE 1..64 (67..130) + LFO FM (131)")
    present(read(processor), "std::array<float,64> routeExtra{};", "1.7.7 DSP: 64-й слот в evaluate (звук не тронут -- сумма та же)")
    present(read(processor), "dvv(juce::jlimit(0,131,juce::roundToInt(") if False else present(read(processor), "juce::jlimit(0,131,", "1.7.7 DSP: dest-кламп матрицы 0..131")
    present(read(processor), "if(schema<13)", "1.7.8 Processor: миграция rate 0.05..20 -> 0..1 + RMODE по старой SYNC (schema 13); 1.7.7: schema<12")
    present(read(processor), "mseg", "") if False else None
    present(read(processor), "hostWasPlaying", "1.7.7 HOST: фронт play/stop -- рестарт фаз MSEG")
    present(read(processor), "mode<2", "") if False else None
    present(read(processor), "if(mmode==1)msegPhase[static_cast<size_t>(mi)]=1.0;", "1.7.7 MSEG: ENVELOPE -- hold на последней точке (без wrap)")
    present(read(root / "PluginProcessor.h"), ",5>,64> routeRaw{}", "1.7.7 Processor: матрица 64 слота x 5 параметров")
    present(read(root / "PluginProcessor.h"), "*,64> auxRaw{}", "1.7.7 Processor: AUX на 64 слота")
    present(read(root / "NovaData.h"), '"Route target",0,132,0,1', "1.7.8 NovaData: DEST 0..132")
    present(read(root / "NovaData.h"), '"RETRIG|VITAL|FREE|SUSTAIN|LOOP POINT|LOOP HOLD"', "1.7.8 NovaData: 6 режимов MSEG как в Vital (ENVELOPE переименован в VITAL)")
    present(read(root / "NovaData.h"), "for(int rn=0;rn<64;++rn){targets+=\"|ROUTE \"", "1.7.7 NovaData: 64 строки маршрутов (DEST-список)")
    present(etext, "for (int r = 0; r < 64; ++r) for (int c = 0; c < 5; ++c) cells", "1.7.7 Editor: матрица 64 строки UI")
    present(etext, "g.drawRect(8, 26, 1120, 64 * 34 + 36, 1);", "1.7.7 Editor: рамка матрицы на 64 строки")
    present(etext, "isInterestedInFileDrag(const juce::StringArray& files) override", "1.7.7 MSEG: WAV drag&drop (интерес к .wav)")
    present(etext, "void filesDropped(const juce::StringArray& files, int, int) override", "1.7.7 MSEG: WAV -> до 256 точек (вейвтейбл)")
    present(etext, "for (int i = 0; i < 6 && i < markerBtns.size(); ++i)", "1.7.7 MSEG: MARKER 1:1..1:8 / FRZ / OFF под маршрутами")
    present(etext, "markerDiv = 1 << i", "1.7.7 MSEG: делитель скорости маркера")
    present(etext, "if (e.mods.isCtrlDown() && !e.mods.isPopupMenu() && near >= 0)", "1.7.7 MSEG: Ctrl-клик -- выделение точек")
    present(etext, "std::vector<int> doomed;", "1.7.7 MSEG: ПКМ по выделенной точке стирает ВСЕ выделенные")
    present(etext, 'retrig.speedKey="MSEG GRID"', "1.7.7 MSEG: RETRIG -- DragChoice-список (стиль мода)")
    present(etext, "static float gridDenF(int idx)", "1.7.7 MSEG: сетка как у ARP -- 20 пунктов (bars/dotted/triplet до 1/64)")
    present(etext, '{"GRID OFF","GRID 1/1","GRID 1/2D","GRID 1/1T","GRID 1/2","GRID 1/4D","GRID 1/2T","GRID 1/4","GRID 1/8D","GRID 1/4T","GRID 1/8","GRID 1/', "1.7.7 MSEG: список GRID/VIEW из 20 пунктов (bars/dotted/triplet до 1/64)")
    present(etext, "processor.setMsegSteps(page, !stepsButton.getToggleState())", "1.7.7 MSEG: ПКМ STEPS = toggle режима (запекание убрано)")
    present(etext, "out.size()) + 2 <= 256", "1.7.7 MSEG: дорисовка до 256 точек (лимит съеден)")
    present(read(root / "models" / "arpeggiator.hpp"), "settings.rndSteps", "1.7.7 PAGE RND: transient-паттерн 8 шагов (не пишет параметры пользователя)")
    present(read(root / "models" / "arpeggiator.hpp"), "rs.transpose = static_cast<int8_t>(static_cast<int>(rng % 25u) - 12)", "1.7.7 PAGE RND: транспон +-1 октава")
    present(read(root / "models" / "arpeggiator.hpp"), "chordTranspose = rs.transpose", "1.7.7 PAGE RND: StepChord играет случайный шаг аккордом")
    present(etext, "randomizeRange(pg,pg,true,true,true);randomizePlock(pg,pg,true,true)", "1.7.7 ARP: PAGE рандомит ВСЁ на странице (включая P-LOCK)")
    present(etext, "slotBtn.setButtonText(tg0 >= 0 ? targetName(processor, tg0) : juce::String(\"P-LIST\"))", "1.7.7 P-LIST: кнопка показывает ТЕКУЩИЙ параметр слота")
    present(etext, "setTitleBarHeight(0);setAlwaysOnTop(true);", "1.7.7 DETACH: окно без заголовка, поверх всех окон")
    present(etext, "page->detachRequest = [this] { detachArp(); };", "1.7.7 DETACH: кнопка DETACH на окне возвращает страницу (toggle)")
    present(read(root / "models" / "DspModes.hpp"), "kDspModeSchemaVersion = 13", "1.7.8 DspModes: schema 13")

    # ---- 1.7.8: Vital-батч -- рейт-режимы LFO, VITAL-рисование, loop/sustain/loop-point/keytrack, LFO FM до 20kHz
    present(read(root / "models" / "modulation_matrix.hpp"), "float* lfoFm = nullptr", "1.7.8 Matrix: 6-й аргумент evaluate -- LFO FM (октавы)")
    present(read(root / "models" / "modulation_matrix.hpp"), 'if (paramId >= 131) return "LFO FM";', "1.7.8 Matrix: цель 131 = LFO FM")
    present(read(root / "NovaData.h"), '"TEMPO|TRIPLET|DOTTED|TIME|HZ"', "1.7.8 NovaData: RMODE -- семья шкалы RATE (как Vital TempoSelector)")
    present(read(root / "NovaData.h"), 'n+"_rmode"', "1.7.8 NovaData: rmode постранично (8)")
    present(read(root / "NovaData.h"), 'n+"_key"', "1.7.8 NovaData: KEYTRACK постранично")
    present(read(root / "NovaData.h"), 'n+"_lpoint"', "1.7.8 NovaData: LOOP POINT постранично (0..1, дефолт 0.5)")
    present(read(root / "NovaData.h"), 'targets+="|LFO FM";', "1.7.8 NovaData: цель LFO FM в DEST-списке")
    present(read(processor), "static const double cpb[15]", "1.7.8 DSP: таблица TEMPO 32bar..1/256+FAST (циклов на бит)")
    present(read(processor), "rate=0.001*std::pow(10.0,4.301*rv)", "1.7.8 DSP: HZ -- лог-кривая 0.001..20kHz")
    present(read(processor), "rate=1.0/std::max(0.001,std::pow(0.001*60000.0,rv))", "1.7.8 DSP: TIME -- период 1ms..60s")
    present(read(processor), "rate*=std::pow(2.0,(static_cast<double>(currentKey%128)-60.0)/12.0)", "1.7.8 DSP: KEYTRACK -- октава вверх = x2")
    present(read(processor), "lfoFmValue.load(std::memory_order_relaxed)", "1.7.8 DSP: LFO FM применяется к rate (до ~20 kHz)")
    present(read(processor), "if(mmode==3)msegPhase[static_cast<size_t>(mi)]=held?std::min(msegPhase[static_cast<size_t>(mi)],lp):1.0;", "1.7.8 DSP: SUSTAIN -- держит LOOP POINT, после note-off доигрывает")
    present(read(processor), "mmode==4)msegPhase[static_cast<size_t>(mi)]=msegPhase[static_cast<size_t>(mi)]-1.0+lp", "1.7.8 DSP: LOOP POINT -- крутит хвост lp..1 (как в Vital)")
    present(read(processor), "mmode==5)msegPhase[static_cast<size_t>(mi)]=held?std::fmod(msegPhase[static_cast<size_t>(mi)],lp)", "1.7.8 DSP: LOOP HOLD -- держит ноту: цикл 0..lp, отпустил -- хвост")
    present(read(processor), "if(schema<13)", "1.7.8 DSP: миграция rate/RMODE по старой SYNC (звук сохранён: TEMPO -- ближайшая доля, HZ -- та же частота)")
    present(etext, 'rmodeBox.speedKey="LFO RATE MODE"', "1.7.8 Editor: R MODE -- DragChoice рядом с ретригом")
    present(etext, 'keyBtn.setButtonText("KEY")', "1.7.8 Editor: KEY -- трекинг высоты")
    present(etext, 'lpoint.setRange(0.0, 1.0, 0.01)', "1.7.8 Editor: LOOP POINT -- слайдер под маршрутами")
    present(etext, 'tag + "_rmode", rmodeBox', "1.7.8 Editor: R MODE биндится постранично")
    present(etext, 'tag + "_lpoint", lpoint', "1.7.8 Editor: LOOP POINT биндится постранично")
    present(etext, '{"RETRIG","VITAL","FREE","SUSTAIN","LOOP POINT","LOOP HOLD"}', "1.7.8 Editor: список режимов MSEG = Vital sync-type")
    present(etext, 'const char* forms[]{"DRAW","DRAW STEP","RAND DRAW 1","RAND DRAW 2","VITAL"}', "1.7.8 Editor: кнопка VITAL добавлена, старые режимы рисования НЕ тронуты")
    present(etext, "void vitalStroke()", "1.7.8 Editor: vital-перо -- сглаживание скользящим окном, до 256 точек")
    present(etext, "if (drawMode == 5) vitalStroke()", "1.7.8 Editor: VITAL финализирует штрих на mouseUp")
    present(etext, 'rate.setRange(0.0, 1.0, 0.01)', "1.7.8 Editor: RATE 0..1 -- позиция в семье RMODE")
    present(etext, '" / 256 POINTS"', "1.7.8 Editor: счётчик точек до 256")
    present(etext, "pts.size()) + 2 > 256", "1.7.8 Editor: ступени STEPS до 256 точек")

    if failures:
        print(f"\nFAIL: {len(failures)} check(s) failed", file=sys.stderr)
        return 1
    # ---- защита от C2026 (MSVC режет строковые литералы ~16КБ)
    text = read(editor)
    _mx = 0
    import re as _re
    for _m in _re.finditer(r'"(?:[^"\\]|\\.)*"', text):
        _mx = max(_mx, len(_m.group(0)))
    if _mx > 15000:
        print("\nFAIL: строковый литерал %d символов -- MSVC C2026; разбей на соседние литералы" % _mx)
        return 1
    print("\nсамый длинный строковый литерал %d символов (лимит MSVC ~16380) -- OK" % _mx)
    print("\nPASS static patch verification")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
