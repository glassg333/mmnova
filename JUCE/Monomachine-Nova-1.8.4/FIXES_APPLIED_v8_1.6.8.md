# Monomachine Nova 1.6.8 — правки по запросу (сентябрь 2026)

Все пункты сделаны сразу в ОБОИХ проектах (Synth и FX). Сборка не запускалась
(по правилу репозитория), но оба `PluginEditor.cpp` и `PluginProcessor.cpp`
проверены `g++ -fsyntax-only` с реальными заголовками JUCE 8 — 0 ошибок,
`verify_dsp_mode_patch.py` — PASS для обоих проектов, `tests/MnmCoreTests` — all OK.

## 1. ARP: квадратные степы вместо фейдеров
`StepLane` переписан: два ряда по 8 ЖИРНЫХ КВАДРАТНЫХ падов 127×127 с зазором
8px (как степ-сек). Верхний ряд — TRANSPOSE (заливка от центра), нижний —
VELOCITY (заливка снизу), в каждом паде — значение цифрами, у держащихся шагов —
жирная «шапка» 4px сверху. Взаимодействие прежнее: тянешь вертикально — меняешь
значение, тянешь в сторону — рисуешь по падам, ПКМ — hold. Слева подписи TR/VEL.

## 2. LFO/MSEG поменялись местами + заголовок LFO без цифры
* Заголовок LFO-панели теперь просто **LFO** (номер страницы виден на кнопках
  [LFO1]/[LFO2]/[LFO3] в шапке).
* Весь блок LFO (панель + кнопки LFO1/2/3) переехал наверх вправо — туда, где
  был MSEG; MSEG (панель + кнопки MSEG/MSEG OUT) — вниз вправо, где был LFO.

## 3. Прицел LFO1/2/3: маршрут всплывает в матрице
`addRouteFromSource` теперь переставляет свежий маршрут на **первую строку
матрицы** (`moveModRoute(slot,0)`), чтобы сразу видеть, что создалось.

## 4. Матрица: сортировка по колонке ON
Заголовки: ON | SOURCE | DESTINATION | DEPTH | MODE. Клик по ON сортирует
(по умолчанию включённые сверху), стрелка-треугольник теперь рисуется пикселями
(старые символы ▼/▲ в пиксельном шрифте не отображались).

## 5. Прицел LFO выбирает ВНУТРЕННИЙ адресат
При наведении во время перетаскивания кнопки LFO1/2/3 сразу выставляются
PAGE (1-й параметр страницы LFO: PTCH|SYNT|AMP|FILT|EFFX|LFO1|LFO2|LFO3) и
DEST (2-й параметр, 0..7) — то есть внутренний адресат, о котором шла речь.
Маршрут в матрице при отпускании по-прежнему создаётся.

## 6. SRR/DSP MODE по ПКМ открывается у курсора
`showDspModes` получил координаты курсора: `withTargetScreenArea(1x1 у курсора)`
вместо привязки к кнопке DSP MODE (которая далеко). Клик по самой кнопке DSP
MODE по-прежнему открывает меню у кнопки.

## 7. RETUNE (repitch) у DTIM — слайдер, а не список
ПКМ на DTIM открывает call-out бокс у курсора со слайдером 0..3
(OFF/FAST/MED/SLOW), значение пишется прямо во время перетаскивания.

## 8. Кракозябры: все подписи UI — английские + проверочная строка
Причина кракозябр: русские тексты хранились как `\xNN` UTF-8-байты, а
`juce::String(const char*)` копирует байты как Latin-1; плюс в пиксельном
шрифте вообще нет кириллицы. Все строки меню DSP MODE и описаний режимов
переведены на английский (models/DspModes.hpp + PluginEditor.cpp).
В конце меню DSP MODE добавлена строка **ENCODING TEST: проверка** — слово
«проверка» специально оставлено в СТАРОЙ сломанной кодировке: если оно
выглядит кракозябрами, а остальной текст читается — всё правильно; если
кракозябры вернулись у английских строк — кодировка снова сломалась.
Пустой файл-проверка: `тест ю кен делит ми иф ю вонт.txt` в корне 1.6.8.

## 9. Режим SYNT запоминается ПО КАЖДОЙ МАШИНЕ
Вместо одного общего `mode_synt` теперь параметры **`mode_synt_m<id>`** —
свой `mnm|old` для каждой машины. Листаешь машины — у одной остаётся old,
у другой mnm. Заголовок SYNT и подменю DSP MODE показывают режим выбранной
машины (в названии подменю — имя машины). Удалить `old` или `mnm` у раздела
целиком — по-прежнему одна строка в `dspSectionModeChoices()` (models/DspModes.hpp).
Схема состояния 10 → **11**: старые состояния наследуют `mode_synt` всем машинам.

## Проверки
```
python3 verify_dsp_mode_patch.py Monomachine_Nova_Synth/Source   -> PASS
python3 verify_dsp_mode_patch.py Monomachine_Nova_FX/Source      -> PASS
g++ -std=c++17 -fsyntax-only (PluginEditor.cpp / PluginProcessor.cpp, JUCE 8) -> 0 ошибок
g++ -std=c++17 -I Source tests/MnmCoreTests.cpp -> all OK
```


## Дополнительно 1.6.8.1 (вторая волна правок)

1. **Страница LFO снова с ручками.** Баг был в bind(): панель 2 ошибочно
   исключалась из привязки -- теперь пропускается только панель 5 (MSEG),
   и LFO-панель снова показывает PAGE/DEST/TRIG/WAVE/MULT/SPD/INTL/DPTH
   выбранного LFO (кнопками LFO1/2/3 в шапке).
2. **Логика кнопок LFO1/2/3:** ЛКМ-перетаскивание = прямой внутренний адресат
   (PAGE+DEST самого LFO, замена текущего адресата на лету при наведении,
   БЕЗ матрицы); ПКМ-перетаскивание = маршрут в матрицу (с всплытием на
   первую строку). Клик ЛКМ -- показать страницу этого LFO.
3. **Кнопки LFO одинаковые и ровные:** 90x24 в одну линию в шапке панели,
   выбранная -- залита (toggle state), а не "[LFO1]" скобками.
4. **DSP MODE -- плоский прокручиваемый список** (длинное меню листается само),
   блоки помечены "FOLDER: ...", вложенные подменю убраны.
5. **REPITCH -- непрерывная шкала 0..3:** 0 = OFF (мгновенно), 1 = FAST,
   2 = MED (дефолт как был), 3 = «лента» ~2.5 с; промежуточные -- линейно.
   Дефолт (2 = MED) не изменился. Под репитчем -- **SMOOTH** (антиклик):
   0 = как было, 127 = самая мягкая доводка ~10 мс при резкой смене длины.
6. Проверочное слово в меню оставлено сломанным НАМЕРЕННО (канарейка).

Проверки: g++ -fsyntax-only (реальные заголовки JUCE 8) -- 0 ошибок в обоих
проектах; verify_dsp_mode_patch.py -- PASS (оба); MnmCoreTests -- all OK.


## ВЕРСИЯ 1.6.11 (и правило нумерации впредь)

ПРАВИЛО: каждый патч = версия +1, номер виден в двух местах:
1) нижний правый угол главного экрана: "BUILD 1.6.11 / Synth" (или / FX);
2) MENU -> ABOUT / LIMITATIONS: заголовок "Monomachine Nova Synth / BUILD 1.6.11"
   и первая строка с перечнем того, что вошло в сборку.
Также version="1.6.11" прописана в ОБОИХ .jucer (видно в Projucer и в хосте)
и в обоих CMakeLists (project(... VERSION 1.6.11)).

КАК ПРОВЕРИТЬ, ЧТО СОБРАЛИСЬ ИМЕННО НОВЫЕ ИСХОДНИКИ (важно: в прошлый раз
по твоим словам "изменений не видно вообще" -- почти наверняка собиралась
старая папка, т.к. версии в UI до сих пор были "BUILD 1.6.6"/"BUILD 1.6.5"
и различить сборки было невозможно):
1. Открой .jucer ИМЕННО из этой папки 1.6.8 (переименуй её в 1.6.11 по желанию)
   -- в заголовке Projucer должно быть 1.6.11. Save and open -> Studio -> Build.
2. Открой плагин в хосте: в списке плагинов у него версия 1.6.11.
3. На главном экране внизу справа: BUILD 1.6.11 / Synth.
4. MENU -> ABOUT: первая строка "Build 1.6.11 (...)". Если этого нет --
   собралась старая копия исходников.

Что вошло в 1.6.9-1.6.11 суммарно (все пункты твоих запросов):
- LFO-страница снова с ручками (был баг bind(), панель исключалась);
- кнопки LFO1/2/3: ЛКМ-тащить = прямой PAGE/DEST (замена текущего адресата,
  без матрицы), ПКМ-тащить = маршрут в матрицу, клик = открыть страницу LFO;
- кнопки одинаковые 90x24, выбранная залита;
- матрица: колонка ON с сортировкой, свежие маршруты всплывают наверх;
- DSP MODE: плоский прокручиваемый список (FOLDER-метки), всё на английском,
  в конце ENCODING TEST (слово "проверка" сломано намеренно -- это канарейка);
- ПКМ-меню открываются у курсора; ARP -- жирные квадратные степы;
- REPITCH: непрерывная шкала 0..3 (0=OFF, 1=FAST, 2=MED -- дефолт как был,
  3=лента ~2.5с), ниже SMOOTH -- антиклик-доводка 0..127;
- SYNT-режим помнится по каждой машине (mode_synt_m<id>, схема 11).

## ВЕРСИЯ 1.6.12

Номер = 1.6.11 + 1 (правило: каждый патч +1). Обе сборки (Synth и FX) снова
синхронны: все правки зеркалятся в оба проекта.

1. DSP MODE: снова ГРУППИРОВАННЫЕ подменю по папкам (пользователь вернул
   группировку после 1.6.8.1). Раздел -- заголовок, основная реализация,
   группа BACKUP (если включена), строка происхождения раздела, у SYNT в
   заголовке имя машины. Фиксированный ROUTE-подраздел и ENCODING TEST
   (сломанная канарейка "проверка") на месте. Попап открывается у курсора.

2. ARP-страница перестроена:
   - SET больше не пересоздаёт страницу (уходил случайный слайдер и моргание);
   - хитбоксы степов строгие: velocity больше не меняется, когда красишь
     транспозицию (ряд выбирается один раз в mouseDown);
   - ПКМ-драг по степам красит HOLD (инвертируя состояние первого степа);
   - ЛКМ-драг работает во всех списках: CLOCK, MODE, RATE GRID, VELOCITY,
     PAGE (чёрный пиксельный скин DragChoice) и в окошках PAGE LIMIT /
     RANDOM / OCTAVES (DragValueBox: драг = +-1, колесо = +-1, двойной клик =
     дефолт);
   - раскладка: RATE, рядом GATE (слайдер со значением) и OCT; ниже WRAP,
     VELOCITY (ширина списка не больше текста), PAGE LIMIT, RANDOM c кнопками
     PAGE и ALL (тултипы показывают число степей);
   - SYNC на главной = только клок арпеджиатора (не глобальный хост-синк);
     таймер видимости RATE GRID привязан к нему.

3. Матрица: DEST сгруппирован в 7 подменю-страниц (MACHINE/AMP/FILTER/EFFX/
   LFO1..3, по 8 адресатов). Прицел LFO теперь может брать LFO-адресата
   (LFO -> LFO: страницы 1..7).

4. Замки маршрутов и LFO:
   - у каждого маршрута кнопка-замок слева от прицела: ЛКМ = FREE/LOCK,
     ПКМ = меню FREE/LOCK/SOLO; LOCK защищает dest от прицела и перезаписи,
     SOLO приколачивает маршрут к параметру (меняется только руками);
     клик по замку на соло-строке только моргает замком, смены нет;
   - ПКМ по PAGE/DEST на странице LFO открывает панель замков (call-out):
     отметка значения = модуляция его перескакивает (битовая маска,
     sticky), ПКМ по отметке = SOLO значения, клик по значению = задать
     ручке PAGE/DEST это значение, CLEAR LOCKS всё сбрасывает;
   - aftertouch/ручные изменения при SOLO работают, замок сам не снимается.

5. LFO INTL работает по мануалу Monomachine (interlace, "straight from the
   Sidstation"): волна чередуется с нулём, INTL задаёт скорость нулевых
   циклов (1..16 делений), 0 = выкл. Раньше ручка ошибочно трактовалась
   как фазовый сдвиг.

6. BBOX: STRT мягче -- кубическая шкала x^3 с шапкой ~25 мс (1/8 длины
   активной части); деклик адаптивный (2..176 сэмплов от скорости атаки):
   транзиент остаётся, щелчок уходит. Прилегающие ретриг-рестарты тоже
   декликатся.

7. Дилей ping-pong: новый режим MID SAFE (PP MODE в repitch-панели, она
   открывается ПКМ и по DTIM, и по DSND): тапы смешиваются 65/35, середина
   не вычитается. CLASSIC = как в прошивке (моно-вход в левую линию).
   Дефолт -- CLASSIC, звук не меняется.

8. Огибающие: режимы переименованы в "old" (легаси 1.4) и "mnm"
   (бывший REFERENCE FIT, табличные огибающие прошивки) -- в списках AMP
   и ENV; тултип поясняет.

9. Прочее: BACK на главной ровно на месте MATRIX (870,58,140,30); BPM
   шагает целыми, Alt/Shift = десятые, подпись BPM мельче; прицел LFO после
   ЛКМ-перезаписи переключает панель на целевой LFO; вспышка прицела
   сокращена до 6 тиков (без секундного шлейфа); предпросмотр drag-n-drop
   в BBOX: серые строки "+ WILL LOAD".

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах (обновлён:
группированные подменю снова в assert, версия 1.6.12); MnmCoreTests --
all OK (обе сборки); g++ -fsyntax-only PluginEditor/PluginProcessor -- чисто.

## ВЕРСИЯ 1.6.13

Номер = 1.6.12 + 1. Обе сборки синхронны.

1. ВСЕ popup-меню -- чёрный пиксельный скин (DSP MODE, меню машин, списки
   ручек, матрица, замки): свой LookAndFeel (фон чёрный, рамка, пиксельный
   шрифт, текст пунктов ПО ЦЕНТРУ -- списки экономнее и ровнее). "Джус"-стиль
   больше нигде не всплывает, включая ЛКМ по PAGE/DEST страницы LFO и ПКМ-моды
   на крутилках.

2. Название машины (SYNT): стрелки по бокам названия (влево/вправо), клик по
   стрелке = один шаг пресета; декоративная стрелка ">" в конце текста убрана.
   Прокрутка машины драгом ЗАМЕДЛЕНА (18 px/шаг), хитбоксы прежние, без
   задержек.

3. GUI DRAG SPEED (MENU > TEMPO/ARPEGGIATOR/GLOBAL, низ страницы): своя
   плавность прокрутки для КАЖДОГО списка (машина, BPM, ARP RATE, списки
   ручек, окошки значений, каждый MODE-комбо, ARP PLAY/VEL/PAGE). Диапазон
   0.1..6 вокруг дефолта, драг/колесо, двойной клик = дефолт. Специально
   НЕ параметры VST -- лимит автоматизируемых параметров не тратится
   (значения живут только в редакторе).

4. Прицел LFO: подсветка ТОЛЬКО текущей ручки и снимается МГНОВЕННО (шлейфа
   на пройденных ручках больше нет). Удержание над кнопкой ДРУГОГО LFO одну
   секунду переключает панель на его страницу (удобно для LFO->LFO). Замочки
   в панели замков можно РИСОВАТЬ зажатием (первый ряд тумблится, проведённые
   получают то же состояние).

5. ARP:
   - ОДНА unified кнопка RATE (grid или ms -- по галочке HOST TEMPO);
     SYNC и TIME рядом переключают режим простым кликом, никаких списков;
   - HOST TEMPO -- только режим RATE арпа (FREE/BPM), глобальный host sync
     всего плагина она больше НЕ трогает;
   - RANDOM-надпись удалена: кнопки PAGE и ALL, под ними PITCH и VEL
     (рандом транспоуза/velocity страницы); ALL = 128 шагов одним батчем
     (suspendProcessing, без жестов хоста) -- лагов нет;
   - строгие хитбоксы и ручки: TR -- от середины (биполярно), VEL --
     от НИЗА (однополярно, растёт вверх); свой ряд HOLD-кнопок (рисуется
     зажатием ЛКМ), ПКМ-драг = СТИРАНИЕ velocity+transpose; шапка холда над
     TR+VEL осталась; SET больше не мигает посторонним слайдером (значение 5
     на микросекунду) -- слайдеры TIME/GRID заменены единой кнопкой;
   - BPM на главном экране: порядок слева направо [BPM SYNC] [значение BPM],
     буквы BPM чуть крупнее (как SYNC) и почти вплотную к цифрам.

6. BBOX: в списке файлов PLAY-колонка рядом с номером (прослушать слот),
   скроллбар списка -- белый вместо синего.

7. Дилей: у DSND СВОЁ окно доп. настроек (PP MODE: CLASSIC/MID SAFE),
   окно DTIM снова только REPITCH+SMOOTH, как в предыдущей версии.

8. Матрица: DEST-колонка -- текст параметров по центру (видно, какая строка
   короче); фикс краша Ableton при клике по шапке последней колонки:
   сортировка пишет только изменившиеся значения под suspendProcessing
   (шторм нотификаций убран). ПКМ по ручке HOLD (страница AMP) открывает
   огибающую, как у остальных ручек.

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах (секция 1.6.13);
MnmCoreTests -- all OK (обе сборки); g++ -fsyntax-only Editor/Processor --
чисто в обеих сборках.

## ВЕРСИЯ 1.6.14

Номер = 1.6.13 + 1. Обе сборки синхронны.

1. СКИН МЕНЮ ДОЧИЩЕН: причина "джус"-меню найдена -- в JUCE 8 попап берёт
   LookAndFeel только из menu.setLookAndFeel, таргет-компонент на это не
   влияет. Все верхнеуровневые меню теперь явно получают тему (машины,
   DSP MODE, MENU, матрица, замки, списки ручек). Пиксельный шрифт, чёрный
   фон, белые рамки.

2. МАШИНЫ: стрелки -- правильная пара "< >" СПРАВА от названия (там, где была
   декоративная), клик = один шаг пресета; хитбоксы стрелок не на тексте.
   Смена страниц LFO мгновенная: Cell::bind больше не перестраивает те же
   ячейки (раньше каждое переключение пересоздавало ~50 attachment'ов).
   Прицел: удержание над кнопкой ДРУГОГО LFO одну секунду переключает панель
   на его страницу -- работает и без движения мыши (таймер Surface).

3. ЗАМКИ: рисуются всегда полным белым (тусклые были не видны) и в матрице,
   и в панели LFO; при наведении на замок матрицы -- подсказка режима
   (FREE/LOCK/SOLO + "RMB: modes"); меню замка открывается У КУРСОРА.

4. AMP old DSP УДАЛЁН (бесполезный выбор): DSP всегда mnm (kernel ADSR),
   комбо в шапке убрано, страница огибающей всегда mnm (кривые активны),
   параметр amp_mode оставлен только для совместимости старых состояний.
   В DSP-меню раздел AMP теперь только mnm.

5. ARP:
   - переключение страниц = квадратное окошко PAGE (значение 1..16, дефолт 1)
     рядом с PAGE LIMIT;
   - STEP RND: при доигрывании каждый новый цикл страницы генерирует новый
     случайный паттерн (transpose+velocity+hold); движок отдаёт эхо шага
     (arpStepEcho), переключатель -- UI-only;
   - рисование степов доходит до краёв поля (кламп координат) -- пол и
     потолок velocity и стирание теперь достижимы; ряд HOLD выше (26 px);
   - колесо страницу ARP больше не крутит; скролл -- зажать ЛКМ на пустом
     месте и тащить;
   - ПКМ по кнопке ARP на главной открывает страницу ARP;
   - лишние надписи со страницы убраны, все подписи/кнопки -- белые.

6. GUI DRAG SPEED теперь РЕАЛЬНО виден: сетка множителей прокрутки внизу
   страницы ARP (с той поры страница TEMPO открывает ARP, старая секция
   была в неиспользуемом классе). Только UI -- лимит автоматизации VST
   не тратится.

7. ПОРТАМЕНТО: у ручки PORT (AMP) своё окно по ПКМ: TIME (сама ручка) +
   SPEED (новый параметр porta_speed). Глиссандо быстрее и явное:
   время = (PORT/127)^1.5 * 0.8 с, масштабируется SPEED (0.1x..3x).

8. FM: новый режим "fma" (SYNT, у КАЖДОЙ машины свой, в mode-комбо и в
   DSP-меню): ядро FM+ прошивки + ПРОПЕРНЫЙ DIST для FM-машин -- мягкая
   tanh-сатурация вместо жёсткого фолда (clamp -1..1 на FM-сигнале
   складывал волну и звучал как лишнее соотношение осциляторов).
   Существующие mnm/old не тронуты.

9. Прочее: ESC выходит из матрицы/арпа/оверлеев (фокус забирается по клику);
   окна доп. настроек (DTIM/DSND/замки/PORTA) больше не забирают клавиатуру
   -- QWERTY-ноты в хосте играют при открытом окне; SYNC на главной без
   слова "BPM", придвинута к значению; скроллбар/подсказки белые.

Примечание: FRQ до 0 в FM не трогал (нужно больше информации по оригиналу).

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах (секция 1.6.14);
MnmCoreTests -- all OK; g++ -fsyntax-only Editor/Processor -- чисто.

## ВЕРСИЯ 1.6.15

Номер = 1.6.14 + 1. Обе сборки синхронны.

1. СЕТКА GUI DRAG SPEED исправлена (нарушалась случайными задевами):
   - драг множителя начинается после мёртвой зоны 6 px -- случайный задев
     окошка при драг-скролле страницы больше не швыряет значение в крайние
     0.10/6.00;
   - наклон мягкий: 48 px = x1 (было 16), с Shift -- x0.1 (тонкая подстройка);
   - кнопка RESET у заголовка сетки возвращает все множители к дефолтам
     одним кликом (двойной клик по окошку по-прежнему сбрасывает его);
   - мёртвый пункт AMP MODE удалён (само комбо убрано в 1.6.14).

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах; MnmCoreTests --
all OK; синтаксис Editor -- чисто.

## ВЕРСИЯ 1.6.16

Номер = 1.6.15 + 1. Обе сборки синхронны.

1. ARP: скролл страницы снова КОЛЁСИКОМ (как было), ЛКМ-драг-скролл убран.
2. ARP-дорожка: ПКМ стирает только тот ряд, на котором находишься
   (TR -> transpose, VEL -> velocity, HOLD -> hold) -- больше не сносит
   velocity и transpose разом; при наведении на степ -- подсказка управления
   (включая инвертированное рисование HOLD: начал с включённого -- красишь
   стирание, начал с выключенного -- красишь включение).
3. Батч-рандом степов (PAGE/ALL/PITCH/VEL/STEP RND) больше НЕ уведомляет хост
   о каждом параметре -- Ableton перестаёт считать это автоматизацией и не
   лагает (значения меняются, автоматизации не создаётся).
4. HOST TEMPO -- единственный переключатель режима RATE: дублирующие кнопки
   SYNC/TIME удалены. GATE -- просто квадрат со значением (слайдер убран).
5. Попап-меню: пиксельный шрифт крупнее (до 21 px, длинные строки усаживаются
   автоматически), ширина по пиксельным метрикам (не уезжает за экран),
   весь текст белым.
6. Панель замков LFO: правильные имена DEST -- на странице PTCH это имена
   ручек текущей машины (были голые "0..7"), на LFO-страницах приставка
   "LFO1..3" (было непонятное PAGE/DEST).

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах; MnmCoreTests --
all OK; синтаксис Editor -- чисто.

## ВЕРСИЯ 1.6.17

Номер = 1.6.16 + 1. Обе сборки синхронны.

1. ФИКС ПРИЦЕЛА LFO -> LFO: отпустил ЛКМ-прицел (кнопка LFO1) над кнопкой
   ДРУГОГО LFO (LFO2) -- этот LFO становится целью (PAGE LFO1 указывает на
   страницу LFO2), и редактор попадает на страницу LFO2, будто нажали её
   кнопку: сразу видно его ручки и можно донастроить конкретную ручку-цель.
   Раньше отпускание над кнопкой не делало ничего.
2. Предпросмотр удержанием (1 с над кнопкой другого LFO, не отпуская ЛКМ)
   больше не перезапускается циклично раз в секунду; срабатывает один раз,
   пока мышь на кнопке; повторное срабатывание -- после ухода/возврата.
3. Система перетягивания будет переделываться пользователем -- изменений
   семантики больше не вносилось.

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах; MnmCoreTests --
all OK; синтаксис Editor -- чисто.

## ВЕРСИЯ 1.6.20

1. **Стрелки машин прижаты к правому краю** окна имени генератора -- больше не скачут вместе с текстом (текст при длинном имени сжимается, стрелки на месте).
2. **Строка ENCODING TEST ("проверка") только в дебаг-меню кнопки DSP MODE** -- из ПКМ-меню всех остальных параметров убрана.
3. **Подсказки на замочках матрицы убраны** (сливались с другими элементами).
4. **Матрица: кнопка CLEAR** -- стирает ВСЕ модуляции разом (все ON в OFF).
5. **SYNC главной панели = общий темп-синк синта (host_sync)**: даёт всему синту темп из хоста. Галочка HOST TEMPO на странице ARP (режим RATE арпа, arp_sync) теперь ПОЛНОСТЬЮ независима: фри-режим в мс и его настройки не касаются общего BPM и галочки SYNC. SYNC подписана только "SYNC" (без букв BPM), вплотную к цифрам.
6. **Список машин: тип пишется СЛЕВА от имени** как одно слово (FM+STAT); на ВЫДЕЛЕННОЙ строке тип тоже показывается (например FM+PAR), не только на первой строке категории.
7. **Кнопки LFO:** выбранная страница = БЕЛАЯ кнопка; кнопка, которую тянут, чуть серее; режим модуляции включается ТОЛЬКО началом перетягивания (порог 6 px), простой клик = просто выбор страницы; мигание страницы-цели MUCH заметнее; прицелом можно вернуться к исходному LFO -- он мигнёт и пустит обратно.
8. **SET на внешней панели заменён на GATE** (arp_length 1..127) -- ритм правится с главной панели; в ARP по-прежнему ПКМ по кнопке ARP.
9. **ПКМ-стирание в ARP-ленте больше не трогает чужие ряды:** ряд фиксируется в момент нажатия (раньше драг "прочёсывал" TR+VEL+HOLD по пути).
10. **HOLD рисуется как velocity:** верхняя половина ячейки = поставить, нижняя = стереть (значение бинарное, хитбокс и логика -- как у VEL).

## ВЕРСИЯ 1.6.19

1. **Прицел LFO без задержки.** Наведение прицела (ЛКМ или ПКМ) на кнопку другого LFO переключает страницу МГНОВЕННО (0 с вместо 1 с); страница, которую модулируем, мигает СЕРЫМ на своей кнопке (нажатая -- белым, как раньше). Серый сбрасывается при отпускании/выходе из прицела.
2. **Меню машин -- две невидимые колонки.** Слева генераторы, справа напротив ПЕРВОГО генератора категории -- её тип; список вдвое ниже. Кастомный пикер в CallOutBox (черный пиксель-скин, инверсия по ховеру).
3. **Режим огибающей old/mnm вернулся** на страницу AMP (комбо + attachment `amp_mode`): выбор движка огибающей больше НЕ привязан к режиму DSP-секции AMP; раздел AMP скрыт из меню DSP MODE.
4. **MSEG OUT назначается как прицел LFO:** тянешь OUTPUT на ручку -- ячейки подсвечиваются, отпускание = маршрут матрицы на эту цель (`addMsegRoute`). Старый DragAndDrop удалён.
5. **Страница MSEG пересоздана по референс-фото:** заголовок LFO CURVE EDITOR + счётчик точек; ряд CUSTOM/генераторов + SHIFT в градусах; сетка с точками (двойной клик добавить/удалить, драг -- двигать, ПКМ -- удалить, Alt -- без SNAP); правая колонка DRAW/FLAT/RISE/FALL/TRI/SINE/SQUARE + clear; ряд X/Y/SNAP/UNDO/REDO/INVERT/FLIP X/COPY/PASTE; ROUTING со знаковой величиной (не зависят от PAGE/DEST/DPTH), крестик = убрать маршрут; LOOP/SYNC/rate внизу.
6. **DSP MODE меню -- обычный шрифт JUCE** (пиксельный скин больше не применяется к этому меню).
7. **Окно About -- чёрный фон**, белый текст, белая рамка.
8. **GUI DRAG SPEED вынесен со страницы ARP в отдельное окно MENU > GUI OPTIONS** (id 9); страница ARP стала ниже (1160x760).
9. **Баг: ПКМ-стирание в ряду HOLD** больше не трогает velocity/transpose -- стирание разбито на явные полосы рядов (TR / VEL / HOLD).
10. **Линия игры под рядом HOLD** (без подписи) показывает текущий играющий шаг (alpha 0.5, заливка ARP ECHO-цветом, номер шага под линией).

## ВЕРСИЯ 1.6.18

Номер = 1.6.17 + 1. Обе сборки синхронны.

1. ПРИЦЕЛ LFO -> LFO БЕЗ ОТПУСКАНИЯ КНОПКИ: держишь кнопку LFO1 (ЛКМ ИЛИ
   ПКМ) над кнопкой LFO2 (или LFO3) одну секунду -- панель ПЕРЕКЛЮЧАЕТСЯ на
   страницу этого LFO (переключение постоянное). Дальше ведёшь на ручку той
   страницы и отпускаешь:
   - ЛКМ = прямой PAGE/DEST этого LFO (LFO1 модулирует ручку LFO2 напрямую);
   - ПКМ = маршрут в матрицу LFO1 -> ручка LFO2.
   Работает для любой пары/тройки LFO подряд (с LFO2 можно переехать на LFO3).
   Отпускание над кнопкой (без ручки) тоже выбирает этот LFO целью (1.6.17).

Проверки: verify_dsp_mode_patch.py -- PASS в обоих проектах; MnmCoreTests --
all OK; синтаксис Editor -- чисто.

## ВЕРСИЯ 1.6.21

### Кнопки-источники модуляции: НОМЕР + ГНЕЗДО с проводом (возврат пропавших кнопок)
В 1.6.20 кнопки выбора страниц LFO и кнопка MSEG пропали целиком -- причина:
однострочный метод, у которого хвост с закрывающей скобкой оказался «приклеен»
к комментарии `//` (комментарий проглотил `}` -- парсер остался внутри класса
Surface). Правило впредь: НИКОГДА не писать `//`-комментарий, за которым на той
же строке есть код.
- Селектор LFO = маленький квадрат с НОМЕРОМ (1/2/3) + справа ГНЕЗДО (джек).
  Клик по номеру = открыть эту страницу LFO. Тянешь ПРОВОД из гнезда:
  - ЛКМ = прямой PAGE/DEST этого LFO, ПКМ = маршрут в матрицу;
  - провод тянется за мышью (белая дуга с точкой на конце), страница-цель
    мигает серым, наведение на чужой номер -- мгновенный просмотр его страницы;
  - отпускание над ручкой = прицел, источник на время прицела чуть серее.
- MSEG = ТРИ таких же кнопки-гнезда (1/2/3): номер открывает редактор кривой
  на этой странице (mseg_* / mseg2_* / mseg3_*), провод = маршрут от кривой.
  У каждой MSEG свои точки (ValueTree MSEG/MSEG2/MSEG3), в DSP идут все три
  (setMsegOutputs), источники матрицы MSEG=10, MSEG2=16, MSEG3=17.
- Провод рисует CableLayer (компонент без мыши, всегда сверху); драг стартует
  только с гнезда и после 6 px (клик по номеру -- просто выбор).

### MSEG-редактор: STEPS, сетка, изгиб линии
- STEPS = рисование «ступеньками-квадратами» (сегмент держит значение левого
  края вместо рампы).
- Вместо SNAP -- выбор СЕТКИ: OFF / 1/8 / 1/16 / 1/32 (Alt -- мимо сетки).
- Наведение на прямой сегмент -- линия жирнее; драг = экспо-изгиб (+/-,
  k в пределах +/-4); ПКМ по сегменту = сброс наклона (синус из 2-3 точек).

### ARP: случайности
- Кнопки RANDOM PAGE/PITCH/VEL/ALL снова работают.
- STEP RND (тумблер) = регенерация паттерна ТЕКУЩЕЙ страницы один раз за
  проход (новый проход = конец паттерна); джиттера позиции больше нет.
- PAGE RND = ОТДЕЛЬНЫЙ тумблер-параметр arp_step_random: каждый проход играет
  случайную страницу в пределах лимита, шаги по порядку.

### Матрица: CLEAR и замки
- CLEAR ЛКМ = стереть ВСЁ в дефолт (on/src/dest/depth/mode/lock, включая
  замки); ПКМ = меню выборочного стирания (всё / on / depth / замки / mode).
- Замки красятся ПРОВЕДЕНИЕМ в матрице (ЛКМ -- toggle LOCK/FREE, ПКМ -- SOLO;
  ПКМ-клик без движения -- меню FREE/LOCK/SOLO у курсора) и в панелях замков
  LFO (первый ряд тумблится, проведённые ряды -- тем же состоянием).

### Мелочи 1.6.21
- Пикер машин: ИМЯ больше не сдвигается никогда (колонка имени на месте);
  категория -- в правой колонке напротив первой машины категории, на
  подсвеченной строке подъезжает вплотную к имени.
- ПКМ по GATE на передней панели = страница ARP; драг значений на передней
  панели -- только ЛКМ (ПКМ по ARP/GATE больше не крутит значения).
- Списки DEST/SRC матрицы и все попапы -- выравнивание ВЛЕВО (строки ровные).
- Комбо режима усилителя (MODE AMP: old/mnm) вернулось в шапку передней панели;
  в меню DSP MODE -- обычный шрифт, фон ЧЁРНЫЙ (Fade-затемнение JUCE убрано).
- Возвращён выпадающий список выбора страницы в STEP-редакторе.
- Строка «проверка» (ENCODING TEST) -- по-прежнему только в дебаг-меню DSP MODE.

Проверки: verify_dsp_mode_patch.py -- секция 23 (1.6.21), PASS; MnmCoreTests --
all OK; синтаксис обоих TU -- 0 ошибок.

## ВЕРСИЯ 1.6.22

### КРАШ: кнопка CLEAR матрицы (ЛКМ)
Причина: лямбда `onClick=[this,&wipeField]` держала ССЫЛКУ на локальную лямбду
конструктора MatrixPage -- после выхода из ctor это висячая ссылка (ПКМ-меню
работало, потому что там захват по значению). Исправлено: захват по значению.
Мусор состояния после такого краша мог оставлять в DEPTH значения вроде «+2 %»:
ПКМ по CLEAR -> ONLY DEPTH VALUES возвращает честные нули.

### Матрица
- Галочки ON красятся ПРОВЕДЕНИЕМ: ЛКМ по галочке задаёт кисть (вкл/выкл --
  противоположно текущему), протаскивание красит остальные; ПКМ -- кисть
  стирания.
- SRC/DEST/DEPTH/MODE реагируют ТОЛЬКО на ЛКМ: ПКМ больше не крутит списки
  (ПКМ зарезервирован за альт-режимами и списками).
- Дефолт цели маршрута (r*_dest) -- 0 (был 16).

### Провода модуляции (постоянные)
- Проложенные провода ОСТАЮТСЯ на панели приглушённо-серыми: маршруты матрицы
  с источниками LFO1..3/MSEG1..3 + прямые прицелы LFO (PAGE/DEST).
- ПКМ по кнопке LFO 1/2/3 или MSEG 1/2/3 -- показать ТОЛЬКО её провода
  (ярким белым); повторный ПКМ -- обратно все.
- Нагрузка копеечная: пара дуг-квадратиков, перерисовка ТОЛЬКО при изменении
  маршрутов (сигнатура), не каждый кадр. Цели -- живые ячейки панели; провода
  к страницам LFO2/LFO3 появляются, когда та страница выбрана.

### MSEG
- ПКМ по STEPS = ПРИМЕНИТЬ ступеньки к графику (точки-уголки пишутся в кривую,
  режим отображения выключается); повторный ПКМ возвращает прошлую гладкую
  форму (плюс UNDO всегда доступен).
- Кнопка DRAW снова работает (свободное рисование ЛКМ), добавлена DRAW STEP:
  квадратные ступени по колонкам 1/16 БЕЗ включения STEPS-режима. Активный
  режим подсвечен; повторный клик -- обратно к точкам.
- Списки SHAPE и GRID крутятся КОЛЕСОМ мыши (WheelCombo; раньше колесо
  уходило в прокрутку страницы).

### ARP
- По умолчанию режим STEPS, STEPS/STEP CHORD переставлены на первые места
  списка (движку соответствует явная карта порядков).
- Ряд случайностей ОДИН, слева направо: PAGE ALL PITCH VEL HOLD (добавлен
  HOLD-рандом). STEP RND и PAGE RND -- галки под ним.
- PAGE LIMIT/случайности живут в режимах STEP: с новым дефолтом STEPS они
  работают сразу; при выключенном PAGE RND движок играет выбранную страницу
  (без затяжного хвоста с нулевой).
- RATE по умолчанию 1/16 (сетка, HOST TEMPO = SYNC).

### Панель и списки
- ПКМ по GATE или по кнопке ARP закрывает страницу ARP, если она открыта
  (тумблер, а не только «открыть»).
- GATE с внешней панели: полный ход 0..127 за ~170 px (чувствительность
  драга x6).
- Список машин возвращён к виду 1.6.20: тип генератора ВЛЕВО от имени одним
  словом (FM+STAT SAW) -- на первой машине категории и на подсвеченной
  строке; подсветка строки ТУСКЛО-СЕРАЯ (белая заливка делала текст
  нечитаемым) + белый маркер слева.
- Стрелки смены пресета раздвинуты, хитбоксы по 24 px без перекрытия.

Проверки: verify_dsp_mode_patch.py -- секция 24 (1.6.22), PASS; MnmCoreTests --
all OK; синтаксис обоих TU в обоих проектах -- 0 ошибок.

## ВЕРСИЯ 1.6.23

### Кабели модуляции: по умолчанию ВЫКЛЮЧЕНЫ
- Постоянные провода больше не рисуются сами; включаются тумблером
  MENU > SHOW PATCH CORDS (experimental) -- чтобы проверить без риска.
- Даже включённые: гаснут под открытыми страницами (MATRIX/MSEG/ARP/SAMPLES)
  и во время прицела (фантомный второй кабель при перетаскивании исчез).
- Живой кабель прицела (при перетаскивании от гнезда) работает как в 1.6.21.
- Идея «оторвать конец провода ПКМ» -- в запас (см. SW-хранилище идей).

### Пикер машин: возврат к 1.6.20
Две невидимые колонки (имена слева, тип генератора справа напротив первой
машины категории и на подсвеченной строке) + БЕЛОЕ выделение строки -- то
состояние, которое было «норм». Сдвиги имени/категории отменены.

### Матрица
- DEPTH-список упорядочен: -100% ... 0% (в середине) ... +100% (был
  случайный порядок). Список открывается ТОЛЬКО по ПКМ; ЛКМ -- драг;
  двойной ЛКМ-клик -- сброс в 0 (то же для полярности и остальных ячеек).
- CLEAR: исправлена ДВОЙНАЯ конвертация (getDefaultValue у float-параметров
  уже нормализован -- отсюда мусорные «2%» после очистки); теперь пишется
  ровно ноль, без нотификации хоста (не создаёт точки автоматизации).
- Краш при кручении матрицы: найден ЧЕТВЁРТЫЙ случай «приклеенного
  комментария» -- комментарий в середине showMenuAsync-лямбды проглотил
  `else ... });` (класс не закрывался). Строка перестроена, комментарий
  перенесён в конец. Правило -- комментарий только в конце полной строки.

### ARP
- HOLD: по мануалу Monomachine HOLD избавляет от NOTE OFF -- нота звучит
  до СЛЕДУЮЩЕГО шага (раньше был хак «гейт x1.75»).
- Рандомайзеры шагов/страниц: убран suspendProcessing (подвисал интерфейс);
  значения пишутся И в параметр, И прямо в атомы -- движок перечитывает
  шаги из атомов каждый блок, лента рисует из атомов.
- PAGE RND: случайная страница из ВСЕХ 16 за проход (лимит страниц больше
  не сужает выбор до одной -- из-за этого «ничего не менялось»).

### Прочее
- Текстовый файл-мусор рядом с проектом удалён (канарейка -- это только
  строка ENCODING TEST в дебаг-меню DSP MODE, она на месте).
- Папка внутри архива переименована по версии (Monomachine-Nova-1.6.23).

Проверки: verify_dsp_mode_patch.py -- секция 25 (1.6.23), PASS; MnmCoreTests
-- all OK; синтаксис обоих TU в обоих проектах -- 0 ошибок.

## ВЕРСИЯ 1.6.24

### Патч-корды: фича удалена
- Экспериментальные постоянные провода (1.6.23) УДАЛЕНЫ полностью: они
  двоились с живым шнуром прицела и создавали фантомные дуги. Остался
  только шнур, который рисуется во время перетаскивания.
- Удалено: CableLayer с массивом Cord, cellForTarget/rebuildCables/
  cablesFocus/lastCableSig, ПКМ-подсветка проводов у кнопок LFO/MSEG,
  пункт MENU «SHOW PATCH CORDS (experimental)», связанные тултипы.
- Отмена прицела: если шнур отпущен НЕ на модуляторе -- попытка
  модуляции отменяется, PAGE/DEST откатываются к значениям до прицела
  (снимок в beginModDrag, восстановление в endModDrag с хост-жестами).
- В режиме прицела ячейки-цели показывают ТОЛЬКО мигающую рамку --
  содержимое скрыто (включая choice-ячейки вроде LFO MULT, как у MSEG);
  курсор над ручками -- обычный (указательный палец вводил в заблуждение
  и блокировал ПКМ).

### Машины и пресеты
- Стрелки пресетов: своя рамка, вся правая полоса забронирована под
  стрелки (перекрытие с вызовом списка машин исключено); стрелки
  реагируют только на горизонталь.
- Колесо мыши срабатывает КАЖДЫЙ раз (накопление дельты с порогом 0.12);
  вертикальный драг по имени листает машины ВВЕРХ/ВНИЗ.
- Пикер: список SYNTH первым (порядок отображения, id машин не менялись);
  имена в первом столбце, тип -- у правого края, ширина окна подобрана
  под самый длинный текст «тип+имя» (434 px).

### Матрица модуляции
- Списки SOURCE/DESTINATION вернулись -- открываются КЛИКОМ (ПКМ их
  больше не трогает); DEST по-прежнему открывается и по прицелу.
- DEPTH: список значений по ПКМ; двойной клик ЛКМ сбрасывает в 0
  (сброс работает только по ЛКМ, ПКМ никогда не сбрасывает).
- Значение DEPTH по центру ячейки; визуал глубины -- лёгкая серая
  заливка + ЯРКАЯ БЕЛАЯ обводка по границе заполнения (в сторону знака).
- Полярность -- простой тумблер UNI<->BI (клик переключает). Семантика
  по Serum: Unipolar = модуляция в одну сторону 0..±depth, Bipolar =
  по центру −depth..+depth. Раньше Bipolar делал source*2-1, что
  дважды расширяло уже биполярный источник.
- НОВАЯ КОЛОНКА AUX SOURCE: вторичный источник масштабирует Amount
  (OFF по умолчанию; KEY/VEL/MACRO/LFO/MSEG/шаги арпа и т.д.). Параметры
  r{0..15}_aux (schema-совместимо, id не сдвигались), CLEAR стирает и aux.
- MOD source list снова открывается (раньше не появлялся из-за ПКМ-гейта).

### ARP
- RETRIG SYNC (галка): первый арп после тишины стартует ровно на
  следующей ДОЛЕ хост-сетки -- записанные ноты попадают в сетку DAW.
- Velocity mode: по умолчанию STEP; STEP|STEP+KEY|STEP+HOLD -- первыми
  в списке (порядок отображения; id не менялись).
- GATE проверен численно: нота выключается ровно на GATE/128 длительности
  шага (движок точен); хвост делает ампл-энвелоп (release), поэтому при
  длинных DEC/REL эффект гейта «не слышен» -- это семантика гейта
  (Sylenth/Sound On Sound), а не баг. Проводка arp_length->noteLength
  и синк арп-настроек при закрытом редакторе перепроверены.

### Сохранение состояния (VST3)
- ГЛАВНЫЙ ФИКС: getState писал СТАРЫЙ тег schema=8, из-за чего при
  КАЖДОЙ загрузке пресета в DAW заново запускались миграции (DSP-remap)
  и могли портить настройки. Теперь пишется актуальная схема.
- Исправлена потеря MSEG1: setState удалял ребёнка "MSEG" ДО чтения --
  loadMseg читал уже изменённое состояние и MSEG1 не восстанавливался
  (MSEG2/3 восстанавливались). Дети захватываются до удаления.
- Полный аудит: arp-, matrix- и mseg-параметры синхронизируются при
  закрытом редакторе (editor==nullptr-пути присутствуют во всех
  обработчиках).

### Прочее
- Шкала LEV: незаполненная часть -- полая белая рамка (виден предел).
- Двойной клик по кнопке ARP на передней панели открывает страницу ARP.
- PORTA SPEED доступна через ПКМ по ячейке SPEED на панели PORTA
  (давно просиженное; ПКМ над ручками снова работает).

Проверки: verify_dsp_mode_patch.py -- секция 26 (1.6.24), PASS (Synth и
FX); MnmCoreTests -- all OK; синтаксис обоих TU в обоих проектах --
0 ошибок.

## ВЕРСИЯ 1.6.25

### Критический фикс (регресс 1.6.24)
- Строка settings.stepPage/stepPageLimit/stepRandom была ПРОГЛОЧЕНА
  комментарием при правке 1.6.24: PAGE RND «не работает», выбор страницы
  в ARP игнорировался движком. Строка восстановлена.
- sortModRoutes: чтение 6 полей из массива на 5 имений (выход за границы)
  -- в массив добавлен "aux".

### Матрица
- DEPTH: серая заливка убрана -- остался контур ячейки (видны границы) и
  белая полоса заполнения; значение по центру (как в 1.6.24).
- Рамка ВСЕЙ матрицы -- белая (была серой 35%).
- Списки: SOURCE/DESTINATION/MODE/AUX открываются КЛИКОМ и листаются
  ЛКМ-драгом (конфликтов нет: список только если не тянули; ПКМ-драг
  ничего не делает и не открывает список). DEPTH: драг ЛКМ, список ПКМ,
  двойной ЛКМ -- сброс (ПКМ никогда не сбрасывает).
- Полярность -- список UNIPOLAR/BIPOLAR по клику (тумблер убран).
- Хитбокс замка расширен в сторону списка DEST (меню DEST больше не
  открывается при попытке замка).
- Списки значений: ПОЛНАЯ БЕЛАЯ рамка (раньше пункты затирали боковые
  кромки -- видны были только верх/низ).
- ИСТОЧНИКИ: enum ModSource без дыры (MSEG2=11, MSEG3=12, шаги арпа 13..17,
  RANDOM=17); в списках 18 источников (было 3 RANDOM, не было MSEG2/3);
  AUX SOURCE -- тот же порядок, что SOURCE (+OFF); addRouteFromSource
  принимает 0..17 (MSEG2/3 больше не зажимаются в 15); MsegPage читает
  новые номера.
- Полярность в движке -- семантика Serum: источник нормализуется к 0..1
  (uniOf), UNI = 0..+depth (одна сторона), BI = -depth..+depth (по центру)
  независимо от родного диапазона источника (было двойное расширение).

### Прицел (возврат к прежней подсветке)
- Мигание рамок и скрытие содержимого УБРАНЫ: видны все ручки и значения,
  белая рамка ТОЛЬКО на текущей цели прицела (как 1.6.13), вспышка при
  отпускании сохранена.
- ПКМ-фолбэк (меню ручки) перенесён ПОСЛЕ спец-обработчиков: ПКМ по
  SAMPLES снова открывает сэмплы, а не список DSP.

### Машины/пресеты
- Стрелки: каждая -- свой хитбокс и своя подсветка (белеет только нажатая);
  драг, начатый со стрелки, не листает пресеты и не открывает список.
- Список машин: колонки ПЕРЕСТАВЛЕНЫ -- тип (полное имя машины) слева,
  семейство справа (на первой строке семейства и на подсвеченной);
  SYNTH-машины первым списком, FX -- в конце; ширина по самому длинному
  типу; белая рамка списка; открывается у ИМЕНИ, а не сбоку от стрелок.

### ARP
- RATE: список значений по ПКМ (сетка или MS по режиму HOST TEMPO).
- PAGE и PAGE LIMIT -- вплотную, двойной клик вводит значение с
  клавиатуры, тултипы (старт страниц степов / лимит).
- Рандомайзеры: ПКМ по PAGE/PITCH/VEL/HOLD -- на ВСЕХ страницах,
  ЛКМ -- только текущая.
- RETRIG SYNC удалена (делала не то).

### Передняя панель
- GATE: рамка на тех же пикселях, что у кнопок (не выше и не ниже);
  галочки по центру высоты (ровно с ARP/GATE).
- Все MODE-окна шириной 91 px (последняя ручка страницы), текст по центру;
  AMP MODE подвинут правее (x=716).

### Прочее
- Панель замков LFO: имена DEST живые -- свап страниц больше не оставляет
  панель «на странице позади» (пересчёт каждый кадр от текущей страницы).

Проверки: verify_dsp_mode_patch.py -- секция 27 (1.6.25), PASS (Synth и
FX); MnmCoreTests -- all OK; синтаксис обоих TU в обоих проектах --
0 ошибок.

## ВЕРСИЯ 1.6.26

### Передняя панель
- MODE-окна выровнены по КОЛОНКАМ крутилок: каждое стоит ровно над
  ПОСЛЕДНЕЙ крутилкой своей страницы (SYNT x=363, AMP x=762, FILT x=363);
  у страницы с двумя модами (EFFX) второй мод стоит над ПРЕДПОСЛЕДНЕЙ
  крутилкой (DLY x=666, DIST x=762). Ширина 91 сохранена.
- Полоса стрелок пресетов получила СВОЙ чёрный фон: нажатие имени машины
  (открытие списка) больше не заливает стрелки белым.

### Пикер машин
- Ровно первый чёрный интерфейс с переставленными колонками: ТИП слева --
  ОДИН раз на категорию (и на подсвеченной строке), КОРОТКИЕ имена машин
  справа -- каждая строка (раньше тип повторялся на каждой строке, а в
  правой колонке были полные имена-дубликаты).

Проверки: verify_dsp_mode_patch.py -- секция 28 (1.6.26), PASS (Synth и
FX); MnmCoreTests -- all OK; синтаксис обоих TU в обоих проектах --
0 ошибок.

## ВЕРСИЯ 1.6.27

### Прицел/мапинг
- Подсветка цели прицела снова БЕЛАЯ: в 1.6.25/26 использовалась константа
  knockout, которая в этой теме ЧЁРНАЯ -- рамка получалась невидимой/серой.
- ПОСТ-АНИМАЦИЯ ПОДТВЕРЖДЕНИЯ УБРАНА по умолчанию: после отпускания шнура
  на ручке ячейка больше не вспыхивает.
- MENU > GUI OPTIONS: новая галка MAP FLASH -- если включена, подтверждение
  мапинга мигает бледно-белым ОДИН раз (0.3 с). Выключена по умолчанию.
- Белая рамка при наведении теперь работает и при выборе цели из матрицы
  (крестик в строке маршрута): подсвечивается именно текущая ячейка.
- PixelButton научился отражать toggle-состояние (галка MAP FLASH залита
  белым, как нажатые кнопки).

Проверки: verify_dsp_mode_patch.py -- секция 29 (1.6.27), PASS (Synth и
FX); MnmCoreTests -- all OK; синтаксис обоих TU в обоих проектах --
0 ошибок.

## ВЕРСИЯ 1.6.28

### Прицел/мапинг
- В режиме прицела ЛКМ больше НЕ крутит ручку: ЛКМ -- мапинг, Alt+ЛКМ --
  кручение (мягкое, ~300 px на весь ход). Ручка на время прицела закрыта
  для мыши (случайные подравкивания значений исключены).
- Выбор цели из матрицы получил ту же белую рамку-подсветку при наведении.

### Матрица
- DEPTH: возвращена СЕРАЯ полоса заполнения (контур ячейки и белая кромка
  сохранены; общий серый фон ячейки остался убранным).
- Списки SOURCE/DESTINATION -- ПЛОСКИЕ, с заголовками папок (задержка
  появления подменю устранена). SOURCE: RANDOM первым, затем MACRO и
  папки MIDI / LFO / MSEG / ARP. DEST: MACHINE/AMP/FILTER/EFFX/LFO1-3.
- DEST: если маршрут выключен и назначение не задано -- пишутся прочерки.

### Машины/пикер
- Пикер: ширина колонок под ПОЛНЫЙ кегль (SWAVE больше не мельче других
  категорий), ширина окна -- по тексту (пустое место справа убрано).
- Драг имени машины -- мёртвая зона 3 px: драг НИКОГДА не открывает список.
- Стрелки -- полностью изолированный жест (клик со стрелки не доходит до
  кнопки, список не открывается), у левой стрелки запас хитбокса.
- Заголовки категорий (SYNT/LFO/FILT/EFFX/MSEG/AMP) -- кнопки от линии
  панели до края первой крутилки; MSEG открывает редактор кривых,
  остальные -- «под будущий функционал» (тултип); шрифт категорий меньше
  (EFFX влезает). Рамка заголовка больше не дублирует название текстом.
- MODE-окна: 92 px, правый край почти вплотную к пунктирной линии конца
  страницы (все пять, включая EFFX DLY/DIST).

### Передняя панель
- Галка ARP: квадрат на всю высоту кнопки (28 px), стоит на вертикальной
  линии начала AMP; ARP и GATE сдвинуты за ней ровными шагами по 124 px;
  MIDI поджат к GATE.
- LEV: риска максимального уровня и микрориска на 100 внутри пунктира;
  пунктир вытянут до низа секций (одинаковые нижние линии).

### ARP
- Надпись PLAY убрана, верхние строки скомпонованы плотнее.

### MSEG
- Хитбокс величины (SIGNED AMOUNT) расширен по всем сторонам.

Отложено (следующие партии): сетка MSEG (триплеты, wheel при рисовании,
GRID VIEW, галка SYNC сеток, хоткеи Alt1-4/B), чинка DRAW/DRAW STEP,
список страниц MSEG с +/- (нужны новые наборы параметров mseg4+ --
расширение схемы) и назначение MSEG-страницы целью из матрицы
(нужно применение сумм 56..63 в процессоре).

Проверки: verify_dsp_mode_patch.py -- секция 30 (1.6.28), PASS (Synth и
FX); MnmCoreTests -- all OK; синтаксис обоих TU в обоих проектах --
0 ошибок.

## 1.6.29 -- MSEG: страницы, DEST, сетка/рисование + регрессии 1.6.28

### About
- Окно About теперь содержит ТОЛЬКО подпись "mnm mod by restrange aka
  kraduvremya" (чёрный фон, без иконки "i" -- NoIcon). Стена текста истории
  убрана из плагина: история живёт в этом файле, справка по управлению --
  в MANUAL.md (рядом с архивом). Заодно исчез риск MSVC C2026: самый длинный
  строковый литерал в редакторе теперь 312 символов.

### Матрица
- DEST снова ПО ПАПКАМ: MACHINE/AMP/FILTER/EFFX/LFO1..3 как подменю
  (раскрытие -- стандартное у JUCE, дополнительной задержки нет), и НОВАЯ
  папка MSEG -- страницы MSEG OUT (56..63) после модулей.
- SOURCE: добавлены источники MSEG4..MSEG8 (18..22, в конце списка после
  RANDOM; порядок выбора = значению enum, без дыр). AUX SRC -- то же
  расширение (OFF + 23).
- DEPTH/списки без изменений (серая полоса, "---", лок-зона).

### Движок
- ModulationMatrix: kTargetCount 56 -> 64; маршруты на 56..63 собираются
  отдельно и СДВИГАЮТ ВЫХОД соответствующей кривой MSEG (сумма/127,
  кламп 0..1). Источники MSEG4..8 читают выходы страниц 4..8.
- Страницы MSEG: 1..8 (было 3). Параметры mseg4..8_{rate,sync,loop}
  добавлены в схему (дефолты как у MSEG1). Скорость/фаза считаются только
  для видимых страниц (msegpages).
- addMsegRoute понимает страницы 0..7 (страница p -> источник 10+p для
  0..2, 15+p для 3..7).

### MSEG-редактор
- Страницы -- лента кнопок: ЛКМ выбирает, вертикальный ЛКМ-драг листает,
  ПКМ открывает список; кнопки «+»/«-»: «+» добавляет страницу (сама
  появляется в матрице), «-» удаляет ТЕКУЩУЮ: если на неё ссылаются
  маршруты -- диалог подтверждения; маршруты на удалённую страницу
  выключаются, страницы выше ПЕРЕИМЕНОВЫВАЮТСЯ (источники, цели 56..63 и
  параметры сдвигаются вниз) -- назначения не теряются, а переименовываются.
- Блок ROUTING: строки 34 px (как в матрице), до 8 строк, тот же порядок
  обхода, источники/цели MSEG4..8 и MSEG OUT подписаны.
- Сетка/рисование (начато в 1.6.28-партии, доведено): полная сетка
  OFF/1/1..1/64 + триоли 1/4T..1/32T; GRID VIEW -- визуальная сетка,
  LINK -- синхронизация рисующей и визуальной сеток; колесо над графиком
  крутит сетку (при рисовании степов -- сетку рисования); Alt+1 грубее,
  Alt+2 мельче, Alt+3 триоли, Alt+4 вкл/выкл привязку, B -- режим
  рисования; подсказка в редакторе дополнена хоткеями.
- DRAW: свободный штрих пишется в ЛЮБУЮ сторону (накопление точек при
  движении, сортировка по X, прореживание, фиксация на отпускании);
  DRAW STEP больше не сбрасывает волну при входе; CUSTOM помнит волну,
  с которой ушли, и возвращает её при возврате; нажатие кнопок форм
  не затирает неотредактированную кастомную волну (undo+restore).

### Панель (регрессии 1.6.28 по отзыву)
- LEV: серая полоса-«шлейф» (полупрозрачная рамка поверх пунктира) убрана;
  пунктир вытянут до низа секций (y=570); риски теперь две ОДИНАКОВЫЕ
  узкие -- на дефолте (100) и на максимуме.
- Кнопка AMP: шрифт категории 20 px как у остальных (был 28).
- ARP: галка-квадрат на всю высоту кнопки (SquareToggle -- рамка закрыта
  со всех четырёх сторон); ARP сдвинут ВПЛОТНУЮ к галке, GATE/TRIG
  сдвинуты с ним (шаги ARP->GATE->TRIG сохранены: 499/623/661).
- Пикер машин: открывается ПОД строкой имени (в пределах окна плагина).
- Строка машины: при смене пресета стрелкой МИГАЕТ и отжимается
  (залипшее состояние "нажато" самоликвидируется -- проверка физического
  зажатия мыши при отрисовке). Стрелки работают как раньше.
- Плавные анимации не добавляются (правило).

### Состояние (state)
- FIX: setStateInformation больше НЕ удаляет ребёнка "MSEG" до загрузки --
  MSEG1 снова загружает свои точки (раньше loadMseg получал пустоту).
- Сохраняются все 8 страниц (теги MSEG..MSEG8) + счётчик "msegpages".

Проверки: verify_dsp_mode_patch.py -- секция 31 (1.6.29) + обновлённые
старые проверки, PASS (Synth и FX); MnmCoreTests -- all OK; синтаксис
обоих TU в обоих проектах -- 0 ошибок; glue-скан чисто. Версии: уголок
BUILD 1.6.29, .jucer x2, CMakeLists x2. JucePluginDefines.h остался 1.6.5
(так было и в 1.6.28 -- не трогаем).

## 1.6.30 -- UX-пакет: пикеры/списки, матрица, MSEG-draw, единые локи, мини-AMP, ARP в пресетах

### Списки и пикеры
- НОВЫЙ СТАНДАРТ: все новые списки открываются НИЖЕ своего хитбокса (док без
  CallOutBox, одна белая рамка включая правую кромку, без стрелки к старому UI).
- Пикер машин: убрана вторая (синяя) рамка и стрелка; список -- под строкой
  имени, верх по границе зоны крутилок. Клик сразу после листания машины больше
  НЕ открывает список (guard 300 мс).
- Папки-подменю получили нарисованную стрелку «>» (пять 2x2 квадратиков).
- MENU: RESET ALL PARAMETERS первым; имена ARP / MOD MATRIX / MSEG / CURVE
  EDITOR / BBOX SAMPLES / BBOX RESTORE KIT / PANIC / CLEAR TAILS / GUI / ABOUT;
  ширина списка -- по самому длинному имени.

### Передняя панель
- MODE-кнопки: текст по центру (PixelButton по умолчанию центрирует), отступ
  как у категорий; заголовок AMP -- тот же паддинг, шрифт 20.
- LEV: надпись в ряду ARP; фейдер РАСТЯНУТ (12,128,46,434): низ на пунктире
  секции фильтра, верх на макс-тике. Серой рамки нет никогда.

### Матрица
- DEPTH -- ГОРИЗОНТАЛЬНЫЙ ЛКМ-драг (Shift -- мелко), ПКМ -- список, дабл-клик --
  сброс. Линии колонок/строк ярче (0.55/0.50 альфа). Колонка DEST -- по самой
  длинной строке параметров.
- SOURCE -- папки как у DEST: RANDOM первым (без папки), MACRO без папки, папки
  MIDI/LFO/MSEG/ARP; MSEG -- только добавленные страницы; баг «ARP id+18»
  устранён. Выбор MSEG-источника/цели активирует страницу при необходимости
  (и бинд в движке).
- Прицел: после подтверждения ячейка мигает чёрное<->белое x2 медленно (~1.3 c,
  330 мс/фаза) -- по умолчанию ВКЛ (MAP FLASH). Клик сразу после прицела список
  не открывает (pickClick-guard).

### MSEG-редактор
- Страницы: ОДИН список («PAGE n/всего», ЛКМ-драг листает, клик -- список ниже
  хитбокса) + «+»/«-». Активны первые 3 страницы, остальные активируются по
  мере выбора/бинда; удаление страницы маршруты СОХРАНЯЕТ (переименовывание).
- SHAPE: кнопки-формы убраны (формы в списке), порядок CUSTOM FLAT RISE FALL
  TRI SINE + SQUARE. CLEAR -- одна точка слева внизу.
- DRAW: обычный DRAW -- по сетке GRID (Alt -- свободно, без сетки); DRAW STEP --
  ручное рисование квадратов по выбранной сетке (без автопреобразования);
  RAND DRAW 1 -- прежняя «дикая» случайная отрисовка (экс-баг = фича); RAND
  DRAW 2 -- управляемый гладкий случайный ход. Собственной сетки поверх GRID
  VIEW больше нет. Хоткеи сетки -- CTRL+1..4 (Alt остался обходом привязки).
- Исправлен мутаж графика во время штриха (в 1.6.29 превью перезаписывало
  точки до фиксации).

### LFO
- Одна объединённая панель замков PAGE+DEST (вместо двух отдельных): LMB --
  замок/рисование драгом, ПКМ -- соло, клик по имени = выбрать; окно -- док
  ниже ячейки. ENFORCEMENT: замок/соло одного LFO запрещает выбор этой цели
  другим (пункт в списке недоступен, драг перескакивает, клик в панели
  игнорируется). По умолчанию выделен LFO1.

### Огибающая AMP
- ЛКМ по категории AMP -- компактное мини-окно (всё содержимое: режим, 8
  слайдеров, график) поверх зоны AMP-крутилок; повторный ЛКМ закрывает.
  ПКМ -- большое окно как раньше. Список режимов -- ЛКМ-драг (обычный клик --
  список).

### Состояние
- getStateInformation теперь синхронизирует атомы PARAM из источника истины
  (паттернов): шаги/VEL/PITCH/HOLD арпеджиатора сохраняются в VST3-пресет.
  Восстановление -- через штатный replaceState.

### Регрессии
- Синтаксис 0 ошибок x4 (Synth/FX), verify PASS (оба), MnmCoreTests OK.

## 1.6.31 -- правки по скриншоту 1.6.30: chrome/LEV/SOURCE/MSEG-draw/прицел

### Chrome и передняя панель
- Серые прямоугольники панелей УБРАНЫ (разметка не видна); осталась рамка шапки.
- Пунктир колонки LEV заканчивается на линии секций (562), а не тянется до 570.
- Риски LEV выровнены по сетке панелей: максимум -- низ шапки категории (124),
  дефолт 100 -- пунктир ячеек (224). На звук не влияет, только вид.
- Рамки PixelButton рисуются заподлицо с границей компонента -- стенка имени
  машины совпадает с панелями (ушло расхождение в 1-2 px).
- Кнопки MODE: границы = границы кнопки категории (95..123 / 335..363);
  выбранный текст комбобокса (MNM и MODE) -- ПО ЦЕНТРУ (positionComboBoxText).

### Матрица
- ФИКС: драг ЛКМ за колонками (края матрицы) менял DEPTH предыдущей строки
  (чтение cells[r*5-1] за границей массива) -- теперь край матрицы неактивен.
- Замок маршрута больше НЕ блокирует прицел (ни крестик-пикер в ячейке DEST,
  ни cord) -- замок защищает только от списков/случайной правки.
- Откат мигания ЧЕРНО-БЕЛЫМ самой ячейки параметра: снова один бледно-белый
  мигок рамкой (как 1.6.26..29). После бинда прицелом мигает РАМКА DEST-ячейки
  строки в матрице (белая/черная x2, 330 мс фаза; галка MAP FLASH).
- Белая рамка прицела больше не залипает на параметре после бинда.
- SOURCE: заголовок "SOURCE" убран; RANDOM первым без папки; папки MIDI (KEY,
  VEL, PITCH WHL, MOD WHL, AFTERTOUCH -- последним), LFO, MACRO (X/Y -- после
  LFO), MSEG (1..8), ARP.
- AUX SOURCE = КОПИЯ меню SOURCE + OFF первым (папки, тот же порядок; правило
  на будущее: aux = source + OFF).
- Стрелки папок развернуты ВПРАВО (острие к параметру, а не к тексту).

### MSEG-редактор
- Страницы ФИКСИРОВАНЫ: 8, все активны ( активация/бинды не нужны). Кнопка
  страниц = просто номер текущей; список -- черный пиксельный скин; "+"/"-"
  удалены. Колесо над редактором листает страницы (прокрутка грида колесом
  убрана).
- DRAW дорисовывает к существующей кривой (точки вне штриха сохраняются) --
  кривую больше не стирает в ровную линию и не мутирует при быстрой риске.
- DRAW STEP: вход в режим и первое касание НЕ квадратят кривую; ступени
  пишутся только в тронутых колонках, остальная кривая жива. Смена сетки
  перезапускает степ-рисование.
- Вертикальные линии-грид от каждой точки кривой убраны.
- Внизу (матрица маршрутов MSEG): прицел как в основной матрице -- потяни ЛКМ
  за гнездо-номер строки на ручку, отпускание = маршрут этой страницы
  (провод, подсветка цели, мигок).

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2 (секции 32/33), MnmCoreTests OK,
  glue-скан чисто. Версии: BUILD 1.6.31, .jucer x2, CMakeLists x2.

## 1.6.32 -- лицо: MODE/категории/LEV/UNDO-REDO; матрица: DEST OFF + AUX DEPTH; спасение сохранения

### Передняя панель
- Кнопки категорий 96px: правая стенка ровно по границе первой крутилки.
- Кнопки MODE 96px, левые края на линиях колонок крутилок (DLY -- на линии
  SRR); рамка вровень с рамкой категории; списки MODE компактные
  (по размеру кнопки, шрифт 14px) и открываются ПОД кнопкой (только MODE).
- Текст MODE/MNM центрируется через drawLabel -- поверх старой логики.
- Фейдер LEV растянут (124..562): заполнение доходит до обеих рисок
  (127 -> верхняя, 100 -> нижняя, 0 -> пунктир 562).
- CLEAR прижат к верхней рамке (1013,1); UNDO/REDO -- две кнопки-стрелки
  между BPM и MENU, в ряд с галкой BPM-синка: полный снимок состояния
  (до 60 шагов, без уведомлений хоста), ручки/списки/матрица/ARP/MSEG.

### Матрица
- DEST: "---" = OFF (значение 0), в списке его нет; цель = значение-1
  (0 зарезервирован); двойной клик -- OFF; драг/колесо 0..64. Старые пресеты
  не мигрируют сознательно.
- Новая колонка AUX DEPTH (+-100%, по умолчанию 0 -- AUX молчит, пока не
  открыл): список по клику, драг/колесо, дабл-клик -- 0. Колонки сдвинуты,
  DEST/AUX SRC расширены (все имена 14px, AFTERTOUCH больше не мельчает).
- AUX SRC: вернули RANDOM (сразу после OFF) -- меню снова дословная копия
  SOURCE + OFF.
- UNI/BIP: клик переключает, список по ПКМ, драг остался.
- Хитбокс замка/прицел пересчитаны под компактную иконку.

### MSEG
- Колесо над редактором снова прокручивает содержимое (смена страниц колесом
  откачена); кнопки -- единый шрифт 14px, ряд ужат, зоны не наезжают.
- DRAW больше не дёргает начало штриха: во время рисования -- только
  предпросмотр (полупрозрачный), запись один раз на отпускании.
- Список сетки открывается ПОД кнопкой (ПКМ/ЛКМ -- список, драг крутит).

### Сохранение (критично)
- Пресеты снова сохраняются: откачен цикл 1.6.30 в getStateInformation
  (перезапись каждого параметра в дерево рвал сохранение); истину в дерево
  тянет тихий таймер syncTreeFromAtoms (4 Гц, без notifyHost) -- ARP-страницы
  и рандомайзеры тоже попадают в пресет.

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2 (568 проверок, новые секции 1.6.32),
  MnmCoreTests OK, glue-скан чисто. Версии: BUILD 1.6.32, .jucer x2,
  CMakeLists x2.

## 1.6.32 hotfix -- критический фикс сохранения (норм/денорм) + возврат лица

### Сохранение (корень найден)
- Причина «пресетов-нулей»: цикл 1.6.30 и таймер 1.6.32 писали в дерево
  НОРМАЛИЗОВАННОЕ значение, а дерево APVTS хранит ДЕНОРМАЛИЗОВАННОЕ --
  загрузка превращала значения в нули/минимумы, а таймер ещё и пружинил
  ручки назад каждые 250 мс («крутить не даёт»).
- Все перезаписи состояния при сохранении УДАЛЕНЫ: сохранение = чистый
  copyState(), JUCE сам флашит атомы в дерево (в т.ч. ARP-шаги и
  рандомайзеры, писанные без notify) -- поведение ДО 1.6.29.
- Таймер syncTreeFromAtoms удалён полностью. RESET ALL PARAMETERS теперь
  сбрасывает и все 8 MSEG-страниц.

### Лицо (съеденные комментарии -- код выпадал молча)
- Вернулись: BPM-ряд (SYNC + цифры), кнопка AMP MODE, гашение мигания
  ячейки ручки после отпускания патч-корда.
- Бинд прицелом снова ОТКРЫВАЕТ матрицу и мигает рамкой DEST-ячейки.
- Списки MODE открываются ПОД кнопкой (пустая target-зона вводила JUCE
  в заблуждение -- вбок); фон лейбла больше не съедает левую/правую
  кромки рамок MODE.
- Новый статический сканер «съеденных комментариев» (batches/check_comments.py)
  -- в дереве 0 подозрительных.

## 1.6.33 -- доводка лица/матрицы/MSEG по прогону 1.6.32

### Сохранение/Undo
- Рандомайзер ARP-степов пишет шаги ПРЯМО в дерево (тихо, без notify) --
  рандомные шаги попадают в пресет всегда; точка отката снимается перед
  рандомом (UNDO/REDO видят рандом степов).
- RESET ALL PARAMETERS сбрасывает и все 8 MSEG-страниц (1.6.32x).

### Передняя панель
- Кнопки MODE: рамка-обводка как у кнопок категорий (лейбл внутри рамки),
  текст -- тот же шрифт, что у названий категорий (21px, по центру).
- LEV: шкала фейдера кусочная (127 = верхняя риска, 100 = нижняя, 0 =
  пунктир секций) -- хват фейдера и МЫШЬ попадают в риски точно; громкость
  не изменена.
- UNDO/REDO: квадратные пиксель-стрелки с подписями UNDO/REDO.
- Галка ARP: чуть левее категории AMP, чуть выше и чуть ниже вниз.
- Стрелки выбора машин -- полоса вплотную к правой стенке кнопки.
- MAP FLASH удалена (кнопка + серый мигок ячейки ручки после модуляции):
  мигает только РАМКА DEST-ячейки в матрице.
- Бинд патч-кордом (ПКМ-мод) теперь ОТКРЫВАЕТ матрицу и мигает DEST-ячейкой
  нужного маршрута -- как прицел из матрицы.
- Списки MENU и CLEAR (ПКМ) -- компактные (как MODE).

### Матрица
- AUX DEPTH: клик открывает компактный список ступеней ПОД ячейкой (рамка,
  имена, сортировка по значению); скачок «всегда +100» исправлен (драг брал
  неинициализированный X).
- DEST: добавлен 65-й выбор -- «---» = OFF индексом 0; MSEG8 OUT снова
  выбирается (раньше Choice из 64 значений прижимал 64 -> 63 = MSEG7 OUT).

### MSEG
- SHAPE/GRID/GRID VIEW -- логика DEST-матрицы: клик открывает компактный
  список ПОСЛЕ отпускания (ЛКМ-драг больше не выбирает пункт), драг/колесо
  листают значения.
- GRID VIEW стартует синхронным с GRID (LINK включён по умолчанию).
- ПКМ по точке = режим стирания: ведение мыши стирает точки.
- В DRAW курсор -- перекрестие, точки и изгибы не подсвечиваются.

### LFO
- Окно замков: ДВЕ КОЛОНКИ PAGE | DEST, строки 30px, шрифт крупнее; кисть
  замков красит ту колонку, где курсор (раньше -- колонку начала жеста).

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2 (588 проверок, секции 1.6.33),
  MnmCoreTests OK, glue-скан и сканер комментариев чисто. Версии: BUILD
  1.6.33, .jucer x2, CMakeLists x2.

## 1.6.34 -- LEV-критфике, порядок папок SOURCE/AUX, MSEG-прицел, чистка матрицы

### Критично: звук/LEV
- 1.6.33 сломала LEV: кастомная шкала на ПАРАМЕТРЕ заставляла движок читать
  громкость как 0 (звука нет), а серый шток рисовался мимо рисок.
- Теперь: параметр level -- обычный 0..127; кусочная шкала живёт НА СЛАЙДЕРЕ
  (мышь и заполнение считаются ею же): 127 = риска макс, 100 = риска дефолта,
  0 = линия секций; рисование -- заполнение + чёрная насечка-хват (серого
  штока больше нет).

### Матрица
- AUX SRC: «пустая ячейка» после RANDOM (сепаратор) убрана; порядок папок:
  MIDI, LFO, ARP, MSEG, MACRO -- в SOURCE и AUX SRC одинаково (одна таблица
  позиций).
- Шапка: заголовок AUX DEPTH теперь реально рисуется (цикл обрезался на 7
  колонках), добавлены вертикальные границы колонок в шапке.
- Сортировка и перетаскивание строк переносят ВСЕ 7 полей -- AUX SRC и
  AUX DEPTH больше не отрываются от своей строки.
- Шрифт общих списков (SOURCE/DEST/...) уменьшен до 14px, высота пункта 22.
- MODE: правый край кнопок до рамки панели страницы (SYNT/FILT/AMP/DIST = 99px).

### MSEG
- Гнездо строки маршрута -- теперь ПРИЦЕЛ (клик-клик, как DEST в матрице),
  провод убран: клик по гнезду прячет окно, клик по ручке назначает маршрут
  ЭТОЙ страницы, окно возвращается и мигает строка маршрута (рамка, как в
  матрице). UNASSIGNED-строки получили такие же гнёзда-прицелы с крестом.
- Корд с MSEG-гнёзд (1/2/3) на ручке: матрица ОТКРЫВАЕТСЯ и мигает DEST-ячейка
  слота (как у LFO).
- Ампаунт маршрута: драг ГОРИЗОНТАЛЬНЫЙ, бар вдвое короче (400..650), зона =
  бару.
- GRID VIEW стартует синхронным с GRID (LINK) -- сетка рисования совпадает
  до первой смены.

### Назначения
- Новое назначение (прицел матрицы по пустой строке, корды) всегда всплывает
  на строку 0, остальные сдвигаются вниз; addMsegRoute тоже поднимает и
  возвращает слот.
- Галка ARP: верх на 1px выше и низ на 1px ниже уровня кнопки ARP (rate).

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2 (+секции 1.6.34), MnmCoreTests OK,
  сканер комментариев чисто. Версии: BUILD 1.6.34, .jucer x2, CMakeLists x2.

## 1.6.35 -- LEV: шкала переживает attachment; ширина списков; чистка

### LEV (критический фикс 1.6.34)
- SliderAttachment при создании ПЕРЕЗАПИСЫВАЕТ шкалу слайдера шкалой
  параметра -- кусочная шкала 1.6.34 ставилась ДО него и умирала: фейдер
  снова линейный, а рисование в L&F было ещё и инвертировано (127 снизу).
- Теперь: кусочная шкала ставится ПОСЛЕ создания attachment; рисование --
  заполнение сверху вниз на кусочной позиции: 127 = риска макс (124),
  100 = риска дефолта (224), 0 = линия секций (562). Параметр -- обычный
  0..127 (звук не тронут).

### Матрица/MSEG
- CLEAR: сбрасывает и AUX DEPTH (раньше колонка пережила клир).
- Строки маршрутов MSEG: ампаунт-бар 395..585, значение % и кнопка
  стирания придвинуты к бару (всё в левой половине, до конца имени).
- Возврат в MSEG-редактор после прицела -- позиция скролла сохраняется.

### Списки
- Компактные списки (MODE/ENVELOPE old-mnm/GRID/...) теперь РОВНО по ширине
  своей кнопки (минимум пункта = ширине кнопки, от 96px).
- Режим огибающей old/mnm в окне AMP -- компактный список по ширине кнопки,
  открывается вниз; драг/колесо листают.
- Текст MODE -- 1-в-1 как кнопки категорий: отступ 5px, шрифт 20, центр.

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2 (+секции 1.6.35), MnmCoreTests OK,
  сканер комментариев чисто. Версии: BUILD 1.6.35, .jucer x2, CMakeLists x2.

## 1.6.36 -- LEV: найден настоящий корень (перепутанные convert-функции)

- В кусочной шкале LEV convertFrom0To1 и convertTo0To1 стояли МЕСТАМИ:
  convertFrom0To1 получала пропорцию 0..1, зажимала в 0..127 и всегда
  отдавала ~0 (клик/драг = 0 «и визуально и реально»), а convertTo0To1(100)
  возвращала 1.0 (двойной клик 100 выглядел как максимум).
- Проверено численно: пропорция риски дефолта (0.7717) -> значение 100;
  значение 100 -> заполнение ровно на риске 224; 127 -> верх (124);
  0 -> линия секций (562); мышь ведёт 1-в-1.
- Шкала ставится после SliderAttachment (1.6.35), параметр -- линейный 0..127.
- MODE: рисование текста 1-в-1 как у категорий (отступ 5px, шрифт 20, центр);
  компактные списки -- ровно по ширине кнопки.

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2, MnmCoreTests OK. BUILD 1.6.36.

## 1.6.37 -- по скриншотам: НАСТОЯЩИЙ корень «MODE не центрирован», LEV-зона, ширина списка

### MODE (корень, наконец)
- Лейбл комбобокса создаётся через createComboBoxTextBox и это PixelLabel
  со СОБСТВЕННЫМ paint() -- он рисовал текст 14px ВЛЕВО и никогда не звал
  drawLabel. Все центрирования 1.6.31..35 просто не выполнялись.
- Теперь PixelLabel рисует по justification (центр у комбобоксов), а лейбл
  комбобокса -- 20px, как шрифт кнопок категорий AMP/SYNT/FILT.

### LEV (по пиксельному замеру скриншота: заполнение 233..549 вместо 124..562)
- V4::getSliderLayout отдаёт зону слайдера с отступами ~15px сверху/снизу
  под большой палец -- кусочная шкала считалась не на тех пикселях.
- Переопределён getSliderLayout: у LEV зона = вся высота компонента
  (12,124,46,438) -- заполнение точно 124/224/562, мышь 1-в-1 (та же зона
  используется и для ввода).

### Списки
- Лишние пиксели справа/слева дропдауна = рамка PopupMenu (2+2px):
  getPopupMenuBorderSizeWithOptions = 1, пункт = кнопка-2 -- внешняя ширина
  списка ровно по ширине кнопки.

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2, MnmCoreTests OK. BUILD 1.6.37.

## 1.6.38 -- LAYOUT EDIT (простой редактор интерфейса) + Standalone

### Зачем
- «Простой редактор интерфейса -- переместить что-то и пересохранить очень
  простым образом» -- без секретных хоткеев: пункт прямо в MENU.

### Как работает
- MENU -> LAYOUT EDIT: поверх ТЕКУЩЕГО экрана (лицевая панель, ARP, MSEG,
  MATRIX) появляется полупрозрачный оверлей с рамками вокруг двигаемых
  компонентов. ЛКМ-драг -- переместить; SHIFT+ЛКМ-драг -- изменить размер;
  двойной клик по элементу -- вернуть как было (и удалить из файла); RESET
  ALL -- сбросить всё; SAVE EXIT / ESC -- выйти. Под курсором -- имя и
  живые координаты.
- Двигается только именованное (componentID): лицевая панель -- arp/matrix/
  undo/redo/bpm/категории/MODE-списки/LEV/48 ручек-ячеек (c<панель><индекс>);
  ARP-страница -- enable/hold/mode/rate/oct/gate/случайности/page/lane;
  MSEG -- shape/grid/steps/loop/sync/rate/shift/страницы; матрица -- CLEAR.
- Каждое перемещение СРАЗУ пишется в layout.json
  (userApplicationDataDir/MonomachineNova/layout.json) -- «переместил и
  пересохранил» одним жестом, отдельной кнопки «сохранить» нет.
- Применение: в самом КОНЦЕ resized() лицевой панели/ARP/MSEG (после всех
  дефолтных setBounds) и в точке создания CLEAR матрицы -- сохранённые
  координаты всегда сильнее дефолта; загрузка файла один раз в конструкторе
  редактора, до первого resized.

### Standalone
- FORMATS Standalone VST3 в обоих CMakeLists (+ pluginFormats в .jucer):
  обе цели собираются и как самостоятельные приложения (JUCE AudioPluginHost
  -стиль), где редактор доступен без DAW.

### Регрессии
- Синтаксис 0 ошибок x4, verify PASS x2, check_comments 0 x2, деревья Synth
  и FX идентичны по общим файлам. BUILD 1.6.38.

## 1.6.39 -- откат Standalone
- Standalone оказался не нужен (пользователю отдельный таргет плагина не
  требуется): FORMATS снова только VST3 в обоих CMakeLists и .jucer.
- LAYOUT EDIT остаётся пунктом MENU внутри плагина (см. 1.6.38). Вопрос
  формы редактора (отдельная мини-прога «только интерфейс» vs пункт меню)
  и «запечь мой расклад как дефолт» -- обсуждается с пользователем.
- Регрессии: синтаксис 0 x4, verify PASS x2, check_comments 0 x2. BUILD 1.6.39.

## 1.6.40 -- LAYOUT EDITOR: отдельная прога «только интерфейс»
- Новый CMake-таргет MonomachineNovaLayoutSynth / MonomachineNovaLayoutFX
  (juce_add_gui_app, только CMake -- .jucer второй таргет не держит):
  Source/LayoutEditorMain.cpp открывает обычный редактор поверх живого
  процессора БЕЗ аудио и сразу включает LAYOUT EDIT (новый публичный
  MonomachineNovaAudioProcessorEditor::startLayoutEdit()).
- Расклад -- общий с плагином MonomachineNova/layout.json: передвинул в
  проге -- в плагине стало так (и наоборот). Ресайс окна НЕ делаем (решено).
- Workflow с агентом: пользователь присылает layout.json -- правим/дополняем
  набор двигаемого (componentID) или закрепляем расклад как заводской дефолт.
- Регрессии: синтаксис 0 x6 (Editor/Processor/Main x Synth/FX), verify PASS
  x2, check_comments 0 x2. BUILD 1.6.40.

## 1.6.41 -- LAYOUT EDIT v2 + матрица (колонки/sort/clear) + живучие доп-окна

### Редактор расклада
- F1 -- открыть/закрыть LAYOUT EDIT (и MENU-пункт остался), F2 -- сохранить
  (надпись SAVED!). Работает на любом экране.
- CTRL+ЛКМ-драг -- выравнивание по ближайшему соседу (граница или центр, порог
  8px, X и Y независимо), жёлтая пунктирная линия показывает, по чему ровняет.
- ALT+ЛКМ -- взять ОДНУ границу элемента (какая ближе к курсору, до 8px) и
  тянуть только её.
- Двойной клик по ТЕКСТОВОМУ элементу (кнопки/подписи/ручки-ячейки) --
  переименовать (FACTORY TEXT в диалоге = вернуть заводское имя); у
  нетекстовых даблклик по-прежнему откатывает позицию. DEL на элементе --
  полный откат (позиция+текст), RESET ALL -- всё заводское (вкл. тексты и
  ширины колонок).
- Базовый (заводской) текст запоминается при ПЕРВОМ переименовании -- откат
  работает и после перезапуска.

### Матрица
- РЕСАЙЗ КОЛОНОК: тянуть границу колонки в ШАПКЕ (курсор <->); правый край
  неподвижен, AUX DEPTH поджимается. Ширины хранятся в layout.json
  (matrix.colw), применяются при открытии страницы.
- Сортировка кликом по шапке теперь и для AUX, и для AUX DEPTH (раньше было
  только ON..MODE; guard column>5 в процессоре).
- CLEAR: RMB "ERASE EVERYTHING" не чистил AUX DEPTH (был только в ЛКМ);
  добавлены пункты ONLY AUX SOURCE / ONLY AUX DEPTH.

### MSEG
- Кнопки форм (DRAW/DRAW STEP/RAND DRAW) и инструментов (UNDO/REDO/INVERT/
  FLIP X/COPY/PASTE) получили componentID -- теперь видны в LAYOUT EDIT.
- Страницы MSEG: уникальные ID pg0..pg7 (был один "pagebtn" на все).

### Доп-окна (REPITCH/DSND/PORTA/GUI SPEED)
- CallOutBox (модальный, закрывался по клику вне) заменён на нёмодальное
  плавающее окно: клик по MENU или вне окна плагина БОЛЬШЕ НЕ закрывает
  настройки. Закрытие -- повторный RMB по тому же контролу (тумблер).

### Регрессии
- Синтаксис 0 x6, verify PASS x2, check_comments 0 x2. BUILD 1.6.41.

## 1.6.42 -- LAYOUT EDIT v3 (заметки/undo/снап) + ARP по скрину + доп-окна внутри

### LAYOUT EDIT
- F2 -- заметка для агента У КУРСОРА: текст + кнопка COPY COORDS (кладёт "x,y" в
  буфер обмена для промпта). Заметки -- жёлтые маркеры в редакторе, двойной клик
  правка, DEL удалить; хранятся в layout.json (RESET ALL их не трогает).
- F4 -- сохранить (кнопка теперь "SAVE F4"), F1 -- закрыть.
- CTRL+Z / CTRL+Y -- откат/повтор движений расклада (до 60 шагов).
- SHIFT-ресайз и ALT-граница с зажатым CTRL теперь тоже ПРИЛИПАЮТ к соседям
  (жёлтые линии). Исправлен сдвиг координат при ресайзе на страницах.

### Нарисованные тексты -- теперь в редакторе (TextTag)
- Название синта, значок-логотип, LEV, BPM: двигаются, ресайзятся (шрифт
  масштабируется), переименовываются двойным кликом (FACTORY TEXT -- заводское).
- Имена строк матрицы MSEG (вкл. UNASSIGNED) -- тег rownames: двигаются по X и
  по ширине (вертикаль привязана к строкам).
- Окно страниц (viewport) двигается и ресайзится в редакторе -- видно, как
  меняется окно MSEG при рисовании.

### Доп-окна (REPITCH/DSND/PORTA/GUI)
- Панель снова ВНУТРИ окна плагина у крутилки (не поверх других приложений при
  alt-tab), с БЕЛОЙ ОБВОДКОЙ. ЛКМ вне НЕ закрывает. Закрытие: ЛЮБАЯ ПКМ в окне
  плагина (кроме самой панели) или повторный RMB по тому же контролу.

### Меню
- Сепаратор после BBOX RESTORE KIT -- тонкая линия вместо пустой "кнопки".

### Мини-окно AMP ENV -- точно по скрину mode env digital reference
- Настоящие кнопки AMP и MODE остаются видимыми и кликабельными поверх окна
  (шапка AMP | MNM), окно рисует только рамки ячеек; список old/mnm не
  дублируется. RMB по AMP открывает большую страницу (мини закрывается).

### ARP-страница -- пересобрана по скрину arp ref clean2
- Одна шапка: MODE/OCT/RATE/GATE/VELOCITY/WRAP с подписями, галки AUTO SWAP
  PAGE и FOLLOW PLAY, PAGE+PAGE LIMIT, PAGE RND, RANDOM PAGE/PITCH/VEL/HOLD.
- ALL убрана: ПКМ на PAGE рандомит все страницы (как и было с 1.6.25).
- STEP RND заменён на AUTO SWAP PAGE: 8 шагов -- переход на СЛЕДУЮЩУЮ страницу
  в пределах PAGE LIMIT, по кругу (движок: раньше `stepIndex %= 8` ломал цепочку
  страниц -- PAGE LIMIT не работал).
- FOLLOW PLAY: лейн показывает ИГРАЮЩУЮ страницу (PAGE RND и авто-переход видны).
- Лейн поднят (14,147,1150,326), страница 478px -- всё видно БЕЗ скролла.
- ЛКМ-драг списков и клики работают как раньше (DragChoice).

### Регрессии
- Синтаксис 0 x6, verify PASS x2, check_comments 0 x2, деревья идентичны.
- Попутно пойман и починен съеденный хвост строки (класс бага) в мини-ENV.

## 1.6.43 -- LAYOUT EDIT v4 (зум/выделение/группы/стили) + фиксы AUX/WRAP/DSND

### LAYOUT EDIT v4
- Колесо мыши -- зум редактора x1..x4 ВОКРУГ КУРСОРА. F1/ПКМ/Esc -- выход (зум
  сбрасывается). F3 -- перечитать layout.json с диска (если поправил руками).
- Рамка-выделение на пустом месте; SHIFT-клик -- добавить/убрать элемент из
  выделения. G -- склеить выделенные в ГРУППУ (двигаются одним блоком; G на
  одиночном элементе группы расклеивает). Группы хранятся в layout.json.
- Ресайз за УГОЛ по диагонали (курсоры LR/UD/TL/TR), порог 8 px; тянемая стенка
  подсвечивается ЗЕЛЁНЫМ; CTRL -- прилипание к соседям (двиг и ресайз).
- C -- стиль текста у выделенного тега: размер (0.5..3x), шрифт PIXEL/SANS/MONO,
  PICK COLOR (живой пикер в отдельном окне), RESET. Кнопки -- масштаб шрифта.
- Readout показывает nx/ny -- координаты БЛИЖАЙШЕЙ границы соседа (гориз/верт).
- Всплывающие подсказки-заголовки ARP (MODE/OCT/RATE/GATE/VELOCITY/WRAP/PAGE...)
  -- теги LAYOUT EDIT (двигаются, как всё остальное).

### Фиксы поведения
- DSND / REPITCH / PORTA / GUI снова открываются по ПКМ: в 1.6.42 панель гасилась
  ТЕМ ЖЕ кликом, которым открывалась (mouseDown ячейки срабатывает раньше
  слушателя "ПКМ гасит окно"). Повторный ПКМ по той же крутилке -- тумблер.
- AUX-меню матрицы: off-by-one (выбор KEY ставил RANDOM, VEL -- KEY); SOURCE без
  изменений; AUX DEPTH-список под ячейкой как был.
- WRAP (ARP): снова работает -- квадрат со значением 1..16 (в 1.6.42 строка
  range/attachment была съедена комментарием и функция молчала); подпись WRAP.
- Мини-ENV: сепараторы пунктира выровнены (96/288 вместо 129/258 -- те самые
  линии 601,114/729,107 при доке 471,95).
- Списки значений матрицы (SOURCE/DEST/DEPTH/UNI) открываются у 120,227;
  ширины колонок не тронуты, последняя колонка не выталкивается за край.
- Расклад верхнего ряда из JSON пользователя: undo 1098, redo 1129, menu
  1161,0; sync 961,4,61,30; BPM 1022,5,70,30; arp-галка 471,58,32,31; ARP
  503,58,116,30; GATE 619,57,42,32; viewport 72,95,1180,470; title 72,4,399,36;
  logo 26,16,18,29. DSP MODE и LEV не двигались.

## 1.6.44 -- ПКМ по крутилке, булавка доп-окон, групповые размеры, Z-слой

### Доп-окна (REPITCH/DSND/PORTA/GUI)
- ФИКС: ПКМ по самой КРУТИЛКЕ DTIM/DSND снова открывает окно. Причина: клик по
  ручке получает слайдер ВНУТРИ ячейки, а слушатель "ПКМ гасит окно" срабатывал
  раньше и гасил панель тем же кликом (в 1.6.43 был закрыт только клик мимо ручки).
- Окна закрываются ЛЮБЫМ кликом мимо (как раньше). Справа вверху -- БУЛАВКА:
  нажал -- окно закреплено, клики мимо его не закрывают; повторный ПКМ по той же
  крутилке по-прежнему тумблер. ЛКМ за пустое место окна тащит его по экрану.

### AMP/ENV
- Мини-ENV больше НЕ открывается поверх большого окна огибающей.
- AMP/MODE не просачиваются на страницу большого ENV.
- ARP ENABLE/HOST TEMPO/ARP HOLD: хитбокс 150->104 -- больше не наезжает на
  текст MODE страницы ARP (клик по надписи больше не переключает галку).

### LAYOUT EDIT
- Изменение края (ALT), угла и SHIFT-размер применяются ко ВСЕМ выделенным
  (рамка/SHIFT-клик) -- "чуть-чуть изменить всем" работает.
- Z -- слой выделенного: НА СЛОЙ ВПЕРЁД/НАЗАД + список СЛОЙ 1..N (задний..передний);
  слой хранится в layout.json и восстанавливается. G-группы УБРАНЫ (кнопка LFO и
  патчкорд -- единое целое, склейка не нужна).
- hitAt берёт САМЫЙ ВЕРХНИЙ компонент -- основной viewport легко подцепить.
- Плавающие доп-окна редактором не двигаются (у них свой драг/булавка).
- Пикер цвета (C -> PICK COLOR): добавлена кнопка OK -- подтвердить и закрыть.

### Матрица и списки
- Колонки начинаются от ЛЕВОЙ границы (в окне плагина = 85,133; было 154);
  ширины -- присланный JSON: [15,52,165,165,150,124,120,148] (сумма 939),
  правый край таблицы 987; ресайз колонок и matrix.colw в layout.json работают.
- Списки MODE-комбо (AMP MODE и др.) выравниваются по ПРАВОМУ краю кнопки --
  не выпирают за правую стенку категории (854,399). Списки ручек (DTIM и др.)
  привязаны к ячейке и не пересекают категорию.
- Окно выбора машин: масштаб имён 0.9.

## 1.6.45 -- FIX: угловой ресайз больше не "инвертирован"
- LAYOUT EDIT: тянешь угол -- элемент УЛЕТАЛ в одну сторону и растягивался.
  Причина: дельта считалась как (курсор минус СМЕЩЕНИЕ курсора внутри элемента),
  т.е. к каждому кадру прибавлялась координата самого элемента (~2x) -- первый же
  кадр уносил край. Теперь угол тянется от ТОЧКИ НАЖАТИЯ, база -- снапшот
  прямоугольника на старте жеста (как у ALT-стенки). Групповой угол у выделения
  наследует ту же правильную дельту.

## 1.6.46 -- FIX: границы колонок матрицы
- Шапка и строки: разделители колонок теперь НА ОДНОЙ вертикали (colX-3;
  было: шапка colX-1, строки colX-4 -- разъезжались на 3px и в разных местах).
- AUX DEPTH: добавлена ПРАВАЯ граница (шапка + каждая строка), линии строк
  заканчиваются у неё (раньше таблица обрывалась на пустых 35px).
- ON: добавлена ПЕРЕДНЯЯ граница в шапке -- тянется курсором (ресайз полосы
  номеров строк) = сдвиг старта колонок от начала матрицы; сохраняется в
  matrix.colw.
- Тусклый отдельный разделитель AUX убран (общий ряд границ).

## 1.6.47 -- FIX: ресайз переписан с нуля (единый снапшот-движок) + краш-гигиена
- LAYOUT EDIT: растяжение/улетание при изменении размера ЛЕЧЕНO РАДИКАЛЬНО. Все
  4 пути ресайза (двигать / ALT-край / угол / SHIFT-размер) раньше жили на разных
  семантиках (offset против точки против живого прямоугольника) -- отсюда
  "инверсии" и улетания. Теперь ЖЕСТ = {элемент, точка старта, СНАПШОТ
  прямоугольника}; дельта всегда = курсор минус точка старта; изменение всегда
  = снапшот + дельта. Улетание структурно невозможно.
- Край без ALT больше НЕ ресайзит (только двигает) -- случайно растянуть,
  взявшись за край, нельзя. Углы: строго углы (курсор по диагонали). ALT --
  любая стенка (зелёная подсветка). SHIFT после мультивыделения -- размер всем.
- КРАШ ABLETON при ALT+драге: добавлена краш-гигиена оверлея -- каждый
  mouse-ивент пересверяет набор компонентов; исчезнувшие элементы вычищаются
  из выделения/жеста/групп до первого обращения (висячих указателей больше нет).
  Если краш повторится -- нужен точный сценарий (какой элемент, какая страница).

## 1.6.48 -- оверлей без каши + F5/F4 + честный undo + пин поверх
- "Куча текстов друг на друге" и "всё супер белое" при перемещении: во время
  жеста горячим считался КАЖДЫЙ элемент (белая рамка 2px + readout у всех).
  Теперь белый -- только тащимый, readout -- одна строка у тащимого.
- F5 -- ПЕРЕЧИТАТЬ layout.json с диска (F3 работает как алиас). F4 -- СБРОС
  расклада к дефолту (RESET ALL). Сохранение -- кнопка SAVE (клик). Фокус
  возвращается по любому клику в редактор -- F-клавиши не умирают после
  диалогов/пикера цвета.
- RESET ALL: синхронизирует список элементов перед сбросом; сброс = очистка
  layout.json + полная переразкладка страницы (теперь и для ARP/страниц).
- Undo/Redo: точка отката ставится ТОЛЬКО когда жест реально что-то изменил
  (раньше каждый клик пихал в стек пустые шаги и чистил redo -- "1 шаг и
  Ctrl+Y пропал"). CTRL+Z / CTRL+Y как раньше, до 60 шагов.
- "Сурфейс сзади выделяется вместо крутилок": невидимый viewport больше не
  перехватывает клик и не рисуется -- выделяется то, что видно.
- Булавка мини-окон: закреплённое окно держится ПОВЕРХ соседей (надписи
  больше не перекрываются элементами страницы).

## 1.6.49 -- хоткеи по схеме пользователя + AMP-кнопка двигаемая + честный сброс
- ИТОГОВАЯ СХЕМА ХОТКЕЕВ LAYOUT EDIT: F1 -- закрыть/открыть, F2 -- заметка
  (вне редактора F2 -- сохранить), F3 -- СОХРАНИТЬ (кнопка SAVE дублирует),
  F4 -- СБРОС к дефолту, F5 -- перечитать layout.json с диска. Ctrl+Z/Ctrl+Y --
  откат/повтор. Аудит конфликтов: C/Z-диалоги больше не открываются от
  Ctrl+C/Ctrl+Z (добавлены guards); пересечений F1..F5 между уровнями нет.
- Кнопка категории AMP: добавлена в layIds как "ampcat" -- теперь тоже
  двигается/сохраняется в LAYOUT EDIT (все соседи давно умели, она -- нет).
- RESET ALL / DEL: теги (название синта, логотип, заголовки ARP и пр.)
  возвращаются на ЗАВОДСКИЕ места и текст (base) -- раньше теги оставались
  съехавшими до перезахода (их дефолт живёт только в конструкторе).

## 1.6.50 -- FIX: layout.json == то, что на экране
- Клик по элементу БЕЗ движения больше не создаёт/не обновляет запись в файле
  (раньше любой клик по ARP ON etc. писал его координаты -- "в файле только
  arpon, хотя я его вернул").
- CTRL+Z / CTRL+Y теперь сразу пишут результат в layout.json (раньше откат
  жил только в памяти -- файл оставался "старой версией" до следующего жеста).
- Правило целостности: ЛЮБОЕ изменение расклада (жест, откат, повтор, сброс,
  переименование, стиль) мгновенно и синхронно пишет файл. F3 -- принудительно
  перезаписать тем, что на экране; F5 -- наоборот, взять файл.

## 1.6.51 -- LAYOUT EDIT: вся механика мышкой + диагностика записи
- В шапке редактора ВТОРОЙ РЯД КНОПОК: UNDO / REDO / COPY JSON. Откат и повтор
  больше не зависят от клавиатуры (хоткеи "через раз" = украденный фокус после
  меню/диалогов; фокус теперь возвращается автоматически после каждого меню,
  диалога и пикера цвета, но критичный путь -- кнопки).
- COPY JSON -- ВЕСЬ текущий расклад в буфер обмена одним кликом: координаты
  можно брать без файла (вставлять в промпт агенту). COPIED! -- подтверждение.
- SAVE: теперь честный -- при ошибке записи видно "SAVE FAIL!" (раньше сбой
  выглядел как "сохранил, а файла нет"). Кнопка SAVE больше не закрывает
  редактор. Каталог расклада создаётся перед записью.
- Схема неизменна: F3 сохранить / F4 сброс / F5 перечитать с диска; любой жест
  пишет файл мгновенно.

## 1.6.52 -- канва редактора БЕЗ виртуальных кнопок, всё в меню F2
- ПРИЧИНА "кнопки не пишутся в файл": виртуальные кнопки редактора (SAVE/RESET/
  UNDO/REDO/COPY JSON) были нарисованы ПОВЕРХ верхнего ряда реальных контролов
  и съедали клики -- undo/redo/menu/bpm/dspmode нельзя было даже схватить.
- С канвы убрано ВСЁ кликабельное. Подсказка хоткеев -- вниз канвы, фидбек
  SAVED!/SAVE FAIL!/COPIED! -- тоже вниз (ничего не перекрывают).
- F2 -- МЕНЮ редактора: SAVE / RELOAD FILE / RESET ALL / UNDO / REDO /
  COPY JSON / ERASE FILE / NOTE AT CURSOR / CLOSE. ERASE FILE -- удалить
  layout.json и получить чистый заводской расклад.
- Теперь клик по канве всегда попадает в элементы -- редактор честно работает
  как пикер координат: подвинул -> F2 -> COPY JSON -> координаты в буфере.
- Палитра (C): цвета рисуются только у ТЕГОВ (название/BPM/LEV/заголовки ARP) --
  у кнопок цвет текста в движке не параметризован; размер/шрифт у тегов тоже.

## 1.6.53 -- FIX: мультиинстанс-гонка layout.json + честная диагностика записи
- ПРИЧИНА "двигаю -- в файле пусто": layout.json -- ОБЩИЙ для Synth-инстанса,
  FX-инстанса и standalone-редактора. Каждый держит свой расклад В ПАМЯТИ и
  при сохранении перезаписывает файл ЦЕЛИКОМ. Стер файл -> один инстанс
  записал правку -> второй переписал файл своим (пустым/старым) магазином.
  Отсюда же "одни элементы пишутся, другие нет": ты видел записи другой
  сессии, а свои затирались.
- FIX: каждый жест теперь НАЧИНАЕТСЯ с тихого перечитывания файла -- магазин
  всегда стартует с состояния диска, перетирание чужих правок исключено
  (последовательное редактирование в любом числе инстансов безопасно).
- ДИАГНОСТИКА на канве: внизу слева "entries:N" -- число записей расклада.
  Подвинул элемент -> N выросло -> запись прошла. В меню F2 добавлен
  "DEBUG: PUT PROBE" -- пишет тестовую запись __probe МИМО жеста: если probe
  в файле появился, конвейер записи исправен и вопрос только в жесте.

## 1.6.54 -- FIX: пачка пишется ЦЕЛИКОМ, микросдвиг не ресайзит
- Ваш JSON с 4 записями (bpm/redo/sync/undo) показал: конвейер записи работает.
  Проблема была в учёте пачки: если жест начинался на элементе МИМО выделения
  (хоть на пиксель), выделение схлопывалось в него -- двигался/писался один.
- Теперь mouseUp пишет ВСЁ АКТИВНОЕ ВЫДЕЛЕНИЕ (селекция = источник записи),
  а не книгу жеста. Нажал мимо -- визуально видно: голубые рамки снимаются.
- SHIFT-клик с микросдвигом мыши (<3px) больше не ресайзит всех случайно.

## 1.6.55 -- WROTE-телеметрия + COPY JSON первой кнопкой меню
- После КАЖДОГО жеста на канве (внизу) 1.5 сек висит "WROTE <id> [+N]
  entries:M" -- что записано, сколько элементов пачкой, всего записей.
  Это закрывает спор "записалось/не записалось" фактом на экране.
- Меню F2: COPY JSON -- ПЕРВАЯ кнопка и на Enter (координаты пачкой в буфер
  для вставки в JSON). Старое подменю заметок из меню убрано.

## 1.7.0 -- большой апдейт: лэйаут-багфиксы + MSEG/ARP по JSON + рейты Sylenth1
- LAYOUT EDIT: в меню F2 вернулся COPY COORDS (x,y из-под курсора мыши).
- ПИН мини-окон: фикс -- setAlwaysOnTop на дочернем компоненте в JUCE поднимал
  РОДИТЕЛЯ, а не панель; теперь панель честно toFront (надписи не пропадают).
- ARP: lane -- заводская из JSON пользователя (21,87,634,217); ARP-ряд
  (галка/кнопка/GATE) -- из JSON (471,57/507,59/624,58); имя машины -- шрифт 0.9.
- HOLD в лейне: ПРОСТОЙ КЛИК всегда ставит галку (драг вверх/вниз ставит/стирает
  как было, ПКМ -- стёрка).
- RANDOM: Page random -- бинарный параметр OFF|ON (галка больше НЕ слетает при
  выходе из страницы ARP: toggle писал 1 из 0..100). Переименование честное:
  эта галка = случайная страница на проход (в движке так и реализовано).
  STEP RND (случайный порядок степов) -- отдельная движковая фича, следующая
  партия.
- ARP RATE: 19 значений от меньшего к большему, как Sylenth1:
  1/1 1/2D 1/1T 1/2 1/4D 1/2T 1/4 1/8D 1/4T 1/8 1/16D 1/8T 1/16 1/32D 1/16T 1/32
  1/64D 1/32T 1/64. Дефолт 1/16.
- MSEG: заводская раскладка ряда -- из JSON пользователя (shape/pg0/steps/grid/
  link/viewgrid/shift/loop/sync/tools). Подписи SHIFT и значение едет с ручкой,
  "N / 32 POINTS" крупнее, описание ROUTING / SIGNED AMOUNT удалено.
- Следующая партия (движковые фичи): P-LOCK + SLIDE p-lock, папка ARP в DEST
  матрицы (arp rate/gate), STEP RND (порядок степов), ретриг MSEG от MIDI/ARP,
  редактирование размеров окна MSEG.

## 1.7.1 -- P-LOCK + SLIDE, STEP RND, папка ARP в DEST, RETRIG, ресайз MSEG
- P-LOCK (Elektron-style "параметр лок"), ARP-страница: кнопка P-LOCK (прицел:
  ЛКМ -- прицел, клик по ручке лицевой панели = цель слота; ПКМ -- список
  параметров: занятые слоты сверху с индикацией, остальные по папкам матрицы;
  повторный выбор слота -- снова показывает его строку). 4 слота P1..P4,
  мини-крутилка дестинейшена (цели те же, что в матрице DEST: драг = листать,
  клик = список, ПКМ = очистить), кнопка полярности UNI/BIP как в матрице.
  Линия значений под лейном -- рисуется как velocity (1..127, 0 = без лока,
  центр 64 = нейтраль BIP); бинд сразу назначает степы страницы текущим
  значением цели. SLIDE: флажок на локнутом степе = диагональ от этого
  значения до следующего p-lock, тот останавливает слайд и играет обычно.
  Без лока на степе параметр играет обычно. Рандом-секция: PLK и SLIDE --
  ЛКМ = текущая страница, ПКМ = все 16. P-LOCK хранится в состоянии плагина.
- STEP RND (новая галка рядом с PAGE RND): 8 степов страницы играются в
  случайном ПОРЯДКЕ (мешок пересобирается каждый проход). PAGE RND -- случайная
  СТРАНИЦА, STEP RND -- случайный ПОРЯДОК. ФИКС движка: в 1.7.0 параметр стал
  0..1, но процессор делил на 100 -- галка PAGE RND доигрывалась как OFF.
- DEST матрица: папка ARP -- ARP RATE (64) и ARP GATE (65) как цели маршрутов
  (и в матрице, и в строках MSEG, и в списке P-LOCK). RATE модулируется
  октавами (+-2 на полной глубине), GATE -- прибавка к длине ноты 1..127.
- Owed-фикс: кламп прицела addRouteFromSource/addMsegRoute 0..55 -> 0..63
  (прицел не доставал MSEG OUT 56..63).
- RETRIG (галка на странице MSEG): перезапуск фаз MSEG от входящего MIDI или
  арпа. Дефолт ON = как всегда себя вела; OFF = кривые бегут свободно.
- Окно MSEG редактируемого размера: грип справа внизу (1000..1800 x 700..1300),
  граф тянется от заводского 1170x800, размер хранится в layout.json (msegsize).
- Матрица дестинейшенов MSEG теперь ДВИГАЕТСЯ в LAYOUT EDIT: тащи тег rownames
  -- гнёзда прицела, бары ампаунта, проценты и крестики стирания едут вместе.
- Скролл списков MSEG (SHAPE/GRID/VIEW и прочих DragChoice/WheelCombo) смягчён:
  накопление дельты колеса (~шаг за нотч), тейлпад/скролл больше не улетает.
- Следующее: P-LOCK сохранение в пресеты хоста проверено; дорожка -- autotune
  поведения UNI на целях с малым максимумом (SYNT), если понадобится.

## 1.7.2 -- по ответам: RETRIG постранично, P-LOCK слоты безгранично
- RETRIG: 8 ПОСТРАНИЧНЫХ галок (как LOOP/SYNC -- у каждой страницы своя, бинд
  перезапускает фазу только этой страницы). Параметры mseg_retrig/mseg2..8_retrig,
  дефолт ON = прежнее поведение. Старая общая галка 1.7.1 читается как галка
  первой страницы (совместимость сохранений).
- P-LOCK: слоты БЕЗГРАНИЧНЫ (32, играют ВСЕ одновременно). Лейн один; кнопка S#
  и ПКМ на P-LOCK открывают список занятых слотов -- выбор показывает его строку;
  "-- new empty slot --" готовит чистый. Прицел: та же ручка = тот же слот,
  другая ручка = новый слот (plockAcquire). Пустые слоты в состояние не пишутся.
- UNI-нормировка P-LOCK: перепроверено -- УЖЕ есть (UNI масштабируется
  максимумом SYNT-ручки, остальные цели 0..127 родные). Не менялось.
- Скролл списка волн SHAPE (CUSTOM/FLAT/RISE/...): колесо смягчено в 1.7.1
  (накопление дельты), отдельных действий не потребовалось.

## 1.7.3 -- P-LOCK в UNDO/REDO + критичный фикс сохранения 1.7.2
- ФИКС 1.7.2: комментарий внутри строки сериализации съел n.setProperty("target"/"pol")
  -- цели и полярность слотов P-LOCK НЕ сохранялись в состояние плагина. Исправлено:
  сериализация вынесена в helper plockToTree(), там target/pol пишутся честно.
- UNDO/REDO плагина теперь откатывает P-LOCK целиком: каждый undo-снимок несёт
  дерево PLOCK (plockToTree), applyTree восстанавливает слоты (plockFromTree;
  слот без записи в снимке = пустой). Точки отката ставятся на: правку линии
  P-LOCK, смену цели крутилкой/списком, очистку слота, прицел-бинд.
- plockStep пересчитывает активные локи с нуля каждый степ: снятый или
  откатившийся слот отпускает цель сразу (раньше осталась бы замороженная
  модуляция до смены режима/паники).

## 1.7.4 -- два фикса по репорту "ручки не крутятся, RETRIG не видно"
- ФИКС (критичный): после прицела P-LOCK все ручки лицевой панели оставались в
  режиме выбора цели (клики шли в бинд, драг и ПКМ игнорировались) -- ветка
  P-LOCK в completeTargetPick не снимала setPickMode с ячеек. Теперь режим
  прицела снимается сразу после бинда -- ручки крутятся, ПКМ работает.
- ФИКС: галка RETRIG страницы MSEG была невидима -- стояла на (150,70), ровно
  ПОД комбой SHAPE. Перенесена на свободное место (600,70) -- правее RATE,
  левее подписи SHIFT.
- Страховка: в getStateInformation PLOCK-ребёнок удаляется перед добавлением
  (после undo в parameters.state оставался старый PLOCK -- получался дубль).

## 1.7.5 -- большой батч: PITCH/ROUTE-цели, P-LIST слоты, прицел из матрицы, пакет мелких фиксов
- НОВОЕ: PITCH -- высота тона как цель маршрутов (папка ARP в DEST-списках: матрица, прицелы
  MSEG, P-LIST). Полная глубина = ±24 полутона (UNI одна сторона, BI обе).
- НОВОЕ: ROUTE 1..16 -- цель "глубина другой строки матрицы" (matrix>dest): источник строки
  прибавляется к глубине строки-цели (кламп -64..63). В списке DEST папка MATRIX показывает
  «N - что модулирует», DEST-колонка -- то же динамически.
- НОВОЕ: прицел из матрицы (ПКМ): ПКМ по кресту строки тянет патчкорд -- на месте прицела
  «дырка»-гнездо, провод как у LFO-джека, матрица остаётся открытой. Drop на DEPTH или
  AUX DEPTH другой строки = назначить её глубину (dest = ROUTE этой строки); ячейка под
  курсором подсвечивается белой рамкой; после drop -- обычная логика назначения: новый
  маршрут всплывает наверх, мигает рамка DEST-ячейки. ЛКМ-прицел (на ручки лицевой панели)
  не тронут.
- НОВОЕ: кнопка слотов P-LOCK теперь «P-LIST» вместо «S1»: вертикальный драг кнопки листает
  слоты (список после драга не открывается), клик или ПКМ = список слотов «N - имя параметра».
  Слоты безграничны, играют все, в редактор вызывается выбранный.
- НОВОЕ: RESET ALL сбрасывает и P-LOCK (все слоты в ноль), с точкой отката UNDO.
- НОВОЕ: DETACH ARP -- ARP открывается отдельным окном (кнопка DETACH в правом углу
  ARP-страницы; повторный клик возвращает страницу на место), лицевая панель остаётся рабочей.
- НОВОЕ: MSEG UNASSIGNED -- клик по строке без маршрута открывает список целей (папки как в
  P-LIST: MACHINE..LFO3, MSEG OUT, ARP, MATRIX), выбор создаёт маршрут.
- ФИКС: галка RETRIG страницы MSEG видна на всех страницах -- (920,70), обе старые точки
  (150 и 600) перекрывались соседними контролами.
- SLIDE: ЛКМ только клик-тумблер (драга нет), ПКМ стирает только свой ряд; рисование P-LOCK
  не трогает слайды и наоборот; слайд-ряд = высота HOLD (26px). Хитбоксы рисования клампятся
  к 127/0 за границей (как VEL/PITCH).
- Кнопки рандома PLK/SLIDE работают при пустом текущем слоте (фолбэк на первый занятый).
- PAGE RND рандомит страницы в пределах лимита (раньше rng%16 игнорировал STEP PAGE LIMIT;
  STEP RND не тронут).
- Matrix: хитбокс ресайза колонки = видимая линия разделителя, драг от текущего положения,
  минимумы ширины (текст колонок не схлопывается); нумерация строк 1..16 перед ON.
- MSEG: теги прицелов строк = «rowheads» в layout-редакторе (хитбокс+выделение); SHIFT-
  сортировка стабильна; ручка DEG: точки сортируются после поворота (кривая не переворачивается).
- Envelope: ПКМ переключает малое/большое окно в обоих режимах (ПКМ-кручения нет); пин не
  прячет текст мини-окон (дети поднимаются выше).
- Зумленный layout: СКМ+драг = пан (перемещение картинки).
- SYNC/RATE ARP не мигают при входе -- repaint кнопки только при изменении текста.
- RAND DRAW 1/2 -- шрифт 16 как у остальных кнопок форм; DSND -- компактное окно 238x90;
  FM+PAR -- машина по умолчанию.

## 1.7.6 -- КРИТИЧНО: пропавший звук + пакет фиксов по репорту 1.7.5
- КРИТИЧНО: в 1.7.5 хвостовой комментарий съел вызов matrix.evaluate в блоке
  обработки -- модуляция не считалась, параметры машин оставались нулями =
  тишина (клик только при смене машины). Вызов восстановлен. Тот же класс
  ошибки, что в 1.7.2 (съеденные setProperty) -- правило «old брать полной
  строкой» снова подтверждено.
- КРИТИЧНО (латентный): ROUTE-цели (67..82) во втором проходе evaluate
  попадали в ветку arpDests[id-64] -- запись за границей массива (порча
  стека при использовании matrix>dest). Теперь ROUTE-цели завершаются в
  pass 1, во второй не попадают.
- Envelope: ПКМ из большого окна теперь ЗАКРЫВАЕТ большое и открывает малое
  на его обычном месте лицевой панели (а не «вписывает» малое в большое);
  ПКМ из малого открывает большое. В обе стороны, отложенно (callAsync) --
  без удаления компонента в собственном mouseDown.
- LFO LOCKS: LFO3 показывала мусорные имена (TOM/CLAP/RIM/COW) -- таблица
  имён читалась со сдвигом на одну строку, для страницы LFO3 чтение шло ЗА
  таблицей (соседние статические строки). Теперь имена = ручки страницы,
  выбранной в PAGE данного LFO -- функция идентична LFO1/2. Ручки машин
  (включая DPRO-BBOX: PTCH/STRT/RTRG/RTIM) модулируемы: матрица цели SYN 1..8
  = слоты 0..7 текущей машины, движок получает модулированный массив.
- Matrix: в режиме ПКМ-патчкорда колесо мыши листает матрицу -- режим не
  отменяется, параметр под курсором не крутится.
- ARP: галки AUTO SWAP PAGE и FOLLOW PLAY больше не отжимаются при закрытии
  окна/страницы -- состояние хранится в процессоре и восстанавливается.
- DETACH ARP: окно = ТОТ ЖЕ класс страницы (копия раскладки, не новая
  компоновка), LookAndFeel мода -- заголовок чёрный, MODE: STEP и списки/галки
  не джус-дефолт; крестик окна работает (закрывает окно); исходная страница
  ARP при детаче закрывается -- ручки лицевой панели свободны; кнопка ARP на
  панели показывает окно, не создавая страницу заново; кнопка DETACH --
  иконка «квадратик со стрелкой», поднята выше.
- MSEG: маркер PLAY FOLLOWER -- белая черта с точкой показывает, где сейчас
  играет MSEG (фазы пробрасываются из аудио-потока).
- MSEG прицел: ЛКМ по гнезду строки = прицел как раньше (клик по ручке
  создаёт маршрут), ПКМ по гнезду UNASSIGNED-строки = список целей; гнёзда
  списка больше не перехватывают занятые строки.
- PLK/SLIDE рандомы: пишут в ПОКАЗЫВАЕМУЮ страницу (учтён FOLLOW PLAY), слот
  после фолбэка на первый занятый сразу виден в линии P-LOCK.
- PAGE RND: возвращено старое поведение работавшего STEP RND -- случайная
  страница из ВСЕХ 16 на каждом проходе; PAGE LIMIT остаётся длиной цикла
  AUTO SWAP (ранее 1.7.5 ограничивал выбор лимитом -- при лимите 1 был мёртв).
- Мелочь: setCurSlot больше не переименовывает кнопку слотов обратно в S1
  (она теперь P-LIST).

## 1.7.7 -- патч 1/2: PAGE RND (шаги с питчем), матрица 64, MSEG 256 + WAV, RETRIG-режимы, детач, маркер
- PAGE RND переписан ПО ТРЕТЬЕМУ разу по требованию: рандомится СОДЕРЖИМОЕ
  шагов страницы каждый НОВЫЙ ПРОХОД (transient-паттерн rndSteps[8]: транспон
  +-1 октава, velocity 1..127, hold 25%), а не выбирается случайная страница
  (реализации 1.7.5 span и 1.7.6 rng%16 -- обе отменены пользователем).
  Параметры пользователя не пишутся; StepChord играет случайный шаг аккордом.
- Матрица модуляции: 16 -> 64 слота (ROUTE 1..64, цели 67..130, DEST-кламп
  0..131). Все слоты ON по умолчанию (schema<12 миграция: недостающие r*_on
  создаются = 1). evaluate получил 5-й аргумент routeExtra[64] -- сумма
  модуляций не изменилась (звук не тронут). UI матрицы, CLEAR, прицелы,
  MATRIX-папки и таблица имён -- на 64 строки.
- MSEG: лимит точек 32 -> 256 (bakeSteps/STEPS дорисовывают до конца).
  RETRIG-галка -> СПИСОК RETRIG|ENVELOPE|FREE (default RETRIG; ENVELOPE =
  один проход и HOLD последней точки без wrap; FREE = без рестарта).
  Миграция schema 11->12: >0.5 -> RETRIG(0), иначе -> FREE(2).
- MSEG: сетка GRID/VIEW как у ARP -- 20 пунктов (OFF,1/1..1/64 с dotted и
  triplet, den-таблица gridDenF), дефолт 1/16; ПКМ STEPS = toggle режима
  (запекание по ПКМ убрано).
- MSEG: WAV drag&drop -- .wav/.aiff кидаем на граф: читается один период,
  нормируется по пику, разворачивается в до 256 точек (вейвтейбл из Serum).
- MSEG: Ctrl+клик = выделение точек (белые рамки), ПКМ по любой выделенной
  стирает ВСЕ выделенные сразу.
- MSEG: маркер PLAY FOLLOWER получил настройки под маршрутами: делитель
  скорости 1:1/1:2/1:4/1:8, FRZ (заморозка), OFF. База -- фазы из 1.7.6.
- ARP: PAGE рандомит ВСЁ на видимой странице (транспоз+вел+холд+слайды и
  P-LOCK). P-LIST кнопка показывает ИМЯ ТЕКУЩЕГО параметра слота (живёт).
- DETACH ARP: окно БЕЗ заголовка, ПОВЕРХ всех окон (always-on-top), DETACH
  работает как toggle: повторное нажатие (или кнопка ARP) ВОЗВРАЩАЕТ страницу
  в синх. Крестик/иконка окна не нужны.
- HOST: фронт play хоста (transport start) ресетит фазы всех 8 MSEG-страниц
  (сейчас безусловно, галка HOST -- в патче 2/1.7.8).
- Версии -> 1.7.7; verify-якоря обновлены под 64/131/schema12 + секция 1.7.7.
- FX-дерево: cp-sync 9 файлов (впервые включая DspModes.hpp -- schema 12),
  fsyntax 0 ошибок, verify PASS в обоих деревьях.

## 1.8.1 -- попапы у мышки + замки на все 21 страницу + INP -64..+63 + FX: хорус на P1 + аудит матрицы
- **MSEG RATE -- новая цель модуляции (236..243 = MSEG1..8 RATE)** по вопросу
  юзера «рейт мсегов добавлен для модуляций?»: был только legacy LFO FM (131,
  вне списков). Теперь: evaluate -- 12-й аргумент msegRateDests, ветка >=236
  (как LFO FM: сумма в октавах, 2^x до ~20 kHz); P-LOCK -- цель 236..243
  доступна из P-LIST (папка «MSEG RATE»), шаг 64 = +-2 октавы (ABS: центр 64);
  драг DEST в матрице до 244; destName/targetName -- «MSEGn RATE»; plock-массивы
  244; plockSetTarget до 243; клампы маршрутов 244.
- **Единый RATE MSEG -- проверен (был готов с 1.7.8)**: одна ручка mseg_rate,
  RMODE = семья шкалы (TEMPO/TRIPLET/DOTTED -- тактовые доли от BPM; TIME --
  1ms..60s; HZ -- 0.001..20kHz), BPM входит в темповую ветвь живо.
- **P-LOCK и MSEG OUT/ARP перепроверены**: p-lock целятся в MSEG1..8 OUT
  (56..63 -- офсеты кривых) и ARP RATE/GATE (64/65 -- arp.setMod), все цели
  0..243 играют. FIX в довесок: диспетчер SOURCE (c==2) в матрице был на
  старом 26-элементном vals -- выбор P2 LFO писал чужой источник.


- **Попапы спавнились далеко от мышки** (LFO на обеих страницах P1/P2 -- и не
  только они): позиция списка считалась как `pageIndex%3`, а pageIndex -- это
  НОМЕР СТРАНИЦЫ (0=AMP, 1=FILT, 2=EFFX, 3..8/9..14 = LFO), не колонка панели.
  Колонка теперь считается по геометрии ячейки `(getX()-75)/399` -- список
  всегда при своей панели (LFO/MSEG -- правая, AMP -- средняя, FILT -- левая
  нижняя и т.д.). Заодно починились AMP/FILT/EFFX и MSEG.
- **LfoLockPanel: видны ВСЕ 21 страница** (юзер: «все параметры -- для лока/
  анлока, исключения из модуляции»): три колонки -- PAGE 0..10 | PAGE 11..20 |
  DEST 0..7, окно 705x420. Маска page_locks -- 21 бит (0..2097151), PAGE solo
  0..21 (параметры + панель + DSP-кламп). FIX: otherLocksBlock -- чужие замки
  среди ВСЕХ 12 LFO (протухший цикл y<3 видел только lfo1..3); префиксы
  p2lfoN_ для P2. Попап PAGE: чужие замки -- пункт недоступен (как в плоском).
- **INP (INPUT) всех FX-машин -- биполярный показ** как на железе: 64 = 0,
  диапазон -64..+63 (было 0..127 с дефолтом 64). ЗВУК НЕ МЕНЯЛСЯ -- только
  имя значения (юзер: «по звуку одинаково, наименование неверное»).
- **FX-версия VST: панель хоруса юзера -- на P1** (страница открывается
  первой; P2 = запас под доп-эффект): дефолт P1 = FX-CHORUS, руки m15_* =
  kChorusDef (70/95/41/68/127/127/127/127). В synth-версии всё как было
  (дефолт FM+ PAR, руки хоруса не тронуты). P2 (дефолт CHORUS + kChorusDef)
  сохранён в обеих версиях.

- **Аудит матрицы (просьба юзера «замки перепроверь, все параметры есть, отсортировано»)
  нашёл и закрыл 7 проводных дыр эпохи 1.8.0/1.8.0e:**
  1) кламп источников маршрутов 0..25 -> **0..31** -- P2 LFO1-6 как SOURCE молча
     играли сигналом LFO6 (и configureRouting, и addRouteFromSource/прицел);
  2) кламп AUX 0..18 -> **0..32** -- AUX = LFO4-6/P2 LFO играл как RANDOM;
  3) спек r_aux 0..26 -> **0..32** -- значения P2 LFO не влезали в параметр;
  4) AUX-меню матрицы: список vals был 26-элементным (до P2 LFO) -- папка
     «P2 LFO» подставляла ЧУЖИЕ источники, а ARP/MSEG/MACRO читали vals[26..31]
     ЗА границей массива; теперь полный 32-источниковый порядок;
  5) колонка AUX SRC в таблице: кламп показа 0..18 -> 0..32 (LFO4-6/P2 LFO
     показывались как RANDOM), драг ячейки 0..18 -> 0..32;
  6) DEST-список матрицы: папки строк LFO4-6 были продублированы под именами
     LFO1/LFO2/LFO3 -- теперь LFO4/LFO5/LFO6;
  7) **сортировка и перетаскивание строк не переносили lock** (замок оставался
     в слоте, маршрут уезжал) -- lock добавлен в moveModRoute/sortModRoutes
     (8 полей, вместе с aux_depth).
  Замки матрицы (FREE/LOCK/SOLO, кисть, ПКМ-SOLO, CLEAR -> ONLY LOCKS,
  защита слотов от прицела в addRouteFromSource) перепроверены -- работают.

### 1.8.3 -- пауза хоруса без устаревшего буфера + версия 1.8.3
- Версия/папка/тар -> 1.8.3 (CMake VERSION x2, verify-игла VERSION); маркеры
  «1.8.1/1.8.1b/1.8.1c» в коде сохранены как история.
- **Простой хоруса -- ТОЛЬКО по галке в MENU: «CHORUS IDLE (MIX=0, SAVES
  CPU)», по умолчанию ВЫКЛ** (решение юзера: аутентичный звук -- хорус живёт
  непрерывно, «выплывает с хвостами предыдущих объектов»; skip когда-либо
  меняет это поведение). Режимы прогнаны на бенче: idle=OFF 9.4% (как было
  всегда), idle=ON 2.5%. Очистка буферов при входе в паузу УБРАНА -- при
  включённой галке буфер замораживается с хвостами (1.8.1b), ничего «как
  нового» не звучит.
- ASAN-стенд: FX-«overflow в конструкторе» оказался артефактом протухшего
  obj/main.o стенда (старый sizeof процессора; объекты надо пересобирать при
  смене PluginProcessor.h) -- после пересборки оба дерева HEADLESS OK.
- Бенч 1.8.3: default 2.1%, loaded 2.1% (без деградации).

### 1.8.1c -- p-lock MSEG RATE работал неверно (постоянные -2 октавы на UNI)
- plockScaled() не имел ветки 236..243: цель проваливалась в MSEG OUT
  (масштаб 0..1), а rate-цикл ждёт СЫРОЕ значение степа (ABS центр 64,
  REL /32). Итог был «тихий no-op хуже»: UNI p-lock MSEG RATE давал
  ПОСТОЯННЫЕ -2 октавы при любом значении степа.
- Фикс: plockScaled -- t>=236 возвращает сырое v; plockFillPage -- ABS-бинд
  MSEG RATE = нейтраль 64 (было 1 -> сразу -2 октавы после бинда).
- Проверки: fsyntax 0 x4; verify PASS x2; check_comments 0 x2; md5-синк PP.cpp
  в FX; ASAN HEADLESS OK; бенч default 2.0% (не ухудшился).

### 1.8.1b -- CPU: -7% (хорус MIX=0 не рендерится) + cord LFO4-6/P2 + AUX-имена + папки P2 LFO + хорус MIX=0/INP=64
- **CPU 12% -> ~5%**: gprof-бенч (headless -O2) показал ~42% времени -- ChorusCore
  (P2-хорус по умолчанию с 1.8.0b молотил ВСЕГДА, даже при MIX=0: 190 млн
  операций буфера/сек). Теперь: CHORUS с MIX=0 (эфф., с учётом модуляций)
  не рендерится -- P2-вставка идёт прозрачным проходом (как THRU), в FX-версии
  то же для P1-хоруса. Звук идентичен (wet*0=0); при поднятии MIX хорус
  включается сам. Дополнительно: arp-степы -- кэш указателей (snapshot раньше
  строил ~900 juce::String на КАЖДЫЙ блок). Бенч: default 8.9%->2.0%,
  loaded 9.4%->2.2% (у юзера ~12% -> ~5%).
- **Cord от LFO4/5/6 возвращал на страницу LFO3 (P1 и P2)**: устаревшие клампы
  jlimit(0,2,...) в endModDrag (дроп на ручку, клик, откат) и holdLfoPage --
  теперь 0..5; откат PAGE/DEST при промахе работал только для P1 LFO1-3 --
  распространён на LFO4-6 и P2 (хелперы modSrcSel/modSrcPage: 4..6/23..25/26..31
  -> 0..5, страницы P1 3..8 / P2 9..14).
- **AUX SOURCE: LFO писались волнами вместо номеров** -- auxNames в openAuxMenu
  был 24 элемента, источники 24..32 читали за границей (мусор-волны); дописаны
  LFO4-6 + P2 LFO1-6 -> 33 (в колонке матрицы массив уже был 33).
- **Матрица DEST + P-LIST: «P2 LFO» был один плоский список 48 строк** --
  разбит на подпапки P2 LFO1..6; между группами LFO добавлены лёгкие
  визуальные разделители PopupMenu (не пункты меню -- багов с пустыми
  строками, как в старом меню, не будет).
- **Хорус: MIX по умолчанию 0** (юзер подмешивает сам; было 68/127 в kChorusDef
  и 127 в машине), **INP = 64 (центр)**. kChorusDef={70,95,41,0,127,127,127,64};
  verify-игла обновлена.
- Проверки: fsyntax 0 x4; verify PASS x2; check_comments 0 x2; md5-синк 5
  файлов в FX; ASAN-сценарий загрузки HEADLESS OK; бенч gprof сохранён
  (batches/hdtest/build_bench.sh + main_bench.cpp: bench|asan).

### 1.8.1a -- FIX краша при загрузке плагина на дорожку (+ PAGE максимум 20)
- **Краш воспроизведён на headless ASAN-стенде** (ctor -> prepare 44100/512 ->
  20 блоков с noteOn -> get/setStateInformation -> createEditor/delete):
  SEGV strlen(NULL) в juce::String+char* из nova::specs() -- первый же кадр
  конструктора процессора.
- **Причина**: pageLabel(): static const char* labels[15][8] -- в инициализаторе
  было только 13 рядов (10 PAGE-рядов вместо 12); страницы 13/14 (P2 LFO4/5)
  давали labels[13][i]/labels[14][i] == nullptr (zero-init статики);
  juce::String(...)+nullptr -> appendCharPointer(NULL) -> SEGV. Мина сидела
  и в сданном таре 1.8.1 (потеря 2 рядов при редактировании 1.8.0e).
- **Фикс**: дописаны 2 ряда {"PAGE","DEST","TRIG","WAVE","MULT","SPD","INTL",
  "DPTH"} -> 15/15; аудит остальных статических таблиц -- других частичных
  инициализаторов нет.
- **Фикс (функц.)**: клампы maximum для LFO-страниц (PP.cpp:127 snapshot и
  аудио-копия :284) -- PAGE (i/k==0) режется до 7 -> чужие папки 11..20
  (1.8.0g) не доходили до LFO-движка; теперь i==0?20.0f:i==1?7.0f... и
  k-вариант (маркер «1.8.1 FIX: PAGE»). Страницы 8..20 снова доступны.
- Проверки: fsyntax 0 x4; verify PASS x2; check_comments 0 x2; md5-синк
  NovaData.h+PluginProcessor.cpp в FX; ASAN-сценарий загрузки -- HEADLESS OK
  в ОБОИХ деревьях (Synth и FX).

## 1.8.0g -- видимость 12 LFO: единое PAGE-пространство 0..20 + папки

Все 6 LFO семьи видят структуру друг друга ровно как старые P1 LFO1-3.

- **PAGE теперь 21 значение** (было 8): 0=PTCH, 1..4 = SYNT/AMP/FILT/EFFX
  своей машины, 5..10 = свои LFO1..6, **11..20 = чужая страница ОДНОЙ папкой**
  (P1 видит «P2 SYNT|P2 AMP|P2 FILT|P2 EFFX|P2 LFO1..6», P2 -- зеркально «P1 ...»).
  PTCH в папке нет (он общий, page 0) -- как у старого P1 LFO1-3.
- **DSP: три routing-блока (P1 LFO1-3 / P1 LFO4-6 / P2) слиты в один цикл** с
  таблицей `page -> (after/after2/lfo1b/lfo2/lfo2b)`. Старое наивное расширение
  давало after[56..79] OOB и недостижимые страницы -- теперь все 21 значение
  легальны для всех 12 движков, без OOB.
- **Попап PAGE с папками** (просьба юзера: «визуально чище»): PTCH..EFFX
  плоско, свои LFO1..6 -- подменю «LFO» (у P2 -- «P2 LFO»), чужая страница --
  подменю «P2»/«P1». **ЛКМ-драг по ячейке остался плоским** (листает 0..20).
- choiceName (DEST-строки таблицы), aimLfoDest (прицел P-LOCK) и LfoLockPanel
  переведены на ту же формулу 0..20 -- прицел ведёт в любую страницу, включая
  папку чужой семьи; имена в панели замков совпадают с таблицей.
- Замки PAGE (маска 8 бит, значения 0..7) действуют на свои страницы
  PTCH..LFO3; папка 11..20 и строки 8..10 не замкаются (маска байт) --
  по умолчанию всё открыто, лишнее юзер отключает замочком.
- Имена значений PAGE в NovaData: у P1 LFO и P2 LFO -- зеркальные 21-значные
  списки (южная папка в конце).

## 1.8.0f -- добивка проводки 12 LFO (аудит)

- **КРИТИЧНО: lfoLockRaw[24] индексировался до 47** (runtime-цикл уже 12 LFO) --
  чтение за границей массива указателей КАЖДЫЙ блок (UB/риск краша). Массив 48,
  загрузка 12 семейств: P1 = lfo1..6_*, P2 = НОВЫЕ параметры p2lfo1..6_*
  (page_locks/dest_locks/page_solo/dest_solo, 24 спеки в NovaData).
- LfoLockPanel: id() замков по семейству (lfo<6 -> lfoN_, иначе p2lfoN_) -- панель
  замков PAGE/DEST теперь работает для всех 12; Cell::lfoLockAxis -- на всех
  LFO-страницах (3..14); lfoTargetBlocked -- чужие замки/соло среди всех 12.
- lfoLastPage/lfoLastDest init: 12.
- destName FIX: value 131 = ROUTE 64 (раньше лезла "LFO FM" -- off-by-one),
  132 = legacy LFO FM, 133+ = P2/LFO-строки; кламп имён в ROUTE-строках 235.

Проверки: fsyntax 0 x4; verify v18 PASS 1073 x2 (+6 игл); check_comments 0;
Editor 416137 синхронно.

## 1.8.0e -- 12 LFO (P1 1..6 + P2 1..6) -- по ответам юзера (ряд + префикс P2)

**Архитектура**: 12 полноценных движков nova::LFO (было 6): P1 = lfos+lfos3
(страницы 3..8), P2 = lfos2+lfos4 (страницы 9..14, параметры p9_..p14_ новые).
effectiveLfoParams/previousLfo/locks -- [12]. Каждая страница LFO = та же
структура, что LFO1 P1: PTCH|SYNT|AMP|FILT|EFFX|LFO x3 (своя тройка: 1-3 и 4-6
модулируют друг друга). PTCH = вибрато питча (ступени 1ST..3OCT) на всех 12.

**Цели-строки матрицы**: P1 LFO1-3 = 32..55 (после), P2 LFO1-3 = 164..187 (lfo2),
P1 LFO4-6 = 188..211 (lfo1b, НОВОЕ пространство), P2 LFO4-6 = 212..235 (lfo2b).
DEST-список 0..236; evaluate: ветки 212/188 до 164; базис всех строк -- в матрицу
(маршруты на любые LFO-параметры). Источники: LFO4-6 = P1 (23..25), +P2 LFO1-6
(26..31, ModSource::P2LFO1..6), m_lfoOutputs[12].

**UI**: ряд кнопок 1 2 3 4 5 6 (w30, x975..1170) + ОДИН корд (lfoJack, 1174) на
ОБОИХ страницах; корд = источник выбранной страницы (P1: 4..6/23..25, P2:
26..31); ведение по ряду и драг-выбор переключают 6 страниц; отпускание корда на
кнопке = строка той кнопки целью; aimLfoDest переписан (12-страничная математика
PAGE/DEST). LfoLockPanel -- 12 страниц. Списки: SOURCE/AUX папка "LFO" = LFO1-6,
НОВАЯ папка "P2 LFO" = P2 LFO1-6 (имена с префиксом P2 -- выбор юзера); DEST
матрицы: папки LFO4/5/6 + "P2 LFO"; P-LIST: те же папки (id 400+цель);
MSEG-список целей расширен; targetNames() 132 -> 236 (фикс потенциального OOB
в отрисовке маршрутов MSEG). aimBtn/P-LIST/PLK-SLIDE из 1.8.0c не тронуты.

Проверки: fsyntax 0 x4; verify v18 PASS 1067 x2 (+12 игл 1.8.0e); check_comments 0;
Editor 415936 синхронно. Старые сохранённые состояния: параметры P2 LFO1-3
переехали со страниц 6..8 на 9..11 (6..8 теперь P1 LFO4-6) -- берут дефолты
(миграции нет, по правилу).

## 1.8.0d -- репорт #2 (LFO FM / P2 LFO списки / находки-попутки)

1. **LFO FM убран из всех списков**: слот целей 132 теперь "--" (нумерация значений
   P2 сохранена -- старые маршруты не съехали); в P-LIST удалён ранее (1.8.0c);
   в меню DEST его и не было. Сама цель в DSP осталась (работает только для
   старых маршрутов, новых не создать).
2. **КРИТИЧЕСКИЙ фикс маршрутов: LFO FM глотал P2.** В evaluate ветка
   `targetParamId >= 131` стояла ПЕРВОЙ -- все маршруты на P2 (132..163) уходили в
   неё и молча суммировались в частоту LFO. P2-ветка сумм2 была ниже и
   недостижима, а после наивного фикса писала бы за границей arpDests[68].
   Теперь: LFO FM строго == 131; ROUTE-скип только 67..130; P2-ветка поднята
   ВЫШЕ арп-ветки. Маршруты матрицы на P2 впервые реально работают.
3. **Списки P2 LFO = копия рабочих списков LFO1 P1** (просьба юзера):
   PAGE = PTCH|SYNT|AMP|FILT|EFFX|LFO4|LFO5|LFO6. PTCH = вибрато питча (те же
   ступени 1ST..3OCT), SYNT = руки машины P2, AMP/FILT/EFFX = группы P2,
   LFO4-6 модулируют друг друга как LFO1-3. DSP-маппинг страниц переписан
   соответственно. Имена в списках/замках = targetName (P2 MIX на слоте 7 и т.д.).
4. **Цели LFO4-6 перенумерованы 56..79 -> 164..187**: 56..63 = MSEG OUT --
   столкновение пространств (ячейка P2 LFO отчитывалась целью MSEG OUT, маршрут
   на неё писал за границей msegDests[8..23]). NovaData: +24 цели в хвост списка,
   DEST-диапазон 0..188; evaluate: ветка >=164 -> lfo2Dests; PP: lfo2[24]
   (базис lfoParams[3..5] + маршруты), мерж LFO4-6 из lfo2 -- заодно закрыто
   чтение ЗА границей after[56..79] (мусор в параметрах P2 LFO каждый блок!).
   P-LOCK: кламп 211 (значение не портится), runtime скипает 164+.
5. PLK/SLIDE -- фолбэк из 1.8.0c (без слота открывают P-LIST, не молчат).

Проверки: fsyntax 0 x4 (Editor+PP, оба дерева); verify v17 PASS 1056 x2;
check_comments 0; Editor 411866 синхронно.

## 1.8.0c -- репорт по 1.8.0b (3 пункта юзера)

1. **LFO FM удалён из P-LOCK списка** (юзер: «я не просил это») -- пункт убран,
   сама цель матрицы 1.7.9 не тронута.
2. **PLK/SLIDE «мёртвые» кнопки -- корень найден**: при отсутствии хоть одного
   забинженного P-LOCK слота randomizePlock молча выходил (return) -- кнопка
   выглядела мёртвой. Теперь без слота открывается P-LIST для выбора параметра
   (после выбора кнопки работают). Заодно поймано моё же: диспетчер P-LIST брал
   ВСЁ res>=100 как «слот» -- P2-цели (id 142..173) глотались; теперь слоты
   строго 100..131, P2-цели выбираются корректно.
3. **Прицел -- ОТДЕЛЬНАЯ кнопка (aimBtn)** с крестиком правее P-LIST (834,312);
   P-LIST = список: ЛКМ клик -- открыть, ЛКМ драг вверх/вниз -- свапать слоты
   (вертикальный драг как был). Крестик с кнопки P-LIST убран.

Проверки: fsyntax 0 x2; verify v16 PASS 1040 x2 (иглы: aimBtn, P-LIST ЛКМ-список,
PLK/SLIDE showDestList-фолбэк, диспетчер слотов, absent LFO FM); check_comments 0;
Editor 411181 в обоих деревьях.

## 1.8.0b -- раунд #11 (12 пунктов юзера)

1. **Первопричина «на P2 стартует REVERB»**: fxm (isEffect-порядок) = 0=THRU(12),
   1=REVERB(13), 2=CHORUS(15)... -- старый def p2_machine=1 был REVERB. Дефолт
   p2_machine = CHORUS (chorusIdx=2, реверс по юзеру), панель CHORUS с юзер-скрина:
   kChorusDef[8]={70,95,41,68,127,127,127,127} (DEL DEP SPD MIX FB WID LP INP),
   перекрытие заводских дефов только для id==15. THRU остаётся в списке (сухой режим).
2. **targetName для целей 132..163** показывал руки машины P1 под видом P2 -- теперь
   P2 SYN = руки ВЫБРАННОЙ машины P2 (p2FxMachines + p2_machine), секции = P2 AMP /
   P2 FILT / P2 FX (pageLabel 0..2), слот 7 AMP = "P2 MIX". DEST-списки и прицел на
   P2-страницах зовутся P2-именами.
3. **Матрица: SOURCE/AUX** -- в папке LFO добавлены P2 LFO4-6 ("P2 LFO4|P2 LFO5|P2
   LFO6", источники 23..25); vals расширены 23->26, id папок ARP/MSEG/MACRO сдвинуты;
   sourceName кламп 0..25. auxNames заодно починен: было 19 строк -- MSEG4-8 читались
   ЗА границей массива (мусор в AUX-списке), теперь 27. Маршруты в папке MATRIX
   показывают P2-цели (кламп 131 -> 163).
4. **Попапы матрицы у мышки**: DEST/SOURCE-списки открывались намертво на (120,227)
   -- возвращены к курсору (screen.x, screen.y).
5. **LFO-UI (реворк джеков)**: кнопки LFO 1 2 3 (в P2 -- 4 5 6) БЕЗ гнёзд; ОДИН
   патч-корд (lfoJack) рядом -- источник = ВЫБРАННАЯ страница LFO (lfoPageSel
   читается живым в начале и конце драга). Ведение мышью по кнопкам ряда и
   press-and-hold-драг от кнопки переключают страницу на лету (numberDrag).
6. **MSEG**: кнопки-гнёзда модуляций у MSEG удалены (msego1-3 снесены); кривые
   остаются SOURCE в матрице и в редакторе MSEG.
7. **P-LOCK**: (а) plockSetTarget кламп 82 -> 163 -- прицел на LFO FM (131) и P2
   молча портил цель (старший байт целей 1.7.8+); (б) plock-массивы 67 -> 164,
   runtime пускает 132..163, P-LOCK применяется к after2 (машины/AMP/FILT/EFFX/MIX
   P2); (в) P-LOCK список: + LFO FM и папка P2 (руки машины P2, P2 AMP/FILT/EFFX),
   диспетчер пускает 131 и 132..163. Кнопка слота = имя параметра (было и осталось).
8. **Малый режим энвелопа P1/P2**: своя MODE в мини-окне скрыта (осталась одна --
   MODE AMP на лицевой, синхрон та же).
9. **P2 EFFX**: ячейкам вернул цель прицела (bind без ",false") -- обводка ячейки в
   патчкорд-режиме как на P1; DTIM/DSND-меню общие.

Проверки: fsyntax 0 (Editor/PP оба дерева); verify v15 PASS 1034 x2 (иглы: chorus-def,
P2-имена, lfoJack, numberDrag, plock 163/164/after2, списки у мышки, P-LOCK P2-папка,
MODE-mini, P2 EFFX bind); check_comments 0 x3; Editor 410544 в обоих деревьях.

## 1.8.0 -- P2: полноценный второй движок (страница FX)

1. P2 = второй движок: выход P1 (трека) заводится на вход полноценной FX-машины со СВОЕЙ цепью (SYNT->DIST->SRR->FILT->EQ->ENV->DSND), своим AMP-гейтом (p2_amp_*: ATK/HOLD/DEC/REL + режим old|mnm|vital), LEV (p2_level) и MIX (p2_mix, dry P1 vs wet P2). Гейт стоит на входе цепи: его RELEASE глушит хвосты всего, что до него (в т.ч. сустейны P1-эффектов) -- механизм "заткнуть синт" при note-off-е нет.
2. P2-машина -- только эффект-машины (FORMANT, THRU, REVERB, CHORUS, DYNAMIX, RINGMOD, PHASER, FLANGER); GND = прозрачный проход. Ручки машины p2m<id>_<i>, страницы FILT/EFFX p2_1_*/p2_2_*, режимы цепи p2_mode_filt/dist/dly. Латентность машины компенсируется dryDelay2 (dry = копия входа, wet/dry в фазе).
3. LFO4-6 -- собственные LFO страницы P2 (параметры lfos2, страницы p6/p7/p8): их PAGE таргетит и P1 (P1SY/P1AM/P1FI/P1FX), и P2 (P2SY/P2AM/P2FI/P2FX). Матрица: источники +LFO4/LFO5/LFO6 (в src и aux), цели +P2 132..163 ("P2 SYN n / P2 AMP / P2 FILT / P2 FX ..."), dest-параметр 0..164, все клампы расширены (прицел, addMsegRoute, evaluate с before2/after2). P-LOCK работает на P2-цели. MSEG/ARP общие с P1 (пересечение в матрице).
4. UI (переработано после репорта юзера: НЕ оверлей, а параллельная страница той же панели): кнопка P2 в шапке ПЕРЕКЛЮЧАЕТ лицевую панель P1<->P2 -- те же 6 панелей ячеек и ТЕ ЖЕ координаты ручек: [0] ручки FX-машины (p2m<id>_*), [1] P2 AMP (ATK/HOLD/DEC/REL, ПКМ-курсоры открывают окно огибающей P2) + LEV + MIX, [2] страница LFO4/5/6 (кнопки 1/2/3 в шапке выбирают LFO4/5/6), [3] P2 FILT (p2_1_*), [4] P2 EFFX (p2_2_*), [5] MSEG-быстрые (общие). Кнопка машины открывает список FX-машин (showP2MachineMenu); комбо MODE: FILT/DIST/DLY/AMP -> p2_mode_*/p2_amp_mode (SYNT-комбо скрыто). Прицел LFO-кнопками, бинд в матрицу (LFO4-6 = источники 23..25) и P-LOCK работают в P2 на свои страницы; цели P2-ячеек 132..163 (targetOverride в Cell). В DEST-меню матрицы папка "P2". targetName/destName понимают P2-цели ("P2 ...").choiceName LFO4-6 DEST: PAGE 0..3 = P1SY..P1FX, 4..7 = P2SY..P2FX.
5. Пресеты 1.7.x НЕ мигрируют (по решению: schema bump до 14, новые параметры берут дефолты; старое состояние читается, недостающее = дефолт). PANIC глушит и P2 (envelope2/dryDelay2/machine2/chain2). Звук P1 в дефолте не меняется: P2 MIX = 0 (полностью dry; выбрал машину и поднял MIX -- FX в миксе), LEV 100.
6. Попутные фиксы: NovaData стр.имена FX-машин для p2-выбора (juce::String(name)); chain2.volumeReference задан в prepareToPlay (был мусор); verify-иглы обновлены под 1.8.0 (schema 14, BUILD 1.8.0, 164 цели, источники 0..25/26, CMake 1.8.0). FX-версия плагина не тронута.

## 1.8.0 -- правки по репорту #2 (звук/панель/кнопка)

1. КРИТИЧНО: FX-машина P2 не рендерилась -- machine2.render не вызывался (SYNT-категория P2 молчала, звук проходил только через цепь). Теперь порядок как у FX-версии P1: dryDelay2 -> machine2.render(l,r,n,false) -> гейт envelope2 -> chain2.process.
2. Панель AMP вида P2 = ATK/HOLD/DEC/REL/DIST/VOL/PAN (p2_0_4..6) + прочерк. По обсуждению #4: PORT и LEV с P2 УБРАНЫ -- PORT (глайд) нужен только синту и уже есть на P1; LEV на P2 дубль -- сглаженный p2_level удалён вовсе, фейдер LEV страницы P1 МАСТЕРИТ весь плагин (P2 смешивается до финального gain). P2 MIX -- ПОЛНОСТЬЮ УДАЛЁН (решение #6: «я НЕ просил микс для p2»). Модель P2 = ВСТАВКА: выбранная машина стоит в цепи, сигнал проходит через неё, никакого параллельного микса.
7. По репорту #7: OFF НЕ нужен -- сухой режим P2 это машина THRU (в pre-FX прекрасно работала). Дефолт p2_machine = THRU (звук P1 в дефолте не меняется). Меню/стрелки без OFF.
9. Репорт #8 (тест юзера) -- три фикса:
   а) СИНТ: очень тихо и только в левое ухо. Причина: p2_0_4..6 (DIST/VOL/PAN) читались через getParameter()->getValue() -- это НОРМАЛИЗОВАННЫЕ 0..1, а не 0..127. VOL=0.5 давал ~-48 дБ, PAN=0.5 -- (0.5-64)/64 = -0.99, упор влево. Теперь сырые атомики getRawParameterValue/load.
   б) FX: плагин молчал. Причина: гейт P2 умножал вход на envelope2, а в FX при дефолтном LATCH огибающая не стартует без нот -> amp2=0 -> вставка глушила ВЕСЬ сигнал. Теперь гейт P2 разделяет velocity и MIDI/LATCH-бленд P1 (amp2 = a2*vel у Synth; 1+(a2*vel-1)*blend у FX -- при LATCH гейт прозрачен).
   в) Дефолт гейта = прозрачный: p2_amp HOLD=127 (уровень 1, пока нота держится -- «звук как был»), DEC=127. Гасит только по release (заткнуть синт). Нюанс mnm-kernel: HOLD физически держит ~2 с, потом уходит в DEC; для бесконечного HOLD -- MODE old.
   г) Репорт #9 (юзер): «в оригинале mnm на FX-треке DEC=127 и REL=127 -- ничего не гейтит, хвосты проходят». Дефолты гейта P2 = как в оригинале: HOLD 0, DEC 127, REL 127 (прозрачность дают DEC/REL, не HOLD).
   д) Репорт #9 (юзер): «вот микс я не просил и не место ему в арпе» -- fx_mix (Global dry wet) УДАЛЁН целиком: параметр, wire-enum Mix, меню-строка. FX-версия = вставка, полный wet (глобальный dry/wet -- в DAW; сухое -- машина THRU). Гейт FX остался в списке настроек.
   е) Репорт #10 (юзер): «ладно, добавь свой dry/wet второй страницы на место портаменто» -- p2_mix ВОЗВРАЩЁН как DRY/WET P2 на слоте 7 AMP-страницы (где был PORT). Дефолт 127 (полный wet; THRU = чистый проход). Сухой задерживается dryDelay2 на латентность машины (dry/wet в фазе). Цель матрицы 147 = «P2 AMP MIX» (модулируемая). Убран только ГЛОБАЛЬНЫЙ fx_mix -- он был не про P2 и не место ему в ARP.
8. 1.8.0 перенесён в FX VST ЦЕЛИКОМ: деревья -- один код (вся FX-специфика за isSynthVersion/NOVA_SYNTH), скопированы NovaData/PP.h/PP.cpp/Editor/матрица/DspModes (CMake FX 1.8.0, NovaConfig NOVA_SYNTH=0 нетронут). В FX P2-вход = выход FX-цепи; P2-гейт гейтится тем же MIDI (MIDI LATCH и «послать MIDI, чтобы гейтить» работают как раньше). Цель матрицы "P2 AMP PORT" = прочерк "--" (решётка целей 32 не ломается). В аудио c2[12..14] живые DIST/VOL/PAN (цели 144..146), модулируются.
3. MIX-баг фазы: сухой сигнал для микса был незадержанный (латентность машины двигала wet). Теперь dry = задержанная копия (d2L/d2R), wet/dry в фазе.
4. Стрелки пресета и ЛКМ-драг по имени машины: в виде P2 листают СПИСОК FX-МАШИН (p2_machine), а не машины инструментов; клик по имени открывает список FX-машин (было).
5. Кнопка P1|P2: подпись = ТЕКУЩАЯ страница (P1 -- на первой, P2 -- на второй). Перенесена СЛЕВА от матрицы (x=806), сама MATRIX не тронута (старое место). Кнопка добавлена в LAYOUT EDIT (id "p2") -- теперь её можно выделять и двигать.

## 1.7.11 -- P-LIST прицел + AMP MODE синхронно + чистка воркспейса

1. P-LIST кнопка теперь НАСТОЯЩИЙ прицел: ЛКМ = крестик (клик по ручке лицевой панели = параметр в слот, Elektron-style), драг ЛКМ = листать слоты, ПКМ = список целей. Раньше крестик был только рисунком, а настоящий прицел прятался в отдельной кнопке "P-LOCK" -- она удалена (перекрывала прицел и путала). Слот-кнопка по-прежнему показывает имя параметра слота, теперь шире (160px) -- имя видно целиком.
2. Попутный баг прицела: beginPlockPick клампил слот 0..3 -- прицел молча не работал для слотов 4+. Теперь все 32.
3. Попутный баг DEST-бокса P-LOCK: драг целей листал 0..65 -- PITCH был недостижим. Теперь до 66.
4. AMP MODE синхронно во всех трёх местах: лицевая панель (MODE AMP) теперь old|mnm|vital (было old|mnm без vital), мини-энвелоуп снова показывает MODE-комбо (был скрыт с 1.6.42), большое окно без изменений. Везде один параметр amp_mode -- выбор синхронен.
5. Подсказка в большом энвелоупе теперь описывает и vital (раньше при vital писала про mnm).

## 1.7.10 -- PITCH / RANDOM / AUX / Serum-ручка RATE / компоновка MSEG

1. PITCH починен: P-LIST молча отбрасывал цель 66 (кламп <=65) -- п-лок на питч не работал вообще; теперь plock-массивы 67, plockScaled для PITCH = полутона -24..+24 (нейтраль 64), UNI/BIP оба работают. Прицел-матрица тоже (кламп 0..131).
2. PITCH переехал из папки ARP в папку SYNTH во ВСЕХ списках (матрица DEST, P-LIST, MSEG UNASSIGNED) и получил приписку -- теперь называется "SYNT PITCH".
3. LFO FM чинил попутно: значение DEST 132 резалось jlimit-ом 131 в PP и клампом 130 в прицеле -- цель LFO FM превращалась в ROUTE 64. Теперь 0..132 везде.
4. RANDOM-источник матрицы работал всегда 0 (никто не писал m_random). Теперь сглаженный шум: новая цель каждые ~80 мс, сглаживание ~30 мс.
5. AUX SOURCE у строк 16..63 НЕ СУЩЕСТВОВАЛ (параметры создавались только для 16 строк с 1.6.24, а матрица с 1.7.7 -- 64 строки): aux молча игнорировался, модуляция шла как на 100% при любой глубине. Теперь _aux и _aux_depth у всех 64 строк.
6. AUX как в Serum: выбор AUX-источника сразу ставит глубину 100% (если была 0) -- маршрут не молчит; глубина 0% = модуляция полностью перекрыта (гейт = src x depth/63, это уже было в ядре).
7. MSEG/LFO RATE -- единая ручка как в Serum: один слайдер RATE (ряд 2), семья выбирается кнопками SYNC (BPM-доли / свободные секунды, ПКМ = BPM/TIME/HZ), TRIPLET, DOTTED. Комбо RMODE убрано. Значение по-прежнему числом: SPEED под графом. Параметры не менялись -- старые пресеты читаются без миграции.
8. Компоновка MSEG-страницы: ВСЁ управление собрано в два ряда НАД графом (ряд 1: LOOP SYNC SHAPE страницы STEPS GRID LINK VIEW RETRIG SHIFT; ряд 2: SYNC TRIP DOT KEY LOOP POINT RATE + UNDO/REDO/INVERT/FLIP X/COPY/PASTE). Под графом осталась только матрица маршрутов MSEG (строки, прицелы, маркеры). Граф на 28px ниже и на 28px короче (264 вместо 292) -- влезает в экран.
9. Счётчик задействованных точек -- под кнопкой CLEAR (справа, "N / 256").

## 1.7.9 -- фикс-батч по репорту

1. DTIM/DSND/PP мини-окна: текст больше не под булавкой (сдвинут влево), окна компактнее (DTIM/DSND 272x84, DSND/PP 190x78).
2. TIME у MSEG: исправлена формула (период действительно 1 ms .. 60 s; раньше 1 s .. 60 s).
3. Детач-окно ARP: перетаскивание ЛКМ за любое пустое место (ComponentDragger по DialogWindow).
4. PAGE RND: случайный паттерн теперь ВИДЕН -- призрак (транспон + полоска velocity) в ARP-лейне; публикуется из аудио-потока (arpUiRndT/V).
5. Пресет-лоад/RESET ALL: случайные SOURCES убраны -- r{}_src дефолт 0, миграция schema<13 создаёт ВСЕ 8 полей 64 строк с нулями.
6. CLEAR матрицы: теперь через beginChangeGesture/setValueNotifyingHost/endChangeGesture (хост видит очистку); ПКМ-меню + ONLY SOURCES / ONLY TARGETS.
7. Матрица: по умолчанию 16 видимых строк; занятый слот добавляет строку (16..64, timerCallback).
8. MSEG retrig: имя ENVELOPE возвращено (VITAL -- только про AMP-огибающую); вся VITAL-функциональность на месте.
9. AMP MODE: добавлен пункт "vital" (old|mnm|vital) -- витальные кривые A/D/R с дефолтами 70/55/55 (юзер-кривые приоритетны).
10. Рисование MSEG: точки применяются ДО setMsegPointCount во всех путях (restore/writeStroke/vitalStroke/filesDropped/clear/generate) -- граф больше не дёргается; ПКМ по VITAL = штамп формы волны (SINE/TRI/SAW UP/SAW DOWN/PULSE 25/PULSE 50/RANDOM SMOOTH); лестница до 256 ступеней.
11. Скорость MSEG: числовой индикатор "SPEED ..." под графом (TEMPO-доли / TIME-период / HZ).
12. SYN TUNE -- первым в папке машины во всех списках (DEST-меню, P-LIST, MSEG UNASSIGNED).
13. P-LIST: кнопка с крестиком-прицелом как DEST; слот общий между синх-страницей и детач-окном (plockUiSlot в Processor) -- имя параметра показывается синхронно.
14. Матрица: хитбокс перетаскивания колонки <= 6 px (соседние не тянутся); ширина колонок cap 280 (было 400).

## 1.7.8 -- патч 2/2: Vital-батч (рейт-режимы LFO, VITAL-рисование, loop/sustain/loop-point/keytrack, LFO FM)
- РЕЙТ-РЕЖИМЫ LFO/MSEG (по референсу Vital TempoSelector/SynthLfo): RATE теперь
  0..1 = позиция в семье R MODE: TEMPO (32 BAR..1 BAR..1/256 + FAST, sync),
  TRIPLET, DOTTED (те же доли с триолями/точками), TIME (период 1ms..60s),
  HZ (лог-кривая 0.001 -> 0.01 -> 0.1 -> 10 -> 20 kHz; SHIFT замедляет).
  Миграция schema 13: старый rate 0.05..20 -> ближайшая доля TEMPO (если
  SYNC был ON) или та же частота в HZ (если OFF) -- звук сохранён.
- LOOP/SUSTAIN/LOOP POINT/LOOP HOLD (семантика Vital synth_lfo): список режимов
  MSEG расширен до RETRIG|VITAL|FREE|SUSTAIN|LOOP POINT|LOOP HOLD (ENVELOPE
  переименован в VITAL, значение 1 прежнее). SUSTAIN: играет до LOOP POINT,
  держит её, после note-off доигрывает до конца. LOOP POINT: после первого
  прохода крутит хвост от точки до конца. LOOP HOLD: пока нота держится --
  циклит 0..точку, на note-off доигрывает хвост. LOOP POINT -- новый параметр
  mseg*_lpoint (0..1, дефолт 0.5) со слайдером под маршрутами.
- KEYTRACK: галка KEY -- скорость LFO/MSEG едет с высотой последней ноты
  (+1 октава = x2), как в Vital.
- LFO FM: новая цель матрицы 131 "LFO FM" (DEST 0..132) -- прибавка к частоте
  в октавах (глубина 64 ~ +13.3 октавы; с базы 1 Hz -- до ~10 kHz, с HZ-ручки
  на 20 kHz FM уводит выше; evaluate получил 6-й аргумент, звук прежний).
- VITAL-рисование: пятая кнопка форм "VITAL" -- перо со сглаживанием
  (скользящее окно) и лимитом 256 точек (без прореживания до 32); старые
  DRAW/DRAW STEP/RAND DRAW 1/2 НЕ тронуты. Ступени STEPS тоже до 256.
- HOST: рестарт фаз MSEG на старте транспорта оставлен как в 1.7.7 (фазы от
  старта плейбэка -- эквивалент Vital kSync "sync to transport"); отдельной
  галки нет по решению "как в Vital".
- Счётчик точек "/ 256 POINTS"; сетка/кнопки 1.7.7 без изменений.
- Версии -> 1.7.8; verify: +29 якорей 1.7.8, обновлены stale (kTargetCount 132,
  schema 13, MsegRetrig+4*mi, evaluate 6-arg, VERSION 1.7.8). cp-sync 9 файлов
  в FX; fsyntax 0 в обоих деревьях; verify PASS в обоих.
